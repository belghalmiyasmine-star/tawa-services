---
phase: 05-recherche-decouverte
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/(client)/services/[serviceId]/page.tsx
  - src/features/search/components/ServiceDetailClient.tsx
  - src/features/search/components/ServiceImageGallery.tsx
  - src/features/search/components/ProviderMiniCard.tsx
autonomous: true
requirements: [SRCH-05]

must_haves:
  truths:
    - "Client can view a service detail page with full description, photos, price, and provider info"
    - "Service detail page shows image gallery, category badge, pricing in TND"
    - "Provider mini-card shows photo, name, rating, city, and verified badge"
    - "Reserver/Demander un devis buttons are visible based on pricing type"
    - "Similar services section shows related services at the bottom"
  artifacts:
    - path: "src/app/[locale]/(client)/services/[serviceId]/page.tsx"
      provides: "Service detail page with SSR data fetching"
      exports: ["default", "generateMetadata"]
    - path: "src/features/search/components/ServiceDetailClient.tsx"
      provides: "Client-side interactive wrapper for service detail"
    - path: "src/features/search/components/ServiceImageGallery.tsx"
      provides: "Image gallery component with thumbnail navigation"
    - path: "src/features/search/components/ProviderMiniCard.tsx"
      provides: "Reusable provider mini-card with photo, name, rating, city, badge"
  key_links:
    - from: "src/app/[locale]/(client)/services/[serviceId]/page.tsx"
      to: "prisma.service"
      via: "SSR query with includes"
      pattern: "prisma\\.service\\.findUnique"
    - from: "src/features/search/components/ProviderMiniCard.tsx"
      to: "/providers/[providerId]"
      via: "Link to public provider profile"
      pattern: "Link.*href.*providers"
---

<objective>
Build the service detail page where clients see full service information with image gallery, provider mini-card, pricing, and action buttons.

Purpose: SRCH-05 requires clients to view complete service information before booking. This page is the conversion point where browsing turns into action (booking or quote request). It also links back to the provider profile.

Output: Service detail page at `/services/[serviceId]` with gallery, provider card, inclusions/exclusions, and similar services.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prisma/schema.prisma
@src/features/provider/components/PublicServiceCard.tsx
@src/app/[locale]/(client)/providers/[providerId]/page.tsx
@src/components/shared/TrustBadges.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create service detail page with SSR data and image gallery</name>
  <files>src/app/[locale]/(client)/services/[serviceId]/page.tsx, src/features/search/components/ServiceImageGallery.tsx</files>
  <action>
    1. Create `src/features/search/components/ServiceImageGallery.tsx`:
       - "use client" component
       - Props: `photos: string[]`, `title: string`
       - State: `selectedIndex` (number, default 0)
       - Layout: Large main image (aspect-[4/3]) + row of thumbnails below (max 5)
       - Click thumbnail to change main image
       - If no photos, show gradient placeholder with tool emoji (match PublicServiceCard style)
       - Rounded-xl corners, overflow hidden, Airbnb rounded style
       - Mobile: full width. Desktop: max-w-2xl

    2. Create `src/app/[locale]/(client)/services/[serviceId]/page.tsx`:
       - Server component with async params `{ locale, serviceId }`
       - `generateMetadata`: Fetch service title + description for SEO (title: "{service.title} | Tawa Services", description: first 160 chars of description)
       - Prisma query: `prisma.service.findUnique` where `{ id: serviceId, isDeleted: false, status: "ACTIVE" }`
       - Include: `category` (with parent), `provider` (with user, trustBadges where isActive, delegations with delegation.gouvernorat, certifications where not deleted)
       - Call `notFound()` if service is null
       - Increment `viewCount` via `prisma.service.update({ data: { viewCount: { increment: 1 } } })` — fire-and-forget (void, no await blocking render)
       - Layout (mobile-first, responsive):
         - Top: `<ServiceImageGallery>` with `service.photoUrls`
         - Below gallery, 2-column layout on desktop (lg:grid-cols-3), stacked on mobile:
           - Left column (lg:col-span-2): Category badge, Title (h1), Price in TND (bold, "Sur devis" for SUR_DEVIS), Duration if set, Description (whitespace-pre-line), Inclusions list (green checkmark icons), Exclusions list (red X icons), Conditions if set
           - Right column (lg:col-span-1, sticky top-20): ProviderMiniCard, Action buttons: "Reserver" (Button primary, for FIXED pricing) or "Demander un devis" (Button primary, for SUR_DEVIS), "Contacter" (Button outline). Buttons link to `#` for now (Phase 6 booking, Phase 9 messaging). Use i18n keys from "search" namespace.
       - Below: Similar services section — query up to 4 services in same category (exclude current), render with PublicServiceCard. Show heading "Services similaires". If no similar services, hide section entirely.
       - Use `getTranslations("search")` for all labels
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to /services/[any-service-id] — should display service detail with gallery, description, provider card, and action buttons</manual>
  </verify>
  <done>Service detail page renders with image gallery, full service info, inclusions/exclusions, category badge, TND pricing. Similar services section shows related services from same category.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProviderMiniCard and wire PublicServiceCard links</name>
  <files>src/features/search/components/ProviderMiniCard.tsx, src/features/provider/components/PublicServiceCard.tsx, src/features/search/components/ServiceDetailClient.tsx</files>
  <action>
    1. Create `src/features/search/components/ProviderMiniCard.tsx`:
       - Server component (no "use client")
       - Props: `provider: { id, displayName, photoUrl, rating, ratingCount, kycStatus, delegations, trustBadges? }`
       - Layout: Rounded-xl card with border, padding
         - Top: Avatar (64px circle) + Name + Rating stars + Review count
         - Below: City from first delegation gouvernorat
         - Verified badge (checkmark icon, green) if kycStatus === "APPROVED"
         - Trust badges row if provided (use TrustBadges component pattern — inline small badges)
         - "Voir le profil" link to `/providers/{provider.id}` using `Link` from `@/i18n/routing`
       - Style: Consistent with Airbnb rounded style, white bg, shadow-sm, hover:shadow-md

    2. Create `src/features/search/components/ServiceDetailClient.tsx`:
       - "use client" wrapper for interactive elements on service detail page
       - Handles the "Reserver"/"Demander un devis"/"Contacter" buttons
       - Props: `serviceId: string`, `pricingType: "FIXED" | "SUR_DEVIS"`, `providerId: string`
       - Buttons: Primary CTA based on pricingType, secondary "Contacter" outline button
       - All buttons link to `#` with a toast "Disponible prochainement" (Phase 6 booking, Phase 9 messaging) — use `useToast` from `@/components/ui/use-toast`
       - Import `useTranslations("search")` for button labels

    3. Update `src/features/provider/components/PublicServiceCard.tsx`:
       - Change the outer `<div>` to a `<Link>` from `@/i18n/routing`
       - Set `href` to `/services/${service.id}` — this replaces the Phase 4 placeholder `<div>` with an actual link
       - Keep the same visual styling (group flex flex-col overflow-hidden rounded-xl...)
       - Add `as never` type cast on href if needed for TypeScript typed routes
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>PublicServiceCard now links to /services/[id]. ProviderMiniCard shows provider info. Buttons show "coming soon" toast.</manual>
  </verify>
  <done>ProviderMiniCard displays provider photo, name, rating, city, verified badge with link to profile. PublicServiceCard now links to service detail page. Action buttons show placeholder toast for future phases.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Service detail page renders at /services/[serviceId]
- Image gallery shows service photos with thumbnail navigation
- ProviderMiniCard links to /providers/[providerId]
- PublicServiceCard links to /services/[serviceId]
- Action buttons display placeholder toast
</verification>

<success_criteria>
- Client can navigate to /services/[id] and see complete service information
- Image gallery supports multiple photos with thumbnail selection
- Provider mini-card shows photo, name, rating, city, verified badge
- Similar services section shows related services
- PublicServiceCard (from Phase 4) now links to service detail page
</success_criteria>

<output>
After completion, create `.planning/phases/05-recherche-decouverte/05-02-SUMMARY.md`
</output>
