---
phase: 05-recherche-decouverte
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/[locale]/(client)/services/page.tsx
  - src/app/[locale]/(client)/services/[categorySlug]/page.tsx
  - src/features/search/components/SearchFilters.tsx
  - src/features/search/components/SearchResultsGrid.tsx
  - src/features/search/components/SearchPagination.tsx
  - src/features/search/components/SearchSortSelect.tsx
  - src/features/search/components/CategoryGrid.tsx
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-04]

must_haves:
  truths:
    - "Client can browse categories grid and click to see services in that category"
    - "Client can filter services by city/delegation and see only matching providers"
    - "Client can sort results by rating, price asc/desc, newest"
    - "Client can filter by verified only and price range min/max"
    - "Search results display as grid of ServiceCards with pagination"
    - "Filters are in left sidebar on desktop, collapsible Sheet on mobile"
  artifacts:
    - path: "src/app/[locale]/(client)/services/page.tsx"
      provides: "Main search results page with filters and results grid"
    - path: "src/app/[locale]/(client)/services/[categorySlug]/page.tsx"
      provides: "Category-specific results page"
    - path: "src/features/search/components/SearchFilters.tsx"
      provides: "Left sidebar filters component (city, price, verified, pricingType)"
    - path: "src/features/search/components/SearchResultsGrid.tsx"
      provides: "Grid of PublicServiceCard results with loading state"
    - path: "src/features/search/components/SearchPagination.tsx"
      provides: "Pagination controls"
    - path: "src/features/search/components/CategoryGrid.tsx"
      provides: "Grid of category cards with icons and service counts"
  key_links:
    - from: "src/app/[locale]/(client)/services/page.tsx"
      to: "/api/search/services"
      via: "Server-side fetch for SSR results"
      pattern: "fetch.*api/search/services"
    - from: "src/features/search/components/SearchFilters.tsx"
      to: "URL searchParams"
      via: "useRouter push with updated query params"
      pattern: "useRouter|useSearchParams"
    - from: "src/features/search/components/CategoryGrid.tsx"
      to: "/services/[categorySlug]"
      via: "Link to category-filtered results"
      pattern: "Link.*href.*services/"
---

<objective>
Build the category browsing page and search results page with sidebar filters, sorting, and pagination.

Purpose: This is the main discovery surface — how clients find services. Category browsing (SRCH-01) lets users explore by type. Filters by city/delegation (SRCH-02) and sort/filter controls (SRCH-04) refine results. The search results page with ServiceCard grid and pagination is the core browsing experience.

Output: Categories grid page, search results page with sidebar filters (Sheet on mobile), sort dropdown, and pagination.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-recherche-decouverte/05-01-SUMMARY.md
@src/lib/validations/search.ts
@src/lib/constants.ts
@src/features/provider/components/PublicServiceCard.tsx
@src/components/ui/sheet.tsx
@src/components/ui/select.tsx
@src/components/ui/checkbox.tsx
@src/components/ui/input.tsx
@src/components/ui/slider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create category grid and category-filtered results page</name>
  <files>src/features/search/components/CategoryGrid.tsx, src/app/[locale]/(client)/services/[categorySlug]/page.tsx</files>
  <action>
    1. Create `src/features/search/components/CategoryGrid.tsx`:
       - Server component (no "use client")
       - Props: `categories: Array<{ id, name, slug, icon, serviceCount }>`
       - Renders a responsive grid: 2-col mobile, 3-col sm, 4-col md, 5-col lg
       - Each card: rounded-xl border bg-white shadow-sm hover:shadow-md transition
         - Icon (emoji string from DB, centered, text-3xl)
         - Category name (font-medium text-sm)
         - Service count ("X services" in text-xs text-gray-500)
       - Each card is a `Link` from `@/i18n/routing` to `/services/{slug}`
       - If categories array is empty, show a "No categories" message

    2. Create `src/app/[locale]/(client)/services/[categorySlug]/page.tsx`:
       - Server component with params `{ locale, categorySlug }`
       - `generateMetadata`: Look up category by slug for SEO title
       - Fetch category from DB: `prisma.category.findUnique({ where: { slug: categorySlug } })`
       - If not found, call `notFound()`
       - If category has children (is a parent), fetch children as "subcategory filter chips"
       - Fetch services from `/api/search/services` internally using direct Prisma query (NOT fetch — use the same query logic, or call the API with absolute URL). Better approach: use a shared `searchServices()` function. For simplicity, directly use Prisma query matching the API logic with `category` filter pre-set.
       - Actually, the simplest approach: redirect/rewrite to `/services?category={categorySlug}` using the main search results page. Create this page as a thin wrapper that:
         a. Validates the categorySlug exists
         b. Renders the same search results page layout but with category pre-selected
         c. Shows subcategory chips at top if category has children
       - Use `getTranslations("search")` for labels
       - Layout: Breadcrumb (Accueil > Categories > {name}), optional subcategory chips row, then SearchResultsGrid + filters
       - Import SearchFilters, SearchResultsGrid, SearchPagination, SearchSortSelect from search components

    **Important implementation note:** Both `/services` (main search) and `/services/[categorySlug]` (category) share the same filter/results layout. Extract the shared layout into the search components so both pages compose them identically. The category page just pre-sets the category filter.
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to /services/plomberie — should show filtered results for plomberie category</manual>
  </verify>
  <done>Category grid renders clickable cards with icons and counts. Category page shows filtered services for the selected category with subcategory chips if applicable.</done>
</task>

<task type="auto">
  <name>Task 2: Create search results page with filters, sort, and pagination</name>
  <files>src/app/[locale]/(client)/services/page.tsx, src/features/search/components/SearchFilters.tsx, src/features/search/components/SearchResultsGrid.tsx, src/features/search/components/SearchPagination.tsx, src/features/search/components/SearchSortSelect.tsx</files>
  <action>
    1. Create `src/features/search/components/SearchSortSelect.tsx`:
       - "use client" component
       - Props: `currentSort: string`
       - Renders a `<Select>` (shadcn) with sort options: Pertinence, Prix croissant, Prix decroissant, Meilleure note, Plus recents
       - On change: update URL searchParams with new `sort` value using `useRouter` from `@/i18n/routing` and `useSearchParams`
       - Use `useTranslations("search")` for labels

    2. Create `src/features/search/components/SearchFilters.tsx`:
       - "use client" component
       - Props: `categories: Array<{ name, slug }>`, `currentFilters: { category?, city?, minPrice?, maxPrice?, pricingType?, verified?, minRating? }`
       - Desktop: renders as left sidebar div (w-64, sticky top-20)
       - Mobile: renders inside a `<Sheet>` (shadcn) triggered by a "Filtres" button with filter icon
       - Filter sections (each collapsible with Chevron toggle):
         a. **Categorie**: List of category names as radio buttons or clickable items. Highlight active. "Toutes les categories" option to clear.
         b. **Ville**: `<Select>` dropdown with TUNISIA_GOUVERNORATS from constants.ts + "Toutes les villes" option
         c. **Fourchette de prix**: Two `<Input type="number">` for min/max (side by side). Use debounce 500ms before updating URL.
         d. **Type de tarification**: Radio group: "Tous", "Prix fixe", "Sur devis"
         e. **Verifies uniquement**: `<Checkbox>` (Switch component from shadcn)
       - "Effacer les filtres" button at bottom to reset all filters
       - On any filter change: update URL searchParams preserving other params, reset page to 1. Use `useRouter().push()` with updated params.
       - Use `useTranslations("search")` for all labels

    3. Create `src/features/search/components/SearchResultsGrid.tsx`:
       - Server component (no "use client")
       - Props: `services: Array<service data matching PublicServiceCard props>`
       - Renders grid: 1-col mobile, 2-col sm, 3-col lg
       - Each item rendered as `<PublicServiceCard>` (from Phase 4, now with link)
       - If empty, show empty state: icon + "Aucun resultat trouve" + "Essayez de modifier vos filtres" text
       - Show result count at top: "{count} resultats"

    4. Create `src/features/search/components/SearchPagination.tsx`:
       - "use client" component
       - Props: `currentPage: number`, `totalPages: number`
       - Renders pagination bar: Previous | 1 2 3 ... N | Next
       - Previous/Next buttons disabled at boundaries
       - On page click: update URL searchParams `page` value
       - Show "Page X sur Y" text
       - Use `useTranslations("search")` for labels

    5. Create `src/app/[locale]/(client)/services/page.tsx`:
       - Server component
       - `generateMetadata`: "Rechercher des services | Tawa Services"
       - Read URL searchParams (Next.js 15: `searchParams` is a Promise, await it)
       - Parse with searchParamsSchema (safe parse, use defaults on failure)
       - Fetch services via Prisma directly (same logic as API route — or fetch from API):
         Build the Prisma where/orderBy/pagination from parsed params (reuse logic from 05-01 API — consider extracting to a shared `buildSearchQuery()` utility in `src/features/search/lib/search-query.ts`)
       - Fetch categories for filter sidebar: `prisma.category.findMany({ where: { isActive: true, isDeleted: false, parentId: null }, orderBy: { sortOrder: "asc" } })`
       - Render layout:
         - Top: Search heading + result count + SearchSortSelect
         - Two-column on desktop (lg:grid-cols-4): Left col (lg:col-span-1) = SearchFilters, Right col (lg:col-span-3) = SearchResultsGrid + SearchPagination
         - On mobile: SearchFilters hidden behind Sheet trigger button, results full width
       - If no `q` and no `category` and no filters, show CategoryGrid at top before results (homepage-like browsing mode)
       - Pass all props to child components
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to /services — should show category grid and/or results. Apply filters via sidebar — URL updates, results refresh. Change sort — results reorder. Click pagination — page changes.</manual>
  </verify>
  <done>Search results page displays services grid with left sidebar filters (Sheet on mobile), sort dropdown, and pagination. Filters update URL params and results refresh via server re-render. Category grid shows when no filters active.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /services page renders with CategoryGrid, then results when filters applied
- /services/[categorySlug] renders category-filtered results
- Filters (city, price, verified, pricing type) update URL and filter results
- Sort dropdown changes result order
- Pagination navigates between pages
- Mobile: filters appear in Sheet component
</verification>

<success_criteria>
- Client can browse category grid on /services page
- Client can click a category card and see filtered results
- Client can filter by city, price range, verified, pricing type
- Client can sort by rating, price, newest
- Pagination works correctly
- Mobile filters use Sheet component
</success_criteria>

<output>
After completion, create `.planning/phases/05-recherche-decouverte/05-03-SUMMARY.md`
</output>
