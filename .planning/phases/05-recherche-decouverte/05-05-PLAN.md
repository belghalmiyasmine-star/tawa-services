---
phase: 05-recherche-decouverte
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - src/app/[locale]/(client)/page.tsx
  - src/components/layout/Navbar.tsx
  - src/middleware.ts
autonomous: false
requirements: [SRCH-01, SRCH-05]

must_haves:
  truths:
    - "Homepage categories link to /services/[categorySlug] with real DB data"
    - "Navbar categories dropdown uses DB categories (not hardcoded placeholders)"
    - "Full search-to-detail flow works: search → results → service detail → provider profile"
    - "All new routes are listed in middleware PUBLIC_PATHS"
  artifacts:
    - path: "src/app/[locale]/(client)/page.tsx"
      provides: "Updated homepage with DB-driven category grid"
    - path: "src/components/layout/Navbar.tsx"
      provides: "Navbar with DB-driven categories and working autocomplete"
  key_links:
    - from: "src/app/[locale]/(client)/page.tsx"
      to: "/services/[categorySlug]"
      via: "CategoryGrid links"
      pattern: "Link.*href.*services/"
    - from: "src/components/layout/Navbar.tsx"
      to: "/services/[slug]"
      via: "Category dropdown links"
      pattern: "Link.*href.*services/"
---

<objective>
Wire all Phase 5 components together: update homepage with DB-driven categories, update Navbar category dropdown from DB, ensure middleware allows new routes, and verify the complete search-to-detail flow end-to-end.

Purpose: Plans 01-04 built individual pieces. This plan connects them into a cohesive user experience — from homepage browsing through category selection, search filtering, to viewing a service detail and provider profile.

Output: Fully integrated search & discovery flow with DB-driven data replacing all Phase 1 placeholders.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-recherche-decouverte/05-01-SUMMARY.md
@.planning/phases/05-recherche-decouverte/05-02-SUMMARY.md
@.planning/phases/05-recherche-decouverte/05-03-SUMMARY.md
@.planning/phases/05-recherche-decouverte/05-04-SUMMARY.md
@src/app/[locale]/(client)/page.tsx
@src/components/layout/Navbar.tsx
@src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update homepage and Navbar with DB-driven categories</name>
  <files>src/app/[locale]/(client)/page.tsx, src/components/layout/Navbar.tsx, src/middleware.ts</files>
  <action>
    1. Update `src/app/[locale]/(client)/page.tsx`:
       - Convert to async server component (it currently uses `useTranslations` — change to `getTranslations`)
       - Remove hardcoded `CATEGORY_ITEMS` array
       - Fetch categories from DB: `prisma.category.findMany({ where: { isActive: true, isDeleted: false, parentId: null }, select: { id: true, name: true, slug: true, icon: true, _count: { select: { services: { where: { status: "ACTIVE", isDeleted: false } } } } }, orderBy: { sortOrder: "asc" }, take: 10 })`
       - Render CategoryGrid component (from Plan 03) with fetched categories mapped to `{ id, name, slug, icon, serviceCount: category._count.services }`
       - Keep the Hero section and "How it works" section
       - Update the "Rechercher des services" button link to `/services` (should already be correct)
       - Add a search bar (SearchAutocomplete from Plan 04) in the hero section below subtitle, centered, max-w-lg
       - Use `getTranslations("home")` and `getTranslations("categories")`

    2. Update `src/components/layout/Navbar.tsx`:
       - The Navbar is a "use client" component — it cannot directly fetch from DB
       - Approach: Fetch categories from the API `/api/search/categories` on mount using `useEffect` + `fetch`, or pass as props from layout
       - Better approach for client component: Use `useEffect` to fetch categories from `/api/search/categories` on mount. Cache in state. Show skeleton while loading.
       - Replace hardcoded `CATEGORIES` array with dynamic data from API
       - Each dropdown item links to `/services/{slug}` (already the current pattern)
       - Add a "Voir toutes les categories" item at the bottom linking to `/services`
       - Keep the existing SearchAutocomplete integration from Plan 04

    3. Update `src/middleware.ts`:
       - Verify `/services` is already in PUBLIC_PATHS (it is — confirmed from earlier reading)
       - The `/services/[categorySlug]` and `/services/[serviceId]` routes fall under the `/services` prefix, so they are already covered
       - No changes needed if the prefix match `pathname.startsWith(publicPath + "/")` covers all sub-routes (it does)
       - Verify the `/api/search` routes are also accessible (API routes bypass the middleware path check — they are handled by Next.js API routes). Confirm this is the case. If `/api` routes need explicit PUBLIC_PATHS entries, add `/api/search`.
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Load homepage — categories should come from DB (not hardcoded). Navbar dropdown shows DB categories. Search bar in hero works.</manual>
  </verify>
  <done>Homepage displays DB-driven category grid with real service counts. Navbar category dropdown loads from API. Search bar with autocomplete visible in hero section. All routes accessible without auth.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete search & discovery flow</name>
  <files>N/A — checkpoint verification only</files>
  <action>
    Human verification of the complete Phase 5 search and discovery system.
    What was built:
    - Homepage with DB-driven categories and search bar
    - Category browsing page with service grid
    - Search results page with sidebar filters (Sheet on mobile), sort, pagination
    - Autocomplete search with 300ms suggestions
    - Service detail page with image gallery, provider mini-card, action buttons
    - PublicServiceCard links to service detail
    - Provider mini-card links to provider profile
  </action>
  <verify>
    Manual verification steps:
    1. Visit the homepage (`/`) — verify categories display with service counts from DB, search bar visible
    2. Click a category card — verify navigation to `/services/{slug}` with filtered results
    3. On search results page: apply city filter — verify results filter by city
    4. Change sort to "Prix croissant" — verify services reorder by price
    5. Toggle "Verifies uniquement" — verify only KYC-approved providers shown
    6. Set min/max price filter — verify price range filtering
    7. Click pagination — verify page changes
    8. Type in search bar (Navbar or hero) — verify autocomplete suggestions appear within ~300ms
    9. Click a service card — verify navigation to `/services/{serviceId}` detail page
    10. On service detail: verify image gallery, description, inclusions/exclusions, price, duration visible
    11. Verify ProviderMiniCard shows provider info with link to `/providers/{id}`
    12. Click "Voir le profil" — verify navigation to public provider profile
    13. On mobile viewport (< 768px): verify filters appear in Sheet (slide-out panel)
    14. Verify bottom nav search icon navigates to /services
  </verify>
  <done>User confirms the complete search and discovery flow works end-to-end. Type "approved" if flow works, or describe issues found.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Homepage shows DB categories with counts
- Full flow: homepage → category → results → filters → service detail → provider profile
- Autocomplete suggestions appear in < 300ms
- Mobile Sheet filters work
- All 5 SRCH requirements satisfied
</verification>

<success_criteria>
- Complete search & discovery flow works end-to-end
- No hardcoded placeholder categories remain (all from DB)
- All Phase 5 requirements (SRCH-01 through SRCH-05) are verifiable
- Mobile-first responsive design maintained
</success_criteria>

<output>
After completion, create `.planning/phases/05-recherche-decouverte/05-05-SUMMARY.md`
</output>
