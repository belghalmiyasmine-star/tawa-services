---
phase: 12-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/fr.json
  - src/features/search/components/SearchAutocomplete.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/Navbar.tsx
autonomous: true
requirements: [BUGF-01, BUGF-02, BUGF-03, BUGF-04]
must_haves:
  truths:
    - "French accented characters (e with accent, c cedilla, a grave, etc.) display correctly in all UI text — no garbled or missing characters"
    - "Search autocomplete categories show lucide icon components, not raw icon name strings like 'wrench' or 'paintbrush'"
    - "Footer links for FAQ, CGU, Contact, Comment ca marche navigate to the correct paths without 404"
    - "Client navbar dashboard link points to /dashboard instead of / for CLIENT users"
  artifacts:
    - path: "src/messages/fr.json"
      provides: "French i18n translations with proper accented characters"
      contains: "Réessayer"
    - path: "src/features/search/components/SearchAutocomplete.tsx"
      provides: "Autocomplete with dynamic lucide icon rendering"
      contains: "lucide-react"
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer with correct page links"
      contains: "/faq"
    - path: "src/components/layout/Navbar.tsx"
      provides: "Navbar with correct dashboard link for CLIENT role"
      contains: "/dashboard"
  key_links:
    - from: "src/features/search/components/SearchAutocomplete.tsx"
      to: "lucide-react"
      via: "dynamic icon mapping from string name to component"
      pattern: "icons\\[.*\\]|iconMap"
---

<objective>
Fix four frontend/UI bugs: French accent encoding in i18n, autocomplete icon rendering, footer navigation links, and navbar dashboard link for CLIENT users.

Purpose: These are the most visible bugs — garbled text, missing icons, and broken navigation undermine the platform's credibility.
Output: Correct French text, rendered icon components, working footer/navbar links.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/messages/fr.json
@src/features/search/components/SearchAutocomplete.tsx
@src/components/layout/Footer.tsx
@src/components/layout/Navbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix French accents in fr.json i18n file</name>
  <files>src/messages/fr.json</files>
  <action>
    Search through the entire src/messages/fr.json file and replace all unaccented French words with their properly accented versions. This is a systematic find-and-replace task. Key patterns to fix:

    - "Reessayer" → "Réessayer"
    - "Precedent" → "Précédent"
    - "Categorie" → "Catégorie"
    - "Rechercher" → stays the same (no accent needed)
    - "Modifier" → stays the same
    - "Filtrer" → stays the same
    - "Supprime" → "Supprimé"
    - "Ajoute" → "Ajouté"
    - All words containing: é, è, ê, ë, à, â, ç, ù, û, ô, î, ï
    - Watch for: "Créer", "Détails", "Résumé", "Prénom", "Général", "Sécurité", "Vérification", "Téléphone", "Réservation", "Complété", "Validé", "Résolu", "Activé", "Désactivé", "Préférence"
    - Common patterns: words ending in -ee should be -ée, words ending in -e that mean past participle should be -é
    - "Completé" → "Complété" (but really "Complété" → keep)
    - Words with "e" before double consonant often need accent: "Général", "Spécial", etc.

    Do NOT change JSON key names — only change the string values.
    Ensure the file is saved as UTF-8 (it should already be).
    Be thorough — scan every single value in the file.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
    <manual>Open the app and verify French text displays accents correctly (é, è, ç, à, etc.)</manual>
  </verify>
  <done>All French text values in fr.json have correct accented characters — no "Reessayer", "Precedent", "Categorie" etc. remaining as unaccented</done>
</task>

<task type="auto">
  <name>Task 2: Fix autocomplete icons, footer links, and navbar dashboard link</name>
  <files>src/features/search/components/SearchAutocomplete.tsx, src/components/layout/Footer.tsx, src/components/layout/Navbar.tsx</files>
  <action>
    **SearchAutocomplete.tsx — BUGF-02: Map icon string names to lucide components**

    The category `icon` field is a string like "wrench", "paintbrush-vertical", "sparkles" from the database (stored in the Category model). Currently line 254 renders `{cat.icon}` as raw text. Fix this:

    1. Import the `icons` object from lucide-react: `import { icons } from "lucide-react";`
    2. Create a helper function to convert kebab-case icon names to PascalCase (e.g., "paintbrush-vertical" → "PaintbrushVertical"):
       ```typescript
       function getLucideIcon(name: string | null) {
         if (!name) return null;
         const pascalCase = name.split("-").map(s => s.charAt(0).toUpperCase() + s.slice(1)).join("");
         const IconComponent = icons[pascalCase as keyof typeof icons];
         return IconComponent ? <IconComponent className="h-4 w-4" /> : null;
       }
       ```
    3. Replace line 254 `{cat.icon && <span className="text-base">{cat.icon}</span>}` with:
       `{cat.icon ? getLucideIcon(cat.icon) : null}`
       If getLucideIcon returns null (icon name not found), show nothing rather than raw text.

    **Footer.tsx — BUGF-03: Ensure footer links point to correct paths**

    The footer already links to `/faq`, `/contact`, `/terms`, `/privacy`, `/how-it-works`. These pages will be created in Phase 13. For now, the links are correct paths. Verify that the "Comment ça marche" link is `/how-it-works` and the CGU link is `/terms`. If any are wrong, fix them. The footer should have these links:
    - FAQ → `/faq`
    - Contact → `/contact`
    - CGU (Terms) → `/terms`
    - Privacy → `/privacy`
    - How it works → `/how-it-works`

    The footer looks correct already. No changes needed unless a link path is wrong.

    **Navbar.tsx — BUGF-04: Fix CLIENT dashboard link**

    On line 231, the CLIENT user's dashboard link currently points to `"/"` (homepage):
    ```tsx
    href={session.user.role === "PROVIDER" ? "/provider/dashboard" : "/"}
    ```
    Change the CLIENT path from `"/"` to `"/dashboard"`:
    ```tsx
    href={session.user.role === "PROVIDER" ? "/provider/dashboard" : "/dashboard"}
    ```
    This ensures CLIENT users clicking "Dashboard" in the user menu go to `/dashboard` (the client dashboard page at `src/app/[locale]/(client)/dashboard/page.tsx`).
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
    <manual>Verify autocomplete shows icons not text, footer links work, navbar dashboard goes to /dashboard</manual>
  </verify>
  <done>Autocomplete categories render lucide icon components instead of string names; footer links point to correct pages; CLIENT navbar dashboard link goes to /dashboard</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. French text in the UI shows proper accents (é, è, ç, à, etc.)
3. Search autocomplete dropdown shows icon graphics beside category names
4. Footer links navigate to /faq, /contact, /terms, /privacy, /how-it-works
5. CLIENT user menu "Dashboard" link navigates to /dashboard
</verification>

<success_criteria>
- fr.json contains properly accented French throughout (grep for common unaccented patterns returns 0 matches)
- SearchAutocomplete renders icon components dynamically from string names
- Footer and navbar links are correct
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-bug-fixes/12-01-SUMMARY.md`
</output>
