---
phase: 04-profil-prestataire-services
plan: 05
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - src/app/[locale]/(client)/providers/[providerId]/page.tsx
  - src/features/provider/components/PublicProfileHeader.tsx
  - src/features/provider/components/PublicProfileStats.tsx
  - src/features/provider/components/PublicProfileAbout.tsx
  - src/features/provider/components/PublicServiceCard.tsx
  - src/features/provider/components/CertificationsList.tsx
  - src/features/provider/components/CertificationUploader.tsx
autonomous: false
requirements: [PROF-07, PROF-03]

must_haves:
  truths:
    - "Anyone can view a provider's public profile at /providers/[id]"
    - "Public profile displays provider name, photo, bio, verified badge, and member since date"
    - "Public profile shows real-time stats: missions completed, average rating, total reviews, response time"
    - "Public profile lists all active services with prices and categories"
    - "Public profile has tabs: Services, Avis, A propos"
    - "A propos tab shows bio, languages, experience, intervention zone, certifications"
    - "Provider can upload and manage certifications from edit profile page"
  artifacts:
    - path: "src/app/[locale]/(client)/providers/[providerId]/page.tsx"
      provides: "Public provider profile page with tabbed layout"
      min_lines: 60
    - path: "src/features/provider/components/PublicProfileHeader.tsx"
      provides: "Provider header with photo, name, badges, rating stars, city, member since"
      min_lines: 40
    - path: "src/features/provider/components/PublicProfileStats.tsx"
      provides: "Stats cards: missions, rating, reviews, response time"
      min_lines: 30
    - path: "src/features/provider/components/PublicProfileAbout.tsx"
      provides: "About section with bio, languages, experience, zones, certifications"
      min_lines: 50
    - path: "src/features/provider/components/PublicServiceCard.tsx"
      provides: "Public-facing service card (different from provider's management card)"
      min_lines: 40
  key_links:
    - from: "src/app/[locale]/(client)/providers/[providerId]/page.tsx"
      to: "prisma.provider"
      via: "database query"
      pattern: "prisma\\.provider\\.findUnique"
    - from: "src/features/provider/components/PublicProfileHeader.tsx"
      to: "src/components/shared/TrustBadges.tsx"
      via: "component import"
      pattern: "TrustBadges"
    - from: "src/features/provider/components/PublicProfileStats.tsx"
      to: "prisma.provider"
      via: "stats from provider fields"
      pattern: "completedMissions|rating|ratingCount|responseTimeHours"
---

<objective>
Build the public provider profile page visible to clients and the certification management components. The profile page displays provider info, statistics, active services, and an about section with tabbed navigation — the key page clients use to decide whether to book a provider.

Purpose: PROF-07 (statistics display) and PROF-03 (certifications on profile) are satisfied here. This is the client-facing output of all provider data entered in Plans 01-04.
Output: Public profile page at `/providers/[providerId]` with all sections, plus certification upload component for edit profile.
</objective>

<execution_context>
@C:/Users/LENOVO/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-profil-prestataire-services/CONTEXT.md
@.planning/phases/04-profil-prestataire-services/04-03-SUMMARY.md
@.planning/phases/04-profil-prestataire-services/04-04-SUMMARY.md
@prisma/schema.prisma
@src/components/shared/TrustBadges.tsx
@src/features/kyc/actions/compute-badges.ts
@src/types/index.ts
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public provider profile page with header, stats, services tab, and about tab</name>
  <files>
    src/app/[locale]/(client)/providers/[providerId]/page.tsx
    src/features/provider/components/PublicProfileHeader.tsx
    src/features/provider/components/PublicProfileStats.tsx
    src/features/provider/components/PublicProfileAbout.tsx
    src/features/provider/components/PublicServiceCard.tsx
  </files>
  <action>
**`src/features/provider/components/PublicProfileHeader.tsx`** — Server component (no "use client"):
- Props: `provider: { displayName, photoUrl, kycStatus, rating, ratingCount, createdAt, delegations }`, `badges: TrustBadge[]`
- Layout (Airbnb style):
  - Large circular avatar (160px desktop, 120px mobile) or placeholder UserCircle icon
  - Display name (text-2xl bold)
  - TrustBadges component (imported from `@/components/shared/TrustBadges`)
  - Star rating display: yellow stars (lucide-react Star, filled for whole stars) + numeric rating (4.5) + review count "(12 avis)"
  - City/location: derived from first delegation's gouvernorat name (or "Tunisie" if no zones set)
  - "Membre depuis {month} {year}" formatted from createdAt
- All text from getTranslations("provider")

**`src/features/provider/components/PublicProfileStats.tsx`** — Server component:
- Props: `provider: { completedMissions, rating, ratingCount, responseTimeHours }`
- 4 stat cards in a 2x2 grid (or 4-col on desktop):
  1. Missions terminees: provider.completedMissions (icon: CheckCircle2)
  2. Note moyenne: provider.rating.toFixed(1) + "/5" (icon: Star)
  3. Avis clients: provider.ratingCount (icon: MessageSquare)
  4. Temps de reponse: format responseTimeHours — "< 1h" if < 1, "{N}h" otherwise, or "N/A" if null (icon: Clock)
- Each card: white rounded-xl with border, icon + value (text-2xl bold) + label (text-sm muted)
- All labels from getTranslations("provider")

**`src/features/provider/components/PublicProfileAbout.tsx`** — Server component:
- Props: `provider: { bio, yearsExperience, languages, delegations with gouvernorat, certifications }`
- Sections:
  1. Bio: rendered as paragraphs (whitespace-pre-line)
  2. Experience: "{N} ans d'experience" with Briefcase icon
  3. Languages: comma-separated list with Globe icon
  4. Zones d'intervention: grouped by gouvernorat, delegations listed. E.g. "Tunis: Bab Bhar, La Marsa, Carthage"
  5. Certifications: list of certification titles with file links (external link icon to open PDF/image)
- If bio is empty, show placeholder text "Ce prestataire n'a pas encore renseigne sa description."
- All labels from getTranslations("provider")

**`src/features/provider/components/PublicServiceCard.tsx`** — Server component:
- Props: `service: Service & { category: { name: string, parent?: { name: string } | null } }`
- Card layout (Airbnb style, per CONTEXT.md):
  - Top: first photo or gradient placeholder
  - Title (truncated 2 lines)
  - Category badge (small rounded pill)
  - Price: "XX TND" for FIXED, "Sur devis" for SUR_DEVIS, formatted with toLocaleString("fr-TN")
  - Duration if set: "{N} min" or "{N}h"
  - Link wrapping entire card to future service detail page (for now link to # or `/services/[id]` placeholder)
- Heart icon (top-right) for future favorites — just visual placeholder, no functionality

**`src/app/[locale]/(client)/providers/[providerId]/page.tsx`** — Server component:
- This page is in the (client) route group — accessible to anyone (public)
- Fetch provider with all related data:
  ```
  prisma.provider.findUnique({
    where: { id: providerId, isDeleted: false, isActive: true },
    include: {
      user: { select: { name: true, email: true } },
      trustBadges: { where: { isActive: true } },
      delegations: { include: { delegation: { include: { gouvernorat: true } } } },
      certifications: { where: { isDeleted: false } },
      services: {
        where: { status: "ACTIVE", isDeleted: false },
        include: { category: { include: { parent: true } } },
        orderBy: { createdAt: "desc" }
      }
    }
  })
  ```
- If provider not found or not active: show 404 or redirect
- Page structure:
  1. PublicProfileHeader (top hero area)
  2. PublicProfileStats (below header)
  3. Tabs (shadcn Tabs): "Services" | "Avis" | "A propos"
     - Services tab: grid of PublicServiceCard (2 cols mobile, 3 cols desktop). Empty state: "Aucun service publie"
     - Avis tab: placeholder "Les avis seront disponibles prochainement" (Phase 8)
     - A propos tab: PublicProfileAbout
- generateMetadata: dynamic title "{displayName} | Tawa Services", description from bio (first 160 chars)
- OG meta tags for social sharing
  </action>
  <verify>
Run `npx tsc --noEmit` — zero errors. Visit `/fr/providers/[a-valid-provider-id]` in browser. Verify header shows with placeholder data. Verify tabs switch between Services, Avis, and A propos. Verify stats cards render.
  </verify>
  <done>
Public profile page renders at `/providers/[providerId]` with hero header (photo, name, badges, rating, location, member since), 4 stat cards, and 3 tabs. Services tab shows active services grid. A propos tab shows bio, experience, languages, zones, certifications. All labels in French.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create certification management components and integrate into edit profile</name>
  <files>
    src/features/provider/components/CertificationsList.tsx
    src/features/provider/components/CertificationUploader.tsx
    src/app/[locale]/(provider)/provider/profile/edit/page.tsx
  </files>
  <action>
**`src/features/provider/components/CertificationUploader.tsx`** — Client component ("use client"):
- Props: `providerId: string`
- Upload form: title text input (required, 2-200 chars) + file input (accept image/* and .pdf, max 10MB)
- On submit: POST to `/api/provider/certification` with FormData (file + title)
- After upload success: call `addCertificationAction({ title, fileUrl })` if not already handled by API route, show toast, trigger parent refresh
- Show loading state during upload
- Validation: require both title and file before enabling submit button

**`src/features/provider/components/CertificationsList.tsx`** — Client component ("use client"):
- Props: `initialCertifications: Array<{ id: string, title: string, fileUrl: string, issuedAt?: Date | null, createdAt: Date }>`, `providerId: string`
- List of certification cards: each shows title, file icon (FileText for PDF, Image for images), upload date, view link (opens file in new tab), delete button
- Delete button calls `deleteCertificationAction(id)` with confirmation dialog
- CertificationUploader mounted below the list for adding new ones
- Empty state: "Aucune certification ajoutee"
- All labels from useTranslations("provider")

**Integration into edit profile page:** Update `src/app/[locale]/(provider)/provider/profile/edit/page.tsx` to add a fourth tab "Certifications" that mounts CertificationsList with fetched certifications data. The edit page already fetches provider data — add certification fetch:
```
prisma.certification.findMany({ where: { providerId, isDeleted: false }, orderBy: { createdAt: "desc" } })
```

Mount in the tabbed layout:
- Tab 4: "Certifications" with CertificationsList + CertificationUploader
  </action>
  <verify>
Run `npx tsc --noEmit` — zero errors. Visit `/fr/provider/profile/edit` and navigate to Certifications tab. Verify upload form renders with title input + file picker. Verify existing certifications list (empty state for new provider).
  </verify>
  <done>
Certifications tab added to edit profile page. CertificationUploader allows uploading PDF/images with title. CertificationsList shows all certifications with view/delete actions. Certifications displayed on public profile A propos tab.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 4 provider profile and services flow</name>
  <files>n/a</files>
  <action>
Human verification checkpoint for the complete Phase 4 feature set. All automated work is complete in Tasks 1-2 and prior plans 01-04. This checkpoint verifies the E2E flow.
  </action>
  <verify>
1. Log in as PROVIDER (KYC approved)
2. Visit `/fr/provider/profile/edit`:
   - Edit display name, bio, phone — save and verify toast
   - Upload a profile photo — verify preview updates
   - Switch to Zones tab — select delegations from at least 2 gouvernorats, save
   - Switch to Disponibilites tab — toggle Monday off, change Tuesday hours, save
   - Switch to Certifications tab — upload a PDF with title, verify it appears in list
3. Visit `/fr/provider/services`:
   - Verify "Mes services" page shows empty state with create button
   - Click "Creer un service" — fill in title, description (150+ chars), select category, set fixed price, save
   - Verify redirect to edit page — upload 2 photos, add 1 inclusion and 1 exclusion
   - Go back to My Services — verify service card shows with status "Brouillon"
   - Toggle active — verify badge changes to "Actif"
4. Visit `/fr/providers/[your-provider-id]`:
   - Verify header shows your name, photo, badges, "Membre depuis..."
   - Verify stats cards show (0 missions, rating, etc.)
   - Verify Services tab lists your active service
   - Verify A propos tab shows bio, languages, zones, certification
5. Mobile check: resize to 375px width — verify all pages are usable
  </verify>
  <done>
All Phase 4 features verified by human: profile editing, service creation, public profile display, KYC guard, mobile responsiveness.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Public profile page loads at `/providers/[id]` with all sections
- Stats display real-time data from provider record
- Services tab shows only ACTIVE services (not DRAFT/DELETED)
- A propos tab shows bio, languages, experience, zones, certifications
- Certifications upload works with PDF and images
- All text in French via i18n
</verification>

<success_criteria>
- 7 new files created (5 components, 1 page for public profile, 1 edit profile update)
- Public profile at `/providers/[id]` shows provider info, stats, services, about
- Certification management works (upload, list, delete)
- Stats show: completedMissions, rating, ratingCount, responseTimeHours
- E2E flow works: edit profile -> create service -> view public profile
</success_criteria>

<output>
After completion, create `.planning/phases/04-profil-prestataire-services/04-05-SUMMARY.md`
</output>
