---
phase: 04-profil-prestataire-services
plan: 05
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - src/app/[locale]/(client)/providers/[providerId]/page.tsx
  - src/features/provider/components/PublicProfileHeader.tsx
  - src/features/provider/components/PublicProfileStats.tsx
  - src/features/provider/components/PublicProfileAbout.tsx
  - src/features/provider/components/PublicServiceCard.tsx
  - src/features/provider/components/PortfolioGallery.tsx
  - src/features/provider/components/CertificationsList.tsx
  - src/features/provider/components/CertificationUploader.tsx
autonomous: false
requirements: [PROF-07, PROF-03]

must_haves:
  truths:
    - "Anyone can view a provider's public profile at /providers/[id]"
    - "Public profile displays provider name, photo, bio, verified badge, and member since date"
    - "Public profile shows real-time stats: missions completed, average rating, total reviews, response rate %, response time"
    - "Public profile lists all active services with prices, categories, provider name, photo, and rating"
    - "Public profile has tabs: Services, Avis, A propos"
    - "A propos tab shows bio, languages, experience, intervention zone, certifications, and portfolio gallery"
    - "Provider can upload and manage certifications from edit profile page"
  artifacts:
    - path: "src/app/[locale]/(client)/providers/[providerId]/page.tsx"
      provides: "Public provider profile page with tabbed layout"
      min_lines: 60
    - path: "src/features/provider/components/PublicProfileHeader.tsx"
      provides: "Provider header with photo, name, badges, rating stars, city, member since"
      min_lines: 40
    - path: "src/features/provider/components/PublicProfileStats.tsx"
      provides: "Stats cards: missions, rating, reviews, response rate, response time"
      min_lines: 30
    - path: "src/features/provider/components/PublicProfileAbout.tsx"
      provides: "About section with bio, languages, experience, zones, certifications"
      min_lines: 50
    - path: "src/features/provider/components/PublicServiceCard.tsx"
      provides: "Public-facing service card with provider info, rating, and city"
      min_lines: 50
    - path: "src/features/provider/components/PortfolioGallery.tsx"
      provides: "Read-only portfolio photo gallery for public profile"
      min_lines: 40
  key_links:
    - from: "src/app/[locale]/(client)/providers/[providerId]/page.tsx"
      to: "prisma.provider"
      via: "database query"
      pattern: "prisma\\.provider\\.findUnique"
    - from: "src/features/provider/components/PublicProfileHeader.tsx"
      to: "src/components/shared/TrustBadges.tsx"
      via: "component import"
      pattern: "TrustBadges"
    - from: "src/features/provider/components/PublicProfileStats.tsx"
      to: "prisma.provider"
      via: "stats from provider fields"
      pattern: "completedMissions|rating|ratingCount|responseRate|responseTimeHours"
    - from: "src/features/provider/components/CertificationUploader.tsx"
      to: "/api/provider/certification"
      via: "fetch POST only (single DB write point)"
      pattern: "api/provider/certification"
---

<objective>
Build the public provider profile page visible to clients, the portfolio gallery, and the certification management components. The profile page displays provider info, statistics (including response rate %), active services (with provider details), and an about section with portfolio and certifications — the key page clients use to decide whether to book a provider.

Purpose: PROF-07 (statistics display) and PROF-03 (certifications on profile) are satisfied here. This is the client-facing output of all provider data entered in Plans 01-04.
Output: Public profile page at `/providers/[providerId]` with all sections, portfolio gallery, plus certification upload component for edit profile.
</objective>

<execution_context>
@C:/Users/LENOVO/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-profil-prestataire-services/CONTEXT.md
@.planning/phases/04-profil-prestataire-services/04-03-SUMMARY.md
@.planning/phases/04-profil-prestataire-services/04-04-SUMMARY.md
@prisma/schema.prisma
@src/components/shared/TrustBadges.tsx
@src/features/kyc/actions/compute-badges.ts
@src/types/index.ts
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public provider profile page with header, stats, services tab, portfolio gallery, and about tab</name>
  <files>
    src/app/[locale]/(client)/providers/[providerId]/page.tsx
    src/features/provider/components/PublicProfileHeader.tsx
    src/features/provider/components/PublicProfileStats.tsx
    src/features/provider/components/PublicProfileAbout.tsx
    src/features/provider/components/PublicServiceCard.tsx
    src/features/provider/components/PortfolioGallery.tsx
  </files>
  <action>
**`src/features/provider/components/PublicProfileHeader.tsx`** — Server component (no "use client"):
- Props: `provider: { displayName, photoUrl, kycStatus, rating, ratingCount, createdAt, delegations }`, `badges: TrustBadge[]`
- Layout (Airbnb style):
  - Large circular avatar (160px desktop, 120px mobile) or placeholder UserCircle icon
  - Display name (text-2xl bold)
  - TrustBadges component (imported from `@/components/shared/TrustBadges`)
  - Star rating display: yellow stars (lucide-react Star, filled for whole stars) + numeric rating (4.5) + review count "(12 avis)"
  - City/location: derived from first delegation's gouvernorat name (or "Tunisie" if no zones set)
  - "Membre depuis {month} {year}" formatted from createdAt
- All text from getTranslations("provider")

**`src/features/provider/components/PublicProfileStats.tsx`** — Server component:
- Props: `provider: { completedMissions, rating, ratingCount, responseRate, responseTimeHours }`
- 5 stat cards in a responsive grid (2x3 on mobile wrapping, 5-col on desktop):
  1. Missions terminees: provider.completedMissions (icon: CheckCircle2)
  2. Note moyenne: provider.rating.toFixed(1) + "/5" (icon: Star)
  3. Avis clients: provider.ratingCount (icon: MessageSquare)
  4. Taux de reponse: format responseRate — "{N}%" if set, or "N/A" if null (icon: BarChart3). This is the percentage of requests the provider responds to, per CONTEXT.md "response rate %".
  5. Temps de reponse: format responseTimeHours — "< 1h" if < 1, "{N}h" otherwise, or "N/A" if null (icon: Clock)
- Each card: white rounded-xl with border, icon + value (text-2xl bold) + label (text-sm muted)
- All labels from getTranslations("provider")

**`src/features/provider/components/PublicProfileAbout.tsx`** — Server component:
- Props: `provider: { bio, yearsExperience, languages, delegations with gouvernorat, certifications, portfolioPhotos }`
- Sections:
  1. Bio: rendered as paragraphs (whitespace-pre-line)
  2. Experience: "{N} ans d'experience" with Briefcase icon
  3. Languages: comma-separated list with Globe icon
  4. Zones d'intervention: grouped by gouvernorat, delegations listed. E.g. "Tunis: Bab Bhar, La Marsa, Carthage"
  5. Portfolio: PortfolioGallery component (see below) — shows provider's work photos with captions
  6. Certifications: list of certification titles with file links (external link icon to open PDF/image)
- If bio is empty, show placeholder text "Ce prestataire n'a pas encore renseigne sa description."
- All labels from getTranslations("provider")

**`src/features/provider/components/PortfolioGallery.tsx`** — Server component (no "use client"):
- Props: `photos: Array<{ id: string, photoUrl: string, caption: string | null }>`
- Read-only gallery for the public profile (not the uploader)
- Grid layout: 2 cols on mobile, 3 cols on desktop
- Each photo: 1:1 aspect ratio card with image fill (object-cover), caption below in text-sm muted
- If no photos: do not render anything (section hidden when empty)
- If photos exist: section heading "Portfolio" (from getTranslations("provider")) followed by grid
- Clicking a photo could open a lightbox in the future — for now, no click behavior

**`src/features/provider/components/PublicServiceCard.tsx`** — Server component:
- Props: `service: Service & { category: { name: string, parent?: { name: string } | null }, provider: { displayName: string, photoUrl: string | null, rating: float, ratingCount: int, delegations: Array<{ delegation: { gouvernorat: { name: string } } }> } }`
- Card layout (Airbnb style, per CONTEXT.md "Service card component: image, title, price formatted TND, provider name+photo, rating stars, city, heart icon for favorites"):
  - Top: first photo or gradient placeholder
  - Title (truncated 2 lines)
  - Category badge (small rounded pill)
  - Provider mini-section: small avatar (32px circle) + provider displayName + star icon with rating value (e.g. "4.5")
  - City: derived from provider's first delegation gouvernorat name, displayed as small text
  - Price: "XX TND" for FIXED, "Sur devis" for SUR_DEVIS, formatted with toLocaleString("fr-TN")
  - Duration if set: "{N} min" or "{N}h"
  - Link wrapping entire card to future service detail page (for now link to # or `/services/[id]` placeholder)
- Heart icon (top-right) for future favorites — just visual placeholder, no functionality

**`src/app/[locale]/(client)/providers/[providerId]/page.tsx`** — Server component:
- This page is in the (client) route group — accessible to anyone (public)
- Fetch provider with all related data:
  ```
  prisma.provider.findUnique({
    where: { id: providerId, isDeleted: false, isActive: true },
    include: {
      user: { select: { name: true, email: true } },
      trustBadges: { where: { isActive: true } },
      delegations: { include: { delegation: { include: { gouvernorat: true } } } },
      certifications: { where: { isDeleted: false } },
      portfolioPhotos: { where: { isDeleted: false }, orderBy: { sortOrder: "asc" } },
      services: {
        where: { status: "ACTIVE", isDeleted: false },
        include: {
          category: { include: { parent: true } },
          provider: {
            select: {
              displayName: true,
              photoUrl: true,
              rating: true,
              ratingCount: true,
              delegations: { include: { delegation: { include: { gouvernorat: true } } }, take: 1 }
            }
          }
        },
        orderBy: { createdAt: "desc" }
      }
    }
  })
  ```
- If provider not found or not active: show 404 or redirect
- Page structure:
  1. PublicProfileHeader (top hero area)
  2. PublicProfileStats (below header) — pass all 5 stats including responseRate
  3. Tabs (shadcn Tabs): "Services" | "Avis" | "A propos"
     - Services tab: grid of PublicServiceCard (2 cols mobile, 3 cols desktop). Empty state: "Aucun service publie". Each card receives full service + provider data.
     - Avis tab: placeholder "Les avis seront disponibles prochainement" (Phase 8)
     - A propos tab: PublicProfileAbout — receives bio, languages, experience, zones, certifications, AND portfolioPhotos
- generateMetadata: dynamic title "{displayName} | Tawa Services", description from bio (first 160 chars)
- OG meta tags for social sharing
  </action>
  <verify>
Run `npx tsc --noEmit` — zero errors. Visit `/fr/providers/[a-valid-provider-id]` in browser. Verify header shows with placeholder data. Verify tabs switch between Services, Avis, and A propos. Verify stats cards render with 5 metrics including response rate. Verify PublicServiceCard shows provider name, photo, rating, and city. Verify portfolio gallery renders in A propos tab.
  </verify>
  <done>
Public profile page renders at `/providers/[providerId]` with hero header (photo, name, badges, rating, location, member since), 5 stat cards (including response rate %), and 3 tabs. Services tab shows active services grid with provider info on each card (name, photo, rating, city). A propos tab shows bio, experience, languages, zones, portfolio gallery, and certifications. All labels in French.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create certification management components and integrate into edit profile</name>
  <files>
    src/features/provider/components/CertificationsList.tsx
    src/features/provider/components/CertificationUploader.tsx
    src/app/[locale]/(provider)/provider/profile/edit/page.tsx
  </files>
  <action>
**`src/features/provider/components/CertificationUploader.tsx`** — Client component ("use client"):
- Props: `providerId: string`, `onUploadSuccess: () => void`
- Upload form: title text input (required, 2-200 chars) + file input (accept image/* and .pdf, max 10MB)
- On submit: POST to `/api/provider/certification` with FormData (file + title)
- The API route at `/api/provider/certification` is the SINGLE point of DB write — it creates the `prisma.certification.create(...)` record. Do NOT call `addCertificationAction` after upload. This avoids duplicate records.
- After successful POST response: call `onUploadSuccess()` to trigger parent list refresh (parent calls `getProviderCertificationsAction()` to reload the list)
- Show toast success with certification title
- Show loading state during upload
- Validation: require both title and file before enabling submit button

**`src/features/provider/components/CertificationsList.tsx`** — Client component ("use client"):
- Props: `initialCertifications: Array<{ id: string, title: string, fileUrl: string, issuedAt?: Date | null, createdAt: Date }>`, `providerId: string`
- Local state for certifications list, initialized from props
- List of certification cards: each shows title, file icon (FileText for PDF, Image for images), upload date, view link (opens file in new tab), delete button
- Delete button calls `deleteCertificationAction(id)` with confirmation dialog, then refreshes list via `getProviderCertificationsAction()`
- CertificationUploader mounted below the list for adding new ones
- Pass `onUploadSuccess` callback to CertificationUploader that calls `getProviderCertificationsAction()` and updates local state
- Empty state: "Aucune certification ajoutee"
- All labels from useTranslations("provider")

**Integration into edit profile page:** Update `src/app/[locale]/(provider)/provider/profile/edit/page.tsx` to add a fifth tab "Certifications" that mounts CertificationsList with fetched certifications data. The edit page already fetches provider data — add certification fetch:
```
prisma.certification.findMany({ where: { providerId, isDeleted: false }, orderBy: { createdAt: "desc" } })
```

Mount in the tabbed layout:
- Tab 5: "Certifications" with CertificationsList (which internally renders CertificationUploader)
  </action>
  <verify>
Run `npx tsc --noEmit` — zero errors. Visit `/fr/provider/profile/edit` and navigate to Certifications tab. Verify upload form renders with title input + file picker. Verify existing certifications list (empty state for new provider). Verify uploading a certification does NOT cause duplicate DB records (only the API route writes to DB).
  </verify>
  <done>
Certifications tab added to edit profile page (5th tab). CertificationUploader POSTs to `/api/provider/certification` as single DB write point — no duplicate addCertificationAction call. After upload success, list refreshes via getProviderCertificationsAction(). CertificationsList shows all certifications with view/delete actions. Certifications and portfolio displayed on public profile A propos tab.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 4 provider profile and services flow</name>
  <files>n/a</files>
  <action>
Human verification checkpoint for the complete Phase 4 feature set. All automated work is complete in Tasks 1-2 and prior plans 01-04. This checkpoint verifies the E2E flow.
  </action>
  <verify>
1. Log in as PROVIDER (KYC approved)
2. Visit `/fr/provider/profile/edit`:
   - Edit display name, bio, phone — save and verify toast
   - Upload a profile photo — verify preview updates
   - Switch to Zones tab — select delegations from at least 2 gouvernorats, save
   - Switch to Disponibilites tab — toggle Monday off, change Tuesday hours, save
   - Switch to Portfolio tab — upload 2 work photos with captions, verify grid shows "2/10 photos", edit a caption, delete one photo
   - Switch to Certifications tab — upload a PDF with title, verify it appears in list
3. Visit `/fr/provider/services`:
   - Verify "Mes services" page shows empty state with create button
   - Click "Creer un service" — fill in title, description (150+ chars), select category, set fixed price, save
   - Verify redirect to edit page — upload 2 photos, add 1 inclusion and 1 exclusion
   - Go back to My Services — verify service card shows with status "Brouillon"
   - Toggle active — verify badge changes to "Actif"
4. Visit `/fr/providers/[your-provider-id]`:
   - Verify header shows your name, photo, badges, "Membre depuis..."
   - Verify stats cards show 5 metrics (0 missions, rating, reviews, response rate, response time)
   - Verify Services tab lists your active service with provider name, photo, rating stars, and city on the card
   - Verify A propos tab shows bio, languages, zones, portfolio photos with captions, and certification
5. Mobile check: resize to 375px width — verify all pages are usable
  </verify>
  <done>
All Phase 4 features verified by human: profile editing (including portfolio), service creation, public profile display (with response rate stat, provider info on service cards, portfolio gallery), KYC guard, mobile responsiveness.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Public profile page loads at `/providers/[id]` with all sections
- Stats display 5 metrics including response rate % (from CONTEXT.md)
- PublicServiceCard shows provider name, photo, rating stars, and city (from CONTEXT.md)
- Portfolio gallery renders in A propos tab with photos and captions
- Services tab shows only ACTIVE services (not DRAFT/DELETED)
- A propos tab shows bio, languages, experience, zones, portfolio, certifications
- Certifications upload uses API route as single DB write point (no duplicate writes)
- All text in French via i18n
</verification>

<success_criteria>
- 8 new files created (6 components, 1 page for public profile, 1 edit profile update)
- Public profile at `/providers/[id]` shows provider info, 5 stats, services with provider details, about with portfolio
- Certification management works (upload via API route only, list, delete)
- Portfolio gallery shows up to 10 photos with captions on public profile
- Stats show: completedMissions, rating, ratingCount, responseRate %, responseTimeHours
- PublicServiceCard includes provider name, mini avatar, rating, and city
- E2E flow works: edit profile -> upload portfolio -> create service -> view public profile
</success_criteria>

<output>
After completion, create `.planning/phases/04-profil-prestataire-services/04-05-SUMMARY.md`
</output>
