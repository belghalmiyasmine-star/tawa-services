---
phase: 11-demo-data-polish-pfe-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/seed.ts
  - package.json
autonomous: true
requirements:
  - UI-04

must_haves:
  truths:
    - "Running `npm run db:seed` populates the database with at least 10 verified providers across different Tunisian cities and categories"
    - "At least 20 services exist with realistic titles, descriptions, photos, and TND pricing"
    - "At least 30 bookings exist at various statuses (PENDING, ACCEPTED, IN_PROGRESS, COMPLETED, CANCELLED)"
    - "At least 50 reviews exist with star ratings, criteria ratings, and text content"
    - "Payment records exist with TND amounts, 12% commission calculations, and various statuses"
    - "Conversations and messages exist between clients and providers for bookings"
  artifacts:
    - path: "prisma/seed.ts"
      provides: "Complete demo data seed script with Tunisian-realistic data"
      min_lines: 400
  key_links:
    - from: "prisma/seed.ts"
      to: "prisma/schema.prisma"
      via: "Prisma client model operations"
      pattern: "prisma\\.(user|provider|service|booking|payment|review|conversation|message)\\."
---

<objective>
Create a comprehensive seed script that populates the database with realistic Tunisian demo data for PFE presentation.

Purpose: The PFE jury needs to see a realistic platform with diverse data — providers across multiple cities and categories, services with TND pricing, bookings at various stages, reviews with ratings, and payment transactions demonstrating the commission model.

Output: Extended prisma/seed.ts that seeds users, providers, services, bookings, payments, reviews, conversations, messages, and notifications with Tunisian-realistic data.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prisma/schema.prisma
@prisma/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @faker-js/faker and extend seed script with realistic Tunisian demo data</name>
  <files>package.json, prisma/seed.ts</files>
  <action>
Install @faker-js/faker as a devDependency:
```
npm install -D @faker-js/faker
```

Extend the existing prisma/seed.ts (which already seeds gouvernorats, delegations, and categories) to add comprehensive demo data. The existing gouvernorat/delegation/category seeding MUST be preserved.

After the existing category seeding, add the following seed data in order:

**1. Admin user (1):**
- email: admin@tawa.tn, role: ADMIN, name: "Admin Tawa", password hashed with bcryptjs (use "Admin123!")
- emailVerified: true, phoneVerified: true

**2. Client users (5):**
- Realistic Tunisian names (e.g., "Amine Ben Salah", "Fatma Trabelsi", "Youssef Bouazizi", "Nour Chaabane", "Sarra Mejri")
- Emails: {firstname}@gmail.com pattern
- Phones: +216 format (e.g., "+21620123456")
- Role: CLIENT, emailVerified: true, phoneVerified: true
- Password: hash of "Client123!" using bcryptjs

**3. Provider users + Provider profiles (10):**
- Realistic Tunisian provider names (e.g., "Mohamed Haddad", "Karim Jebali", "Slim Bouhlel", "Nadia Ayari", "Riadh Gharbi", "Amel Souissi", "Fathi Ksouri", "Leila Ben Ammar", "Hichem Hammami", "Olfa Maatoug")
- Role: PROVIDER, kycStatus: APPROVED, kycSubmittedAt + kycApprovedAt set
- Each provider has displayName, bio (2-3 sentences describing their expertise in French), photoUrl: null
- yearsExperience: random 2-15, languages: ["Francais", "Arabe"] (some add "Anglais")
- rating: random 3.5-4.9, ratingCount: random 5-40, completedMissions: random 10-100
- Each provider linked to 2-4 delegations via ProviderDelegation (spread across Tunis, Ariana, Ben Arous, Sousse, Sfax, Nabeul)
- Each provider has IDENTITY_VERIFIED trust badge (isActive: true)
- Top 3 providers also get TOP_PROVIDER badge
- Providers with responseTimeHours < 2 get QUICK_RESPONSE badge
- Each provider has 5-6 Availability records (Monday-Saturday, 08:00-17:00 or 09:00-18:00)

**4. Services (20-25):**
- Distribute across categories: plomberie (3), electricite (3), menage (3), cours (3), peinture (2), demenagement (2), jardinage (2), climatisation (2), serrurerie (1), informatique (2)
- Use child (subcategory) IDs, not parent category IDs
- Each service: realistic French title (max 80 chars), description (150-400 chars in French), status: ACTIVE
- FIXED pricing: random 30-500 TND; some services SUR_DEVIS
- durationMinutes: 60-480 depending on category
- inclusions: 2-3 items (e.g., ["Deplacement inclus", "Materiel de base fourni", "Garantie 30 jours"])
- exclusions: 1-2 items (e.g., ["Pieces de rechange", "Travaux supplementaires"])
- photoUrls: empty array (no actual photos needed for seed)

**5. Bookings (35-40):**
- Mix of statuses: 8 COMPLETED, 5 IN_PROGRESS, 8 ACCEPTED, 6 PENDING, 4 REJECTED, 4 CANCELLED
- scheduledAt: spread over last 3 months
- completedAt: set for COMPLETED bookings
- cancelledAt + cancelledBy: set for CANCELLED bookings
- totalAmount: matches service fixedPrice or random 50-400 for SUR_DEVIS
- clientNote: short French notes for some bookings

**6. Quotes (8-10):**
- Mix: 3 ACCEPTED (linked to bookings), 2 RESPONDED, 2 PENDING, 2 EXPIRED
- description: realistic job descriptions in French
- address/city: Tunisian addresses
- budget: 100-500 TND
- proposedPrice for RESPONDED/ACCEPTED: budget * 0.8-1.2

**7. Payments (for all COMPLETED + IN_PROGRESS + ACCEPTED bookings with HELD/RELEASED):**
- COMPLETED bookings: PaymentStatus.RELEASED, commission = amount * 0.12, providerEarning = amount * 0.88
- IN_PROGRESS bookings: PaymentStatus.HELD
- Some ACCEPTED bookings: PaymentStatus.HELD (already paid)
- method: mix of CARD, D17, FLOUCI, CASH
- paidAt, heldAt, releasedAt: appropriate timestamps

**8. Reviews (50-55):**
- For each COMPLETED booking: one CLIENT review + one PROVIDER review (bidirectional)
- Stars: 3-5, quality/punctuality/communication/cleanliness ratings: 3-5
- text: short French review texts (e.g., "Excellent travail, tres professionnel", "Service rapide et soigne", "Ponctuel et competent")
- published: true, publishedAt: 1-2 days after booking completedAt
- Some reviews flagged: false; 2-3 flagged: true for demo
- photoUrls: empty

**9. Conversations + Messages (for 10-15 bookings):**
- Create Conversation linked to booking
- 3-8 messages per conversation alternating between client and provider
- Content: realistic French booking coordination messages (e.g., "Bonjour, je confirme le rendez-vous pour demain a 10h", "D'accord, je serai la. Merci")
- isRead: true for older messages, some unread for recent ones

**10. Notifications (20-30):**
- Mix of NotifType values across users
- BOOKING_REQUEST, BOOKING_ACCEPTED, PAYMENT_RECEIVED, REVIEW_RECEIVED, etc.
- read: true for most, 5-8 unread for recent ones
- title/body in French

Use bcryptjs (already in dependencies) for password hashing. Do NOT use faker.js for text generation — hardcode realistic French/Tunisian strings directly. faker.js is only needed if we want random names, but since we need specific Tunisian names, hardcode everything directly. Remove @faker-js/faker from the install step — it's not needed.

Use upsert patterns where possible for idempotency (re-runnable seed). For entities without natural unique keys, use deleteMany + createMany pattern wrapped in a check.

Wrap the new seeding in a `seedDemoData()` function called after the existing gouvernorat/category seeding. Add a `--demo` flag check: `if (process.argv.includes('--demo'))` to optionally skip demo data when only base data is needed.

Add a new script to package.json: `"db:seed:demo": "tsx prisma/seed.ts --demo"`
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit prisma/seed.ts --skipLibCheck 2>&1 | head -20</automated>
    <manual>Review seed.ts for completeness: 10+ providers, 20+ services, 30+ bookings, 50+ reviews, payment records with 12% commission</manual>
  </verify>
  <done>prisma/seed.ts contains comprehensive Tunisian demo data seeding. Running `npm run db:seed:demo` populates all required entities. Script is idempotent and preserves existing gouvernorat/category seeding.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (seed.ts compiles without type errors)
- seed.ts contains hardcoded Tunisian names, cities, French descriptions
- At least 10 providers, 20 services, 30 bookings, 50 reviews defined in seed data
- Payment records include commission = amount * 0.12 calculation
- Script preserves existing gouvernorat/delegation/category seeding
</verification>

<success_criteria>
- The seed script compiles without TypeScript errors
- Demo data includes all required entity types with realistic Tunisian content
- Running the seed populates the database for a convincing PFE demo
- The script is idempotent (can be re-run without errors)
</success_criteria>

<output>
After completion, create `.planning/phases/11-demo-data-polish-pfe-readiness/11-01-SUMMARY.md`
</output>
