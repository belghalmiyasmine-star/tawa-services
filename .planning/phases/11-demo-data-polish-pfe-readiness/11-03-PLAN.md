---
phase: 11-demo-data-polish-pfe-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/routing.ts
  - src/components/shared/LocaleSwitcher.tsx
  - src/components/layout/Navbar.tsx
  - src/components/layout/BottomNav.tsx
  - src/messages/fr.json
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "A language switcher is visible in the Navbar on desktop"
    - "A language switcher is accessible from mobile (BottomNav or settings)"
    - "The i18n mechanism works — switching locale updates all translated strings on the page"
    - "The locale switcher is ready for adding AR/EN translations (routing config has placeholder)"
    - "All UI strings use translation keys via t() — no new hardcoded French strings"
  artifacts:
    - path: "src/components/shared/LocaleSwitcher.tsx"
      provides: "Polished language switcher dropdown component"
      min_lines: 30
    - path: "src/i18n/routing.ts"
      provides: "i18n routing configuration ready for multi-locale"
      contains: "locales"
  key_links:
    - from: "src/components/shared/LocaleSwitcher.tsx"
      to: "src/i18n/routing.ts"
      via: "routing.locales array for available locales"
      pattern: "routing\\.locales"
    - from: "src/components/layout/Navbar.tsx"
      to: "src/components/shared/LocaleSwitcher.tsx"
      via: "Component import and render"
      pattern: "LocaleSwitcher"
---

<objective>
Polish the language switcher UI component and ensure i18n infrastructure is fully ready for adding translations.

Purpose: The PFE jury will check that the i18n mechanism works and the switcher is visible. While only French is active, the infrastructure must clearly demonstrate readiness for Arabic and English additions.

Output: A polished LocaleSwitcher visible in the Navbar, with routing config demonstrating multi-locale readiness.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/i18n/routing.ts
@src/components/shared/LocaleSwitcher.tsx
@src/components/layout/Navbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish LocaleSwitcher component with globe icon and dropdown design</name>
  <files>src/components/shared/LocaleSwitcher.tsx, src/messages/fr.json</files>
  <action>
Redesign the LocaleSwitcher from a plain HTML select to a polished dropdown using DropdownMenu (already installed from @radix-ui/react-dropdown-menu).

The new LocaleSwitcher should:

1. **Trigger button:** A ghost button with a Globe icon (from lucide-react) and the current locale name abbreviation (e.g., "FR"). On desktop: show "FR" text next to icon. On mobile: icon-only.

2. **Dropdown content:** List all available locales with:
   - Full locale name (e.g., "Francais", "العربية", "English")
   - A checkmark icon next to the active locale
   - Each locale as a DropdownMenuItem

3. **Behavior:** On locale selection, call `router.replace(pathname, { locale: newLocale })` — same as current implementation.

4. **Visibility logic:** Currently the component returns null if `routing.locales.length <= 1`. Change this: ALWAYS show the switcher even with a single locale. This demonstrates to the PFE jury that the i18n infrastructure exists and works. When only one locale is available, the dropdown still opens but shows only one option (French) with a small note "Autres langues bientot disponibles" at the bottom.

5. **Ensure locale translation keys exist in fr.json** under the "locale" namespace:
   - `locale.fr`: "Francais"
   - `locale.ar`: "العربية"
   - `locale.en`: "English"
   - `locale.switchLanguage`: "Changer de langue"
   - `locale.comingSoon`: "Autres langues bientot disponibles"

Use the Button and DropdownMenu components from shadcn/ui. Use the Globe icon from lucide-react.

The component must be a "use client" component (already is).
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx next build 2>&1 | tail -5</automated>
    <manual>Open the app in browser, verify Globe icon visible in Navbar, click to see dropdown with "Francais" selected and "Autres langues bientot disponibles" note.</manual>
  </verify>
  <done>LocaleSwitcher displays as a polished Globe icon dropdown in the Navbar with locale options and "coming soon" note for additional languages.</done>
</task>

<task type="auto">
  <name>Task 2: Update i18n routing config and ensure switcher visibility on mobile</name>
  <files>src/i18n/routing.ts, src/components/layout/BottomNav.tsx, src/components/layout/Navbar.tsx</files>
  <action>
1. **Update src/i18n/routing.ts:**
   - Keep the current config with `locales: ["fr"]` and `defaultLocale: "fr"` as active
   - Update the comment to clearly document readiness:
   ```ts
   // i18n ready for multi-locale. To add Arabic/English:
   // 1. Create src/messages/ar.json and src/messages/en.json
   // 2. Uncomment the locales line below
   // locales: ["fr", "ar", "en"],
   ```

2. **Verify LocaleSwitcher is in the Navbar** (it already is at line 176 — ensure it's still there after any mobile polish changes from Plan 11-02).

3. **Add LocaleSwitcher accessibility on mobile:**
   Since the Navbar is hidden on mobile (md:hidden), the LocaleSwitcher needs to be accessible from mobile. Add the LocaleSwitcher to the BottomNav component:
   - Add a "More" or "Parametres" item (three dots or Cog icon) to the BottomNav as the 5th item (if there's room) OR add the LocaleSwitcher into the mobile user menu/settings area.
   - The simplest approach: if BottomNav already has 5 items, replace the least-used one with a "Plus" menu that includes the locale switcher. OR: add the LocaleSwitcher to the BottomNav header area above the icons.

   Actually, the simpler approach: ensure the LocaleSwitcher is accessible from the client settings page (/settings) which is reachable from BottomNav. Add a "Langue" section to the settings page if it exists, or add the Globe icon to the BottomNav's top bar area.

   **Simplest approach:** Add the LocaleSwitcher to the top of the mobile page layout (above the main content, below any header) OR ensure it appears in the mobile user account menu. Check what the current BottomNav links are and decide the least disruptive placement.

4. **Verify all existing translation keys** — ensure no new hardcoded French strings were introduced in recent phases. Spot-check 3-4 recently modified components for t() usage.
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx next build 2>&1 | tail -5</automated>
    <manual>On mobile viewport (375px), verify the language switcher is accessible. On desktop, verify Globe icon in Navbar. Check i18n/routing.ts has clear documentation for adding locales.</manual>
  </verify>
  <done>Language switcher is visible on both desktop (Navbar) and mobile. i18n routing config clearly documents how to add AR/EN. No new hardcoded French strings.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Globe icon visible in Navbar on desktop, clickable with dropdown
- Language switcher accessible on mobile viewport
- i18n/routing.ts has clear comments for adding AR/EN
- fr.json has locale namespace keys (fr, ar, en, switchLanguage, comingSoon)
</verification>

<success_criteria>
- Language switcher is visible in the Navbar and demonstrates i18n infrastructure
- The switcher is accessible on both mobile and desktop
- i18n routing config is documented and ready for adding translations
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-demo-data-polish-pfe-readiness/11-03-SUMMARY.md`
</output>
