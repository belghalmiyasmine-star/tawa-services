---
phase: 11-demo-data-polish-pfe-readiness
plan: 06
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/app/[locale]/(client)/dashboard/page.tsx
  - src/app/[locale]/(admin)/admin/messages/page.tsx
autonomous: false
requirements:
  - UI-01
  - UI-04

must_haves:
  truths:
    - "The complete demo flow (search -> profile -> booking -> payment -> review) executes without error on seeded data"
    - "All key pages render correctly with seeded demo data (no empty states, no 404s, no runtime errors)"
    - "Admin dashboard shows meaningful KPIs from seeded data (user counts, booking counts, revenue)"
    - "Provider dashboard shows bookings, earnings, and reviews from seeded data"
    - "Client bookings page shows bookings at various statuses from seeded data"
  artifacts:
    - path: "src/app/[locale]/(client)/dashboard/page.tsx"
      provides: "Client dashboard page (if missing)"
      min_lines: 10
  key_links:
    - from: "prisma/seed.ts"
      to: "all page components"
      via: "Seeded data renders in UI without errors"
      pattern: "prisma"
---

<objective>
Verify the complete E2E demo flow works on seeded data and fix any issues discovered during testing.

Purpose: The PFE demo requires a smooth walkthrough: search -> find provider -> view profile -> book service -> payment -> leave review. This plan ensures the seeded data renders correctly across all pages and the flow works without errors.

Output: Verified E2E flow on seeded data with all issues fixed.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-demo-data-polish-pfe-readiness/11-01-SUMMARY.md
@.planning/phases/11-demo-data-polish-pfe-readiness/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix any missing pages and ensure all routes render with seeded data</name>
  <files>
    src/app/[locale]/(client)/dashboard/page.tsx
    src/app/[locale]/(admin)/admin/messages/page.tsx
  </files>
  <action>
Check for any missing pages referenced in navigation that could cause 404s during the demo:

**1. Client dashboard page:**
Check if `/dashboard` page exists for CLIENT role. If it doesn't exist (git status shows it as untracked new directory), create a simple client dashboard page that shows:
- Welcome message with user name
- Quick stats: number of bookings, upcoming booking
- Link to "Mes reservations"
- Link to "Rechercher des services"

If it already exists, verify it renders without errors.

**2. Admin messages page:**
Check if `/admin/messages` exists (git status shows it as untracked). If the AdminSidebar links to it, create a placeholder page or redirect to the main messaging if appropriate. At minimum, create a page that says "Messagerie administrative - Fonctionnalite a venir" so the admin sidebar link doesn't 404.

**3. Verify all existing route pages compile:**
Run `npx next build` and check for any page-level errors. Fix any TypeScript or runtime errors discovered.

**4. Check for any pages that show empty states when data exists:**
Common issues:
- Provider profile page showing "Aucun service" when provider has services (check query includes provider services)
- Search page showing no results when services exist (check filter defaults)
- Admin stats showing 0 when data exists (check aggregation queries)

If any empty-state issues are found, trace the data flow and fix the query or component.

**5. Ensure any untracked files from git status are properly integrated:**
The git status shows untracked files:
- `public/uploads/reviews/` — this is fine, it's an uploads directory
- `src/app/[locale]/(admin)/admin/messages/` — needs page content
- `src/app/[locale]/(client)/dashboard/` — needs page content

Create minimal working pages for any missing routes.
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx next build 2>&1 | tail -10</automated>
    <manual>After seeding, navigate to: homepage, /services, /services/[id], /bookings, /provider/dashboard, /admin — all render with data.</manual>
  </verify>
  <done>All routes render without 404s or runtime errors. Missing pages have been created. Build compiles successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete E2E demo flow on seeded data</name>
  <files>prisma/seed.ts</files>
  <action>
CHECKPOINT: Human verification of the complete demo flow on seeded data.

What was built: Complete platform with seeded demo data, responsive mobile design, language switcher, performance optimizations, and SEO/accessibility improvements from Plans 11-01 through 11-05.

After running `npm run db:seed:demo` (or `npm run db:seed -- --demo`), verify the following flow:

**Step 1: Homepage**
1. Visit http://localhost:3000/fr
2. Verify: Categories load from DB, search bar works, featured services visible
3. Check: Language switcher (Globe icon) visible in navbar

**Step 2: Search and Browse**
4. Click a category (e.g., "Plomberie") or search for a service
5. Verify: Results appear with provider cards, ratings, prices in TND
6. Apply filters (city, price range, verified)
7. Verify: Filters work, pagination works

**Step 3: Service Detail**
8. Click on a service card
9. Verify: Full service detail loads — title, description, price, provider info, inclusions/exclusions

**Step 4: Provider Profile**
10. Click on provider name/link
11. Verify: Provider profile loads with bio, badges, services tab, reviews tab, stats

**Step 5: Mobile Check**
12. Open DevTools, set viewport to 375px
13. Verify: Bottom navigation visible, pages stack vertically, no horizontal scroll
14. Navigate through the same flow on mobile

**Step 6: Admin Dashboard**
15. Log in as admin@tawa.tn / Admin123!
16. Navigate to /admin
17. Verify: Dashboard shows real KPIs (user count, booking count, revenue)
18. Check: Users list, services list, reports, analytics — all have data

**Step 7: Provider Dashboard**
19. Log in as a provider (use one of the seeded provider emails)
20. Navigate to /provider/dashboard
21. Verify: Bookings, earnings, reviews show seeded data

Type "approved" if the demo flow works, or describe any issues found.
  </action>
  <verify>
    <manual>Complete the 7-step verification above. All steps must pass without errors.</manual>
  </verify>
  <done>Human has verified the complete E2E demo flow works on seeded data. All 7 verification steps pass. Platform is PFE-demo-ready.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All navigation links resolve to valid pages (no 404s)
- Seeded data renders correctly across all key pages
- E2E demo flow works: search -> profile -> booking -> payment -> review
- Admin and provider dashboards display meaningful data
</verification>

<success_criteria>
- Complete demo flow executes without errors on seeded data
- All key pages render with data (no empty states when data exists)
- Human verification confirms the platform is demo-ready
</success_criteria>

<output>
After completion, create `.planning/phases/11-demo-data-polish-pfe-readiness/11-06-SUMMARY.md`
</output>
