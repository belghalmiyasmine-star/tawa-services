---
phase: 11-demo-data-polish-pfe-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Navbar.tsx
  - src/components/layout/BottomNav.tsx
  - src/components/layout/Footer.tsx
  - src/app/[locale]/(client)/page.tsx
  - src/features/search/components/SearchFilters.tsx
  - src/features/booking/components/BookingWizard.tsx
  - src/features/review/components/ReviewForm.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "All key pages (home, search, profile, booking, client dashboard, provider dashboard, admin) are fully usable on mobile (375px) without broken elements"
    - "All key pages display correctly on desktop (1280px) without layout issues"
    - "Bottom navigation is visible and functional on mobile viewports (< 768px)"
    - "Cards, forms, and tables adapt gracefully between mobile and desktop breakpoints"
    - "No horizontal scroll appears on any page at 375px viewport width"
  artifacts:
    - path: "src/components/layout/BottomNav.tsx"
      provides: "Mobile-optimized bottom navigation"
      min_lines: 20
  key_links:
    - from: "src/components/layout/Navbar.tsx"
      to: "src/components/layout/BottomNav.tsx"
      via: "Responsive breakpoint md:block / md:hidden"
      pattern: "hidden.*md:block|md:hidden"
---

<objective>
Audit and fix mobile responsiveness across all key pages to ensure the platform is fully usable on 375px mobile and 1280px desktop viewports.

Purpose: 80% of Tunisian users access via mobile. The PFE jury will test on both mobile and desktop — every page must work without broken elements, horizontal scroll, or inaccessible interactions.

Output: Mobile-optimized pages with proper responsive Tailwind classes, no horizontal overflow, and functional bottom navigation.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix mobile responsiveness on public-facing pages</name>
  <files>
    src/app/[locale]/(client)/page.tsx
    src/app/[locale]/(client)/services/page.tsx
    src/app/[locale]/(client)/services/[serviceId]/page.tsx
    src/features/search/components/SearchFilters.tsx
    src/features/search/components/SearchAutocomplete.tsx
    src/features/search/components/SearchPagination.tsx
    src/components/layout/Navbar.tsx
    src/components/layout/BottomNav.tsx
    src/components/layout/Footer.tsx
  </files>
  <action>
Systematically audit and fix responsive issues on public-facing pages. For each file, check and fix:

**Homepage (page.tsx):**
- Hero section: text-3xl on mobile, text-5xl on lg. SearchAutocomplete full-width on mobile.
- CategoryGrid: grid-cols-2 on mobile, grid-cols-3 on md, grid-cols-5 on lg
- Featured services section: grid-cols-1 on mobile, grid-cols-2 on md, grid-cols-3 on lg
- Ensure container has px-4 on mobile for breathing room

**Search/Services page:**
- SearchFilters sidebar hidden on mobile (Sheet already implemented via mobileOnly prop — verify it works)
- Results grid: grid-cols-1 on mobile, grid-cols-2 on md, grid-cols-3 on lg
- SearchPagination: compact on mobile (fewer page numbers, smaller buttons)
- Sort dropdown: full-width on mobile

**Service detail page:**
- Image gallery: single column stack on mobile, side-by-side on lg
- Provider mini-card: full-width on mobile
- Action buttons: full-width stacked on mobile, inline on md+
- Inclusions/exclusions: single column on mobile

**Navbar:**
- Already hidden on mobile (md:block) — verify no edge cases
- Ensure SearchAutocomplete doesn't overflow on md breakpoint (w-72 md:block lg:w-96 is fine)

**BottomNav:**
- Verify all 5 nav items fit on 375px without overflow
- Ensure icons + labels are readable (text-[10px] or text-xs)
- Ensure active state is visually clear (text-primary, font-medium)
- Add safe-area-inset-bottom padding for notched phones: pb-[env(safe-area-inset-bottom)]

**Footer:**
- Stack columns on mobile (flex-col), row on md+
- Reduce padding on mobile
- Links: single column on mobile

Use Tailwind responsive prefixes (sm:, md:, lg:) consistently. Do NOT use arbitrary breakpoints — stick to Tailwind defaults (sm: 640px, md: 768px, lg: 1024px, xl: 1280px).

For any container element, ensure max-w-7xl mx-auto px-4 pattern is used consistently.

Fix any elements that could cause horizontal overflow on 375px:
- Check for fixed widths (w-[400px], min-w-[500px]) without responsive alternatives
- Check for whitespace-nowrap on long text without overflow-hidden/truncate
- Check for flex items that don't shrink (add flex-shrink-0 or min-w-0 as needed)
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx next build 2>&1 | tail -5</automated>
    <manual>Open browser DevTools at 375px width, navigate homepage -> search -> service detail. No horizontal scroll, all elements visible and functional.</manual>
  </verify>
  <done>Public-facing pages (homepage, search, service detail) are fully responsive at 375px and 1280px. No horizontal overflow, cards stack properly, navigation works on all viewports.</done>
</task>

<task type="auto">
  <name>Task 2: Fix mobile responsiveness on authenticated pages (dashboards, booking, reviews)</name>
  <files>
    src/app/[locale]/(client)/bookings/page.tsx
    src/app/[locale]/(client)/bookings/[id]/page.tsx
    src/app/[locale]/(provider)/provider/dashboard/page.tsx
    src/app/[locale]/(provider)/provider/bookings/page.tsx
    src/features/booking/components/AvailabilityCalendar.tsx
    src/features/booking/components/TimeSlotPicker.tsx
    src/features/review/components/ReviewForm.tsx
    src/features/review/components/ReviewCard.tsx
    src/app/[locale]/(admin)/admin/page.tsx
    src/components/layout/AdminSidebar.tsx
  </files>
  <action>
Audit and fix responsive issues on authenticated pages:

**Client bookings page:**
- Tabs: scrollable horizontal on mobile if many tabs (overflow-x-auto)
- Booking cards: full-width on mobile, grid-cols-2 on lg
- Status badges: wrap properly on small screens

**Booking detail page:**
- Status timeline: vertical layout on mobile (already vertical — verify spacing)
- Info sections: stack on mobile, two-column on lg
- Action buttons: full-width on mobile

**Booking wizard (book/quote pages):**
- AvailabilityCalendar: ensure calendar grid doesn't overflow on 375px. Use text-xs for day labels on mobile.
- TimeSlotPicker: grid-cols-2 on mobile, grid-cols-3 on md, grid-cols-4 on lg
- Steps indicator: compact on mobile (icons only, labels hidden)
- Payment method selector: stack vertically on mobile

**Provider dashboard:**
- Stats cards: grid-cols-2 on mobile, grid-cols-4 on lg
- Recent bookings list: card layout on mobile (not table)

**Provider bookings page:**
- Tab list: horizontal scroll on mobile (flex overflow-x-auto)
- Booking cards: stack full-width on mobile

**Review form:**
- Star rating: ensure stars are touch-friendly (min-h-[44px] tap targets)
- Criteria ratings: stack labels above stars on mobile (flex-col on mobile, flex-row on md)
- Photo upload: grid-cols-2 on mobile, grid-cols-3 on md

**Review cards (display):**
- ReviewCard: full-width on mobile
- Criteria bars: full-width

**Admin dashboard:**
- AdminSidebar: collapsed by default on mobile or use Sheet overlay
- Stats cards: grid-cols-1 on mobile, grid-cols-2 on md, grid-cols-4 on lg
- Data tables: horizontal scroll wrapper (overflow-x-auto) with min-width on table
- Admin forms: full-width inputs on mobile

For all admin data tables, wrap in:
```tsx
<div className="overflow-x-auto">
  <Table className="min-w-[600px]">...</Table>
</div>
```

Ensure all touch targets are at least 44x44px on mobile (buttons, links, interactive elements).
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx next build 2>&1 | tail -5</automated>
    <manual>Navigate authenticated pages at 375px: bookings, booking detail, provider dashboard, admin dashboard. All usable without horizontal scroll.</manual>
  </verify>
  <done>All authenticated pages (client bookings, provider dashboard, admin panel, booking wizard, reviews) are fully responsive at 375px mobile and 1280px desktop viewports.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- No horizontal scroll on any page at 375px viewport
- Bottom navigation visible and functional on mobile
- All cards, forms, and tables adapt between mobile and desktop
- Touch targets are at least 44x44px on mobile
</verification>

<success_criteria>
- All key pages are fully usable at 375px (mobile) and 1280px (desktop)
- Bottom navigation works correctly on mobile
- No broken layouts or horizontal overflow on any page
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-demo-data-polish-pfe-readiness/11-02-SUMMARY.md`
</output>
