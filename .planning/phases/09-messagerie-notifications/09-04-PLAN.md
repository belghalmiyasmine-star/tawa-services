---
phase: 09-messagerie-notifications
plan: 04
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - src/features/notification/components/NotificationBell.tsx
  - src/features/notification/components/NotificationDropdown.tsx
  - src/features/notification/components/NotificationItem.tsx
  - src/app/[locale]/(client)/notifications/page.tsx
  - src/app/[locale]/(provider)/provider/notifications/page.tsx
  - src/features/notification/components/NotificationPreferencesForm.tsx
  - src/app/[locale]/(client)/settings/notifications/page.tsx
  - src/app/[locale]/(provider)/provider/settings/notifications/page.tsx
autonomous: true
requirements:
  - NOTF-01
  - NOTF-03
  - NOTF-04

must_haves:
  truths:
    - "Bell icon in navbar shows red unread badge with count, updates via polling"
    - "Clicking bell opens dropdown with recent notifications, each clickable to navigate"
    - "Full notifications page has Tout/Non lus filter tabs"
    - "User can toggle notification preferences per type (email, in-app)"
    - "User can set quiet hours start and end times"
  artifacts:
    - path: "src/features/notification/components/NotificationBell.tsx"
      provides: "Bell icon with red unread badge, dropdown trigger, polling for count"
      min_lines: 40
    - path: "src/features/notification/components/NotificationDropdown.tsx"
      provides: "Dropdown with recent notifications list, mark all read, see all link"
      min_lines: 60
    - path: "src/features/notification/components/NotificationItem.tsx"
      provides: "Single notification display: icon by type, title, body, relative time, read/unread style"
      min_lines: 40
    - path: "src/app/[locale]/(client)/notifications/page.tsx"
      provides: "Full notifications page with Tout/Non lus filter"
    - path: "src/features/notification/components/NotificationPreferencesForm.tsx"
      provides: "Toggle switches per notification type + quiet hours inputs"
      min_lines: 80
  key_links:
    - from: "src/features/notification/components/NotificationBell.tsx"
      to: "getUnreadNotificationCountAction"
      via: "useEffect + setInterval polling every 10 seconds"
      pattern: "setInterval.*10000|getUnreadNotificationCountAction"
    - from: "src/features/notification/components/NotificationDropdown.tsx"
      to: "getNotificationsAction"
      via: "Fetch on dropdown open"
      pattern: "getNotificationsAction"
    - from: "src/features/notification/components/NotificationPreferencesForm.tsx"
      to: "updateNotificationPreferencesAction"
      via: "Form submit updates preferences"
      pattern: "updateNotificationPreferencesAction"
---

<objective>
Build the notification UI: bell icon with unread badge in navbar, notification dropdown, full notifications page with Tout/Non lus filter, and notification preferences page with per-type toggles and quiet hours.

Purpose: Gives users visibility into platform events (bookings, messages, reviews, payments) with a familiar bell-icon notification pattern. Preferences allow users to control notification volume.

Output: NotificationBell component for navbar, notification dropdown, full notifications page, preferences form with toggles and quiet hours.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-messagerie-notifications/09-02-SUMMARY.md
@src/components/layout/Navbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationBell, NotificationDropdown, NotificationItem components</name>
  <files>
    src/features/notification/components/NotificationBell.tsx
    src/features/notification/components/NotificationDropdown.tsx
    src/features/notification/components/NotificationItem.tsx
    src/components/layout/Navbar.tsx
  </files>
  <action>
1. Create `src/features/notification/components/NotificationItem.tsx` ("use client"):
   - Props: `{ notification: NotificationItem; onMarkRead?: (id: string) => void; compact?: boolean }`.
   - Icon per type: use lucide icons — CalendarCheck for BOOKING_*, CreditCard for PAYMENT_*, Star for REVIEW_*, MessageSquare for NEW_MESSAGE, Shield for KYC_*, Bell for SYSTEM/QUOTE_*.
   - Layout: flex row with icon (colored circle bg matching type), title (font-medium), body (text-sm text-muted-foreground, truncated to 2 lines in compact mode), relative time (timeAgo helper).
   - Unread style: bg-blue-50 dark:bg-blue-950/20 border-l-2 border-blue-500. Read: bg-transparent.
   - Click handler: if notification.data has a relevant URL (bookingId -> /bookings/[id], etc.), navigate using Link. Also call onMarkRead if provided.
   - Compact mode (for dropdown): smaller padding, single line body.

2. Create `src/features/notification/components/NotificationDropdown.tsx` ("use client"):
   - Props: `{ onClose: () => void }`.
   - On mount: fetch recent notifications via getNotificationsAction({ limit: 5 }).
   - Render up to 5 NotificationItem components in compact mode.
   - Header: tNotification("title") + "Tout marquer comme lu" button (calls markAllNotificationsReadAction, then refetches).
   - Footer: Link "Voir toutes les notifications" to /notifications (or /provider/notifications based on session role).
   - Empty state: tNotification("noNotifications") with Bell icon.
   - Max height: max-h-96 overflow-y-auto.

3. Create `src/features/notification/components/NotificationBell.tsx` ("use client"):
   - Uses Popover from shadcn (or DropdownMenu) to show NotificationDropdown.
   - State: unreadCount (number), isOpen (boolean).
   - Polling: useEffect + setInterval every 10 seconds. Calls getUnreadNotificationCountAction(). Updates unreadCount.
   - Bell icon (lucide Bell) with relative positioned red badge:
     - If unreadCount > 0: show absolute top-0 right-0 bg-red-500 text-white text-xs rounded-full min-w-[18px] h-[18px] flex items-center justify-center.
     - If unreadCount > 99: show "99+".
     - If 0: no badge.
   - When popover opens, fetch fresh notifications. When popover closes, refetch unread count.
   - Clean up interval on unmount.

4. Update `src/components/layout/Navbar.tsx`:
   - Replace the existing Bell icon placeholder (lines ~155-157: `<Button variant="ghost" size="icon" aria-label="Notifications"><Bell className="h-5 w-5" /></Button>`) with `<NotificationBell />` component.
   - Import NotificationBell from `@/features/notification/components/NotificationBell`.
   - Only render NotificationBell when user is authenticated (session exists). This replaces the static Bell button.
   - Remove the Bell import from lucide-react if it is no longer used elsewhere in Navbar (it may still be used — check before removing).
  </action>
  <verify>
    <automated>cd "C:\Users\pc dell\tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit the app, verify bell icon shows in navbar with red badge for unread notifications, click opens dropdown with recent notifications</manual>
  </verify>
  <done>NotificationBell shows red unread badge polling every 10s, dropdown displays up to 5 recent notifications with mark-all-read and see-all link, NotificationItem renders icon/title/body/time with unread highlight, Navbar updated to use NotificationBell instead of static placeholder</done>
</task>

<task type="auto">
  <name>Task 2: Notifications page, preferences page for client and provider</name>
  <files>
    src/app/[locale]/(client)/notifications/page.tsx
    src/app/[locale]/(provider)/provider/notifications/page.tsx
    src/features/notification/components/NotificationPreferencesForm.tsx
    src/app/[locale]/(client)/settings/notifications/page.tsx
    src/app/[locale]/(provider)/provider/settings/notifications/page.tsx
  </files>
  <action>
1. Create `src/app/[locale]/(client)/notifications/page.tsx`:
   - Server component with getTranslations.
   - Page title: tNotification("title").
   - Render a client component `NotificationsList` that handles:
     - Two filter tabs: "Tout" and "Non lus" (using shadcn Tabs or custom buttons with active state).
     - Fetch notifications via getNotificationsAction({ filter: activeFilter }).
     - Render NotificationItem for each (non-compact mode).
     - "Tout marquer comme lu" button in header.
     - Infinite scroll or "Charger plus" button for pagination (cursor-based).
     - Empty state per filter: "Aucune notification" or "Aucune notification non lue".
   - Mobile-first: full-width, no sidebar. Desktop: max-w-3xl mx-auto.

2. Create `src/app/[locale]/(provider)/provider/notifications/page.tsx`:
   - Mirror of client notifications page. Same NotificationsList component.
   - Can reuse exact same component — notification queries are user-scoped, not role-scoped.

3. Create `src/features/notification/components/NotificationPreferencesForm.tsx` ("use client"):
   - Fetches current preferences via getNotificationPreferencesAction() on mount.
   - Form layout:
     - Section "Notifications in-app": master toggle (Switch from shadcn).
     - Section "Notifications par email": master toggle (Switch).
     - Section "Types de notification": for each NotifType value (from a constant array), render a row with:
       - Type label (from tNotification(`types.${type}`)).
       - In-app toggle (Switch).
       - Email toggle (Switch).
       - Logic: if a type is in disabledTypes, both toggles are off. Track disabledTypes as a Set in state.
     - Section "Heures de silence":
       - Toggle to enable/disable quiet hours.
       - If enabled: two time inputs (HH:MM) for start and end.
       - Description: "Pendant les heures de silence, les notifications email sont suspendues."
   - Save button: calls updateNotificationPreferencesAction with form state. Show success toast.
   - Use react-hook-form with zodResolver for the preferences schema.

4. Create `src/app/[locale]/(client)/settings/notifications/page.tsx`:
   - Server component. Page title: tNotification("preferences.title").
   - Render NotificationPreferencesForm.
   - Breadcrumb or back link to /settings.

5. Create `src/app/[locale]/(provider)/provider/settings/notifications/page.tsx`:
   - Mirror of client preferences page. Same NotificationPreferencesForm component.
  </action>
  <verify>
    <automated>cd "C:\Users\pc dell\tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /notifications, verify Tout/Non lus tabs filter correctly, visit /settings/notifications to toggle preferences and save</manual>
  </verify>
  <done>Notifications page shows paginated list with Tout/Non lus filter tabs and mark-all-read button, notification preferences form has per-type toggles for in-app and email plus quiet hours time inputs, both client and provider routes have notification and preferences pages</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Bell icon in navbar shows red unread count badge
- Clicking bell opens dropdown with up to 5 recent notifications
- /notifications page has Tout/Non lus filter tabs
- /settings/notifications page allows toggling per-type preferences and quiet hours
- Both client and provider route groups have notifications and preferences pages
- Polling for unread count runs every 10 seconds
</verification>

<success_criteria>
- Bell icon with red unread badge in navbar, polling every 10s
- Dropdown shows recent notifications with type icons, mark all read, see all link
- Full notifications page with Tout/Non lus filter, cursor pagination
- NotificationItem shows type-specific icon, title, body, relative time, read/unread styling
- Preferences form: per-type in-app/email toggles, quiet hours start/end, save with toast
- Mobile-first layout for all pages
</success_criteria>

<output>
After completion, create `.planning/phases/09-messagerie-notifications/09-04-SUMMARY.md`
</output>
