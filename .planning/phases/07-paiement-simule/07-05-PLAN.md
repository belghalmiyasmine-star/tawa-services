---
phase: 07-paiement-simule
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - src/components/layout/ProviderSidebar.tsx
  - src/features/booking/components/BookingWizard.tsx
  - src/features/booking/components/QuoteAcceptFlow.tsx
  - src/features/booking/components/ClientBookingCard.tsx
  - src/app/[locale]/(client)/bookings/[bookingId]/page.tsx
  - src/messages/fr.json
autonomous: false
requirements:
  - PAY-01
  - PAY-03

must_haves:
  truths:
    - "After booking confirmation in BookingWizard, client is redirected to checkout page"
    - "After quote acceptance in QuoteAcceptFlow, client is redirected to checkout page"
    - "Client booking detail page shows 'Payer' button linking to checkout when payment is PENDING"
    - "Client booking detail page shows 'Voir la facture' link when payment is RELEASED"
    - "Provider sidebar has 'Mes revenus' entry linking to /provider/earnings"
    - "Full payment flow verified end-to-end: booking -> checkout -> confirmation -> earnings"
  artifacts:
    - path: "src/components/layout/ProviderSidebar.tsx"
      provides: "Updated sidebar with earnings navigation entry"
      contains: "earnings"
    - path: "src/features/booking/components/BookingWizard.tsx"
      provides: "Updated wizard redirecting to checkout after confirmation"
      contains: "checkout"
  key_links:
    - from: "src/features/booking/components/BookingWizard.tsx"
      to: "src/app/[locale]/(client)/bookings/[bookingId]/checkout/page.tsx"
      via: "router.push to /bookings/[id]/checkout after createBookingAction success"
      pattern: "checkout"
    - from: "src/components/layout/ProviderSidebar.tsx"
      to: "src/app/[locale]/(provider)/provider/earnings/page.tsx"
      via: "Link href to /provider/earnings"
      pattern: "earnings"
---

<objective>
Wire all payment pages into the existing navigation and booking flows, then verify the complete payment lifecycle end-to-end.

Purpose: The checkout, earnings, invoice, and statement pages exist but are not connected to the booking flow or provider navigation. This plan wires everything together and verifies the full payment lifecycle works.

Output: Updated BookingWizard, QuoteAcceptFlow, ClientBookingCard, ProviderSidebar with payment navigation. End-to-end verification of full flow.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-paiement-simule/07-CONTEXT.md
@.planning/phases/07-paiement-simule/07-01-SUMMARY.md
@.planning/phases/07-paiement-simule/07-02-SUMMARY.md
@.planning/phases/07-paiement-simule/07-03-SUMMARY.md
@src/features/booking/components/BookingWizard.tsx
@src/features/booking/components/QuoteAcceptFlow.tsx
@src/features/booking/components/ClientBookingCard.tsx
@src/components/layout/ProviderSidebar.tsx
@src/app/[locale]/(client)/bookings/[bookingId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire checkout into booking flow and add earnings to provider sidebar</name>
  <files>
    src/features/booking/components/BookingWizard.tsx
    src/features/booking/components/QuoteAcceptFlow.tsx
    src/features/booking/components/ClientBookingCard.tsx
    src/app/[locale]/(client)/bookings/[bookingId]/page.tsx
    src/components/layout/ProviderSidebar.tsx
    src/messages/fr.json
  </files>
  <action>
1. Update `src/features/booking/components/BookingWizard.tsx`:
   - Currently, after successful createBookingAction, the wizard shows a success state or redirects.
   - Change: after createBookingAction returns success with bookingId, redirect to `/bookings/${bookingId}/checkout` instead of the current success/booking detail redirect.
   - The payment method selection in Step 3 of the wizard currently selects a method and includes it in createBookingAction. Keep that behavior (the method is stored in the Payment record during booking creation). The checkout page will let the client "process" the payment (move from PENDING to HELD).
   - If the wizard currently redirects to booking detail, change to checkout: `router.push(\`/bookings/\${data.bookingId}/checkout\` as never)`

2. Update `src/features/booking/components/QuoteAcceptFlow.tsx`:
   - After acceptQuoteAction succeeds (which creates a booking), redirect to checkout: `router.push(\`/bookings/\${bookingId}/checkout\` as never)`
   - The QuoteAcceptFlow includes PaymentMethodSelector — keep it, the method is stored in the booking/payment record

3. Update `src/app/[locale]/(client)/bookings/[bookingId]/page.tsx` (booking detail):
   - Add conditional UI based on payment status:
     - If payment.status === "PENDING": show prominent "Payer" Button linking to `/bookings/${bookingId}/checkout`
     - If payment.status === "HELD": show "Paiement retenu (escrow)" badge
     - If payment.status === "RELEASED": show "Paiement libere" badge + "Voir la facture" Link to `/bookings/${bookingId}/invoice`
     - If payment.status === "REFUNDED": show "Rembourse" badge with refund amount
   - Use t("payment.status.{STATUS}") for status labels

4. Update `src/features/booking/components/ClientBookingCard.tsx`:
   - If the card shows a booking with payment.status === "PENDING", add a small "Payer" badge/link
   - If payment.status === "RELEASED", add a "Facture" link

5. Update `src/components/layout/ProviderSidebar.tsx`:
   - Add "Mes revenus" (earnings) navigation entry with DollarSign icon from lucide-react
   - Link to `/provider/earnings`
   - Position it after bookings/reservations entry
   - Add i18n key: "navigation.earnings" to fr.json sidebar section

6. Add missing i18n keys to fr.json:
   - "navigation.earnings": "Mes revenus"
   - "booking.payNow": "Payer"
   - "booking.viewInvoice": "Voir la facture"
   - "booking.paymentPending": "Paiement en attente"
   - "booking.paymentHeld": "Paiement retenu"
   - "booking.paymentReleased": "Paiement libere"
   - "booking.paymentRefunded": "Rembourse"
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>1. Create a booking -> verify redirect to checkout. 2. Check provider sidebar shows "Mes revenus". 3. Check booking detail shows payment status and links.</manual>
  </verify>
  <done>BookingWizard redirects to checkout after booking creation. QuoteAcceptFlow redirects to checkout after quote acceptance. Booking detail page shows payment status with appropriate actions (Pay/Invoice). Provider sidebar includes "Mes revenus" entry. All navigation links wired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end payment flow verification</name>
  <what-built>Complete simulated payment system: checkout with 4 Tunisian methods, escrow model, provider earnings dashboard, invoice generation, monthly statements, withdrawal requests, and full navigation wiring.</what-built>
  <how-to-verify>
    Test the following flows on http://localhost:3000:

    **Flow 1 — Direct Booking Payment:**
    1. Log in as CLIENT
    2. Find a fixed-price service and start booking
    3. Complete booking wizard (select date, time, confirm)
    4. Verify: redirected to /bookings/[id]/checkout
    5. Verify: demo banner "Mode demonstration" visible
    6. Verify: fee breakdown shows service price + platform fee = total in TND
    7. Select "Carte bancaire" — verify card form appears (number/expiry/CVV)
    8. Fill simulated card info (e.g., 1234567890123456, 12/28, 123)
    9. Click "Payer maintenant"
    10. Verify: redirected to confirmation page with reference number (TAWA-...)
    11. Verify: booking detail now shows "Paiement retenu" status

    **Flow 2 — Provider Completion + Earnings:**
    1. Log in as PROVIDER
    2. Go to bookings dashboard, find the PENDING booking, accept it
    3. Start the booking (IN_PROGRESS), then complete it
    4. Verify: payment transitions from HELD to RELEASED
    5. Navigate to "Mes revenus" in sidebar
    6. Verify: balance card shows available amount, commission deducted (12%)
    7. Verify: monthly breakdown table shows the completed mission
    8. Verify: transaction history lists the payment with RELEASED status

    **Flow 3 — Invoice:**
    1. As CLIENT: go to booking detail, click "Voir la facture"
    2. Verify: invoice page shows invoice number, date, amounts
    3. Verify: print button works (opens browser print dialog)
    4. As PROVIDER: go to earnings > transaction > click invoice link
    5. Verify: provider invoice shows commission breakdown

    **Flow 4 — Withdrawal:**
    1. As PROVIDER on earnings page
    2. Click "Demander un virement"
    3. Enter amount >= 50 TND
    4. Submit — verify success toast
    5. Try amount < 50 — verify error message

    **Flow 5 — Cash and D17/Flouci methods:**
    1. Create another booking, at checkout select D17
    2. Verify: one-click pay button (no card form)
    3. Create another booking, select "Especes"
    4. Verify: info text about cash payment shown
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Full payment lifecycle: booking -> checkout -> confirmation -> completion -> earnings -> invoice
- All 4 payment methods work on checkout page
- Provider earnings reflect commission correctly
- Invoice accessible by both parties
- Withdrawal request creates DB record with 50 TND minimum
</verification>

<success_criteria>
- PAY-01: Checkout shows 4 Tunisian payment methods with demo banner
- PAY-02: Escrow HELD on checkout, RELEASED on completion with 12% commission
- PAY-03: Earnings dashboard shows full financial overview
- PAY-04: Invoice auto-generated for completed transactions
- PAY-05: Monthly statement accessible and printable
- PAY-06: Withdrawal with 50 TND minimum enforced
- PAY-07: 5-year retention notice displayed
- PAY-08: IPaymentService abstraction layer in place
</success_criteria>

<output>
After completion, create `.planning/phases/07-paiement-simule/07-05-SUMMARY.md`
</output>
