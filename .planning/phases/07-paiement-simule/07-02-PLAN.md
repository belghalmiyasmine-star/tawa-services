---
phase: 07-paiement-simule
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/[locale]/(client)/bookings/[bookingId]/checkout/page.tsx
  - src/features/payment/components/CheckoutPage.tsx
  - src/features/payment/components/CardPaymentForm.tsx
  - src/app/[locale]/(client)/bookings/[bookingId]/confirmation/page.tsx
  - src/features/payment/components/PaymentConfirmation.tsx
autonomous: true
requirements:
  - PAY-01

must_haves:
  truths:
    - "Client sees checkout page with 4 Tunisian payment methods (carte, D17, Flouci, especes)"
    - "Demo banner 'Mode demonstration — Paiement simule' visible on checkout page"
    - "Fee breakdown shows service price + platform fee (Tawa fees) = total in TND"
    - "Simulated card form validates format (16 digits, MM/YY, 3-digit CVV) without real processing"
    - "After payment, client sees confirmation page with reference number"
    - "D17 and Flouci are one-click simulated (logo + pay button)"
  artifacts:
    - path: "src/app/[locale]/(client)/bookings/[bookingId]/checkout/page.tsx"
      provides: "Checkout route page (server component)"
      min_lines: 30
    - path: "src/features/payment/components/CheckoutPage.tsx"
      provides: "Checkout client component with payment method selection and fee breakdown"
      min_lines: 80
    - path: "src/features/payment/components/CardPaymentForm.tsx"
      provides: "Simulated card input form (number, expiry, CVV)"
      min_lines: 50
    - path: "src/app/[locale]/(client)/bookings/[bookingId]/confirmation/page.tsx"
      provides: "Payment confirmation route page"
      min_lines: 20
    - path: "src/features/payment/components/PaymentConfirmation.tsx"
      provides: "Confirmation display with reference number"
      min_lines: 40
  key_links:
    - from: "src/features/payment/components/CheckoutPage.tsx"
      to: "src/features/payment/actions/payment-actions.ts"
      via: "processPaymentAction call on form submit"
      pattern: "processPaymentAction"
    - from: "src/features/payment/components/CheckoutPage.tsx"
      to: "src/features/booking/components/PaymentMethodSelector.tsx"
      via: "reuses existing PaymentMethodSelector component"
      pattern: "PaymentMethodSelector"
    - from: "src/app/[locale]/(client)/bookings/[bookingId]/checkout/page.tsx"
      to: "src/features/payment/components/CheckoutPage.tsx"
      via: "server page renders client component with booking data"
      pattern: "CheckoutPage"
---

<objective>
Build the checkout page and payment confirmation page for the client booking flow.

Purpose: PAY-01 requires a simulated checkout UI showing Tunisian payment methods (card, D17, Flouci, cash). The checkout page is where clients "pay" after booking confirmation. The confirmation page provides a reference number as proof of payment.

Output: Checkout page at `/bookings/[bookingId]/checkout` with fee breakdown + 4 payment methods, card form for simulated input, and confirmation page at `/bookings/[bookingId]/confirmation`.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-paiement-simule/07-CONTEXT.md
@.planning/phases/07-paiement-simule/07-01-SUMMARY.md
@src/features/booking/components/PaymentMethodSelector.tsx
@src/features/booking/components/BookingWizard.tsx
@src/features/payment/actions/payment-actions.ts
@src/lib/validations/payment.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create checkout page with payment method selection and fee breakdown</name>
  <files>
    src/app/[locale]/(client)/bookings/[bookingId]/checkout/page.tsx
    src/features/payment/components/CheckoutPage.tsx
    src/features/payment/components/CardPaymentForm.tsx
  </files>
  <action>
1. Create server page `src/app/[locale]/(client)/bookings/[bookingId]/checkout/page.tsx`:
   - Fetch booking by bookingId with payment and service relations using prisma
   - Verify session is CLIENT and booking.clientId === session.user.id
   - If payment.status !== "PENDING", redirect to booking detail (already paid)
   - Pass booking data (id, totalAmount, service title, service fixedPrice) to CheckoutPage client component
   - Use getTranslations for page metadata

2. Create `src/features/payment/components/CheckoutPage.tsx`:
   - "use client" directive
   - Props: `{ bookingId: string; amount: number; serviceTitle: string; servicePrice: number }`
   - State: selectedMethod (PaymentMethod | null), isProcessing (boolean), showCardForm (boolean)
   - Layout:
     - **Demo banner**: amber bg with text "Mode demonstration — Paiement simule. Aucun montant reel ne sera debite." — use t("checkout.demoBanner")
     - **Order summary card**: service title, service price in TND, platform fee (amount * 0.05 — display only, total already includes it), total = amount TND. Format as `{amount.toFixed(2)} TND`
     - **Payment method selection**: Reuse existing `PaymentMethodSelector` from `@/features/booking/components/PaymentMethodSelector`
     - When CARD selected: show `CardPaymentForm` inline below the selector
     - When D17/FLOUCI selected: show logo area with "Payer avec D17/Flouci" button (one-click simulated)
     - When CASH selected: show info text "Le paiement sera effectue en especes lors de la prestation"
     - **Pay button**: disabled if no method selected or isProcessing. Text: t("checkout.payNow") or t("checkout.processing")
   - On submit:
     - Set isProcessing=true
     - If CARD: validate CardPaymentForm via checkoutFormSchema
     - Call processPaymentAction({ bookingId, paymentMethod: selectedMethod })
     - On success: router.push to `/bookings/${bookingId}/confirmation?ref=${data.referenceNumber}`)
     - On error: toast error, set isProcessing=false
   - Use `useRouter` from `@/i18n/routing`, `useTranslations("payment")`, and `useToast` from `@/components/ui/use-toast`

3. Create `src/features/payment/components/CardPaymentForm.tsx`:
   - "use client" directive
   - Props: `{ onValidate: (valid: boolean) => void }` — reports validation state up
   - Uses react-hook-form with zodResolver(checkoutFormSchema)
   - Fields:
     - Card number: Input with placeholder "1234 5678 9012 3456", maxLength 16, inputMode numeric
     - Expiry: Input with placeholder "MM/AA", maxLength 5
     - CVV: Input with placeholder "123", maxLength 3, type password
   - Auto-format card number with spaces every 4 digits for display (store raw digits)
   - All validation is format-only (regex) — no real card processing
   - Display validation errors below each field using FormMessage pattern from existing forms
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to /bookings/[id]/checkout — verify 4 payment methods shown, demo banner visible, fee breakdown displays in TND</manual>
  </verify>
  <done>Checkout page renders at /bookings/[bookingId]/checkout with demo banner, fee breakdown (service price + platform fee = total TND), 4 payment methods (card form for CARD, one-click for D17/Flouci, info text for CASH), and processPaymentAction integration.</done>
</task>

<task type="auto">
  <name>Task 2: Create payment confirmation page with reference number</name>
  <files>
    src/app/[locale]/(client)/bookings/[bookingId]/confirmation/page.tsx
    src/features/payment/components/PaymentConfirmation.tsx
  </files>
  <action>
1. Create server page `src/app/[locale]/(client)/bookings/[bookingId]/confirmation/page.tsx`:
   - Accept searchParams with `ref` (reference number)
   - Fetch booking with payment and service relations
   - Verify CLIENT session and ownership
   - If no payment or payment.status === "PENDING", redirect to checkout
   - Pass data to PaymentConfirmation client component

2. Create `src/features/payment/components/PaymentConfirmation.tsx`:
   - "use client" directive
   - Props: `{ referenceNumber: string; amount: number; serviceTitle: string; paymentMethod: PaymentMethod; bookingId: string }`
   - Layout:
     - Success icon (CheckCircle from lucide-react, large green)
     - Title: t("confirmation.title") — "Paiement confirme"
     - Demo banner (same amber style as checkout)
     - Reference number card: t("confirmation.reference") + referenceNumber in monospace font
     - Summary: service title, amount in TND, payment method name
     - Two buttons:
       - "Voir mes reservations" → Link to /bookings
       - "Voir le detail" → Link to /bookings/[bookingId]
   - Use `useTranslations("payment")` and Link from `@/i18n/routing`
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>After simulated payment, verify confirmation page shows reference number, success icon, and navigation links</manual>
  </verify>
  <done>Confirmation page renders at /bookings/[bookingId]/confirmation with success icon, reference number in monospace, payment summary, demo banner, and navigation links to bookings list and booking detail.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Checkout page accessible at /[locale]/bookings/[bookingId]/checkout
- Confirmation page accessible at /[locale]/bookings/[bookingId]/confirmation
- PaymentMethodSelector component reused from Phase 6
- Demo banner present on both pages
- Fee breakdown shows TND amounts
</verification>

<success_criteria>
- Client can navigate to checkout from booking, see 4 payment methods with demo banner
- Card form validates format without real processing
- D17/Flouci show one-click buttons
- Cash shows informational text
- After payment, confirmation page shows reference number
- All text uses i18n keys from payment namespace
</success_criteria>

<output>
After completion, create `.planning/phases/07-paiement-simule/07-02-SUMMARY.md`
</output>
