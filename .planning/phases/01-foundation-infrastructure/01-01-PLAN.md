---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - .eslintrc.json
  - .prettierrc
  - .prettierignore
  - .gitignore
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/env.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "La commande `npm run dev` demarre le serveur sans erreur sur http://localhost:3000"
    - "La commande `npm run build` produit un build Next.js 15 valide sans erreur TypeScript"
    - "La commande `npm run lint` passe sans erreur ESLint"
    - "Le projet utilise TypeScript strict avec paths aliases (@/*)"
    - "Prettier est configure et formate les fichiers .ts/.tsx sans conflit avec ESLint"
  artifacts:
    - path: "package.json"
      provides: "Dependances projet Next.js 15 + TypeScript + ESLint + Prettier"
      contains: "next\": \"15"
    - path: "tsconfig.json"
      provides: "Configuration TypeScript strict avec path aliases"
      contains: "@/*"
    - path: "next.config.ts"
      provides: "Configuration Next.js 15 App Router"
    - path: ".eslintrc.json"
      provides: "Configuration ESLint Next.js + TypeScript"
    - path: ".prettierrc"
      provides: "Configuration Prettier"
    - path: "src/app/layout.tsx"
      provides: "Root layout App Router"
    - path: "src/app/page.tsx"
      provides: "Page d'accueil placeholder"
  key_links:
    - from: "tsconfig.json"
      to: "src/**/*.ts"
      via: "paths alias @/*"
      pattern: "@/*"
    - from: "next.config.ts"
      to: "src/app"
      via: "App Router configuration"
      pattern: "appDir|App"
---

<objective>
Initialiser le projet Next.js 15 avec App Router, TypeScript strict, ESLint et Prettier — fondation technique sur laquelle tous les autres plans s'appuient.

User story: En tant que developpeur, je veux un projet Next.js 15 correctement configure avec TypeScript, ESLint et Prettier, afin que tous les sprints suivants demarrent sur une base technique saine et coherente.

Purpose: Ce plan cree le squelette du projet. Sans lui, aucun autre plan ne peut s'executer.
Output: Un projet Next.js 15 App Router fonctionnel avec TypeScript strict, ESLint, Prettier et variables d'environnement typees.
</objective>

<execution_context>
@C:/Users/LENOVO/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialiser le projet Next.js 15 App Router avec TypeScript</name>
  <files>
    package.json
    package-lock.json
    tsconfig.json
    next.config.ts
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    public/.gitkeep
    .gitignore
  </files>
  <action>
Depuis le repertoire C:/Users/LENOVO, creer le projet Next.js 15 dans un sous-dossier `tawa-services/` en utilisant create-next-app:

```bash
npx create-next-app@latest tawa-services \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --src-dir \
  --import-alias "@/*" \
  --no-turbopack
```

Apres creation, verifier que `package.json` contient `"next": "^15"` et `"react": "^19"`.

Modifier `tsconfig.json` pour activer les options strictes supplementaires:
```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": false,
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

Modifier `next.config.ts` pour activer les options recommandees:
```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  experimental: {
    typedRoutes: true,
  },
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "**" },
    ],
  },
};

export default nextConfig;
```

Remplacer le contenu de `src/app/page.tsx` par une page placeholder minimaliste:
```tsx
export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center">
      <h1 className="text-4xl font-bold text-blue-600">Tawa Services</h1>
      <p className="mt-4 text-gray-600">Plateforme de services locaux tunisienne</p>
    </main>
  );
}
```

Mettre a jour `.gitignore` pour inclure:
```
.env
.env.local
.env.*.local
*.env
```
  </action>
  <verify>
```bash
cd C:/Users/LENOVO/tawa-services && npm run dev
# Naviguer vers http://localhost:3000 — doit afficher "Tawa Services"
# Ctrl+C pour arreter

npm run build
# Doit terminer sans erreur TypeScript ni Next.js

npm run lint
# Doit passer sans erreur ESLint
```
  </verify>
  <done>
- `npm run dev` demarre sans erreur, page affiche "Tawa Services"
- `npm run build` produit un build valide (exit 0)
- `npm run lint` passe sans erreur (exit 0)
- `tsconfig.json` contient `"strict": true` et paths alias `@/*`
- `next.config.ts` active `typedRoutes: true`
  </done>
</task>

<task type="auto">
  <name>Task 2: Configurer ESLint strict + Prettier + variables d'environnement typees</name>
  <files>
    .eslintrc.json
    .prettierrc
    .prettierignore
    src/env.ts
    .env.example
  </files>
  <action>
Mettre a jour `.eslintrc.json` pour une configuration stricte Next.js + TypeScript:
```json
{
  "extends": [
    "next/core-web-vitals",
    "next/typescript",
    "plugin:@typescript-eslint/recommended"
  ],
  "rules": {
    "@typescript-eslint/no-unused-vars": ["error", { "argsIgnorePattern": "^_" }],
    "@typescript-eslint/no-explicit-any": "error",
    "@typescript-eslint/consistent-type-imports": ["error", { "prefer": "type-imports" }],
    "no-console": ["warn", { "allow": ["warn", "error"] }]
  }
}
```

Installer les dependances ESLint manquantes:
```bash
npm install -D @typescript-eslint/eslint-plugin @typescript-eslint/parser
```

Creer `.prettierrc`:
```json
{
  "semi": true,
  "singleQuote": false,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "plugins": ["prettier-plugin-tailwindcss"]
}
```

Installer Prettier + plugin Tailwind:
```bash
npm install -D prettier prettier-plugin-tailwindcss
```

Creer `.prettierignore`:
```
node_modules/
.next/
dist/
build/
*.lock
```

Ajouter les scripts dans `package.json`:
```json
{
  "scripts": {
    "format": "prettier --write \"src/**/*.{ts,tsx,json}\"",
    "format:check": "prettier --check \"src/**/*.{ts,tsx,json}\"",
    "typecheck": "tsc --noEmit"
  }
}
```

Creer `src/env.ts` pour les variables d'environnement typees (pattern T3 simplifie sans zod-env, utiliser Zod directement):
```ts
import { z } from "zod";

const serverSchema = z.object({
  NODE_ENV: z.enum(["development", "test", "production"]).default("development"),
  DATABASE_URL: z.string().url(),
  NEXTAUTH_SECRET: z.string().min(32),
  NEXTAUTH_URL: z.string().url().optional(),
});

const clientSchema = z.object({
  NEXT_PUBLIC_APP_URL: z.string().url().optional(),
});

const serverEnv = serverSchema.safeParse(process.env);
const clientEnv = clientSchema.safeParse(process.env);

if (!serverEnv.success) {
  console.error("Invalid server environment variables:", serverEnv.error.flatten().fieldErrors);
  if (process.env.NODE_ENV !== "test") {
    throw new Error("Invalid server environment variables");
  }
}

export const env = {
  ...serverEnv.data,
  ...clientEnv.data,
} as z.infer<typeof serverSchema> & z.infer<typeof clientSchema>;
```

Creer `.env.example`:
```env
# Base de donnees
DATABASE_URL="postgresql://user:password@localhost:5432/tawa_services"

# NextAuth
NEXTAUTH_SECRET="your-secret-key-minimum-32-characters-long"
NEXTAUTH_URL="http://localhost:3000"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

Creer `.env.local` en copiant `.env.example` avec un NEXTAUTH_SECRET factice pour le dev:
```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tawa_services"
NEXTAUTH_SECRET="dev-secret-key-min-32-chars-xxxxxxxxxx"
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

Installer Zod si pas encore present:
```bash
npm install zod
```
  </action>
  <verify>
```bash
cd C:/Users/LENOVO/tawa-services

# Typecheck
npm run typecheck
# Doit passer sans erreur (exit 0)

# Format check
npm run format:check
# Doit passer (exit 0) ou appliquer le format avec `npm run format`

# Lint final
npm run lint
# Doit passer sans erreur (exit 0)
```
  </verify>
  <done>
- `npm run typecheck` passe sans erreur TypeScript (exit 0)
- `npm run format:check` passe ou `npm run format` applique sans conflit
- `npm run lint` passe avec la config stricte @typescript-eslint/recommended (exit 0)
- `src/env.ts` existe avec schema Zod pour les env vars
- `.env.example` documente toutes les variables requises
  </done>
</task>

</tasks>

<verification>
Verification globale du plan 01:

```bash
cd C:/Users/LENOVO/tawa-services

# Suite complete
npm run dev &
sleep 5
curl -f http://localhost:3000 | grep "Tawa Services"
kill %1

npm run build && echo "BUILD OK"
npm run lint && echo "LINT OK"
npm run typecheck && echo "TYPECHECK OK"
```

Tous les commandes doivent retourner exit 0 et afficher les messages de succes.
</verification>

<success_criteria>
Acceptance criteria:
1. `npm run dev` demarre le serveur Next.js 15 sur port 3000 sans erreur
2. La page d'accueil affiche "Tawa Services" avec le bon style Tailwind bleu
3. `npm run build` produit un build valide (aucune erreur TypeScript ni Next.js)
4. `npm run lint` passe avec la configuration ESLint stricte
5. `npm run typecheck` passe avec TypeScript strict mode active
6. `prettier-plugin-tailwindcss` est configure pour trier automatiquement les classes Tailwind
7. `.env.example` documente DATABASE_URL, NEXTAUTH_SECRET, NEXTAUTH_URL, NEXT_PUBLIC_APP_URL
8. `src/env.ts` valide les variables d'environnement au demarrage avec Zod
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md` avec:
- Les versions exactes des packages installes (next, react, typescript, eslint, prettier)
- La structure de dossier cree (`src/app/`, `src/components/` a venir, etc.)
- Les scripts npm disponibles
- Toute decision technique prise (ex: version exacte de Next.js)
</output>
