---
phase: 01-foundation-infrastructure
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tailwind.config.ts
  - src/app/globals.css
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/avatar.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/select.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/toast.tsx
  - src/components/ui/separator.tsx
  - src/lib/utils.ts
  - package.json
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "shadcn/ui est initialise avec les composants de base installes et fonctionnels"
    - "La couleur primaire blue (#2563EB) est configuree comme variable CSS `--primary`"
    - "La couleur secondaire orange/amber est configuree comme variable CSS `--secondary` pour les CTAs"
    - "Le style Tailwind est 'rounded-lg' ou superieur sur les composants (design arrondi et doux)"
    - "La police Inter (Google Fonts) est appliquee globalement avec `font-sans`"
    - "Le dark mode est configure avec `next-themes` et toggle fonctionnel"
    - "Les composants shadcn/ui de base sont disponibles: Button, Card, Input, Badge, Avatar, Dialog, Sheet, DropdownMenu, Select, Skeleton, Toast, Separator"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "Configuration Tailwind avec tokens de couleur personnalises"
      contains: "primary"
    - path: "src/app/globals.css"
      provides: "Variables CSS pour les tokens de design (--primary, --secondary, etc.)"
      contains: "--primary"
    - path: "src/lib/utils.ts"
      provides: "Fonction cn() pour merger les classes Tailwind"
      exports: ["cn"]
    - path: "src/components/ui/"
      provides: "Composants shadcn/ui de base"
  key_links:
    - from: "tailwind.config.ts"
      to: "src/app/globals.css"
      via: "CSS custom properties mapping"
      pattern: "hsl\\(var\\(--"
    - from: "src/components/ui/button.tsx"
      to: "src/lib/utils.ts"
      via: "cn() function import"
      pattern: "from.*utils"
---

<objective>
Configurer shadcn/ui + Tailwind avec le design system complet de Tawa Services: couleurs (bleu primaire, orange accent), style arrondi et soft (Airbnb-inspired), dark mode via next-themes, et les composants de base qui seront utilises dans tous les sprints suivants.

User story: En tant que developpeur, je veux un design system configure avec les tokens de couleur, la typographie et les composants de base de shadcn/ui, afin que tous les futurs composants UI soient coherents visuellement sans avoir a redefinir les styles de base a chaque fois.

Purpose: Un design system etabli des Phase 1 garantit la coherence visuelle sur les 10 sprints suivants. Sans cela, chaque sprint redefinit les couleurs et styles de maniere inconsistante.
Output: tailwind.config.ts avec tokens personnalises, globals.css avec variables CSS, composants shadcn/ui de base installes, dark mode fonctionnel.
</objective>

<execution_context>
@C:/Users/LENOVO/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialiser shadcn/ui et configurer les tokens de design Tawa Services</name>
  <files>
    tailwind.config.ts
    src/app/globals.css
    src/lib/utils.ts
    package.json
    components.json
  </files>
  <action>
Initialiser shadcn/ui (en choisissant les options correspondant aux decisions CONTEXT.md):

```bash
cd C:/Users/LENOVO/tawa-services
npx shadcn@latest init
```

Lors de l'initialisation interactive, repondre:
- Style: Default (New York si propose — style plus moderne)
- Base color: Blue
- CSS variables: Yes
- Global CSS: src/app/globals.css
- tailwind config: tailwind.config.ts
- Components alias: @/components
- Utils alias: @/lib/utils

Apres initialisation, mettre a jour `src/app/globals.css` pour les tokens de design Tawa Services. Remplacer les valeurs par defaut shadcn par les valeurs specifiques au projet:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    /* === TAWA SERVICES DESIGN TOKENS === */

    /* Primary: Blue (#2563EB) — trustworthy, professional */
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;

    --card: 0 0% 100%;
    --card-foreground: 222.2 84% 4.9%;

    --popover: 0 0% 100%;
    --popover-foreground: 222.2 84% 4.9%;

    /* Blue #2563EB = HSL(217 91% 60%) */
    --primary: 217 91% 60%;
    --primary-foreground: 210 40% 98%;

    /* Warm Orange/Amber for CTAs (#F59E0B = HSL(38 92% 50%)) */
    --secondary: 38 92% 50%;
    --secondary-foreground: 222.2 47.4% 11.2%;

    --muted: 210 40% 96.1%;
    --muted-foreground: 215.4 16.3% 46.9%;

    /* Amber accent for highlights */
    --accent: 38 92% 50%;
    --accent-foreground: 222.2 47.4% 11.2%;

    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 210 40% 98%;

    --border: 214.3 31.8% 91.4%;
    --input: 214.3 31.8% 91.4%;
    --ring: 217 91% 60%;

    /* Rounded design — Airbnb-inspired */
    --radius: 0.75rem;

    /* Success color */
    --success: 142 71% 45%;
    --success-foreground: 0 0% 100%;

    /* Warning color */
    --warning: 38 92% 50%;
    --warning-foreground: 222.2 47.4% 11.2%;
  }

  .dark {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;

    --card: 222.2 84% 4.9%;
    --card-foreground: 210 40% 98%;

    --popover: 222.2 84% 4.9%;
    --popover-foreground: 210 40% 98%;

    --primary: 217 91% 60%;
    --primary-foreground: 222.2 47.4% 11.2%;

    --secondary: 38 92% 50%;
    --secondary-foreground: 210 40% 98%;

    --muted: 217.2 32.6% 17.5%;
    --muted-foreground: 215 20.2% 65.1%;

    --accent: 217.2 32.6% 17.5%;
    --accent-foreground: 210 40% 98%;

    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 210 40% 98%;

    --border: 217.2 32.6% 17.5%;
    --input: 217.2 32.6% 17.5%;
    --ring: 217 91% 60%;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
    font-feature-settings: "rlig" 1, "calt" 1;
  }
}
```

Mettre a jour `tailwind.config.ts` pour ajouter les extensions de design system:
```ts
import type { Config } from "tailwindcss";
import { fontFamily } from "tailwindcss/defaultTheme";

const config: Config = {
  darkMode: ["class"],
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/features/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        success: {
          DEFAULT: "hsl(var(--success))",
          foreground: "hsl(var(--success-foreground))",
        },
        warning: {
          DEFAULT: "hsl(var(--warning))",
          foreground: "hsl(var(--warning-foreground))",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      fontFamily: {
        sans: ["Inter", ...fontFamily.sans],
      },
      boxShadow: {
        soft: "0 2px 15px -3px rgba(0, 0, 0, 0.07), 0 10px 20px -2px rgba(0, 0, 0, 0.04)",
        card: "0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06)",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
};

export default config;
```

Installer tailwindcss-animate si pas encore installe:
```bash
npm install tailwindcss-animate
```
  </action>
  <verify>
```bash
cd C:/Users/LENOVO/tawa-services

# Verifier les fichiers generes par shadcn init
ls components.json
ls src/lib/utils.ts
cat src/lib/utils.ts | grep "cn"

# Typecheck
npm run typecheck

# Build pour verifier la compilation Tailwind
npm run build 2>&1 | tail -5
```
  </verify>
  <done>
- `components.json` existe avec la configuration shadcn/ui
- `src/lib/utils.ts` contient la fonction `cn()` basee sur `clsx` + `tailwind-merge`
- `src/app/globals.css` contient `--primary: 217 91% 60%` (bleu) et `--secondary: 38 92% 50%` (orange)
- `tailwind.config.ts` etend les couleurs avec les tokens HSL et inclut `fontFamily.sans: ["Inter", ...]`
- `--radius: 0.75rem` configure le design arrondi
- `tailwindcss-animate` est installe pour les animations shadcn/ui
- `npm run typecheck` passe sans erreur
  </done>
</task>

<task type="auto">
  <name>Task 2: Installer les composants shadcn/ui de base et configurer next-themes</name>
  <files>
    src/components/ui/button.tsx
    src/components/ui/card.tsx
    src/components/ui/input.tsx
    src/components/ui/badge.tsx
    src/components/ui/avatar.tsx
    src/components/ui/dialog.tsx
    src/components/ui/sheet.tsx
    src/components/ui/dropdown-menu.tsx
    src/components/ui/select.tsx
    src/components/ui/skeleton.tsx
    src/components/ui/separator.tsx
    src/components/ui/label.tsx
    src/components/ui/form.tsx
    src/components/ui/textarea.tsx
    src/components/ui/checkbox.tsx
    src/components/ui/scroll-area.tsx
    src/components/ui/tabs.tsx
    src/components/shared/ThemeProvider.tsx
    src/components/shared/ThemeToggle.tsx
    package.json
  </files>
  <action>
Installer les composants shadcn/ui de base:
```bash
cd C:/Users/LENOVO/tawa-services

npx shadcn@latest add button
npx shadcn@latest add card
npx shadcn@latest add input
npx shadcn@latest add badge
npx shadcn@latest add avatar
npx shadcn@latest add dialog
npx shadcn@latest add sheet
npx shadcn@latest add dropdown-menu
npx shadcn@latest add select
npx shadcn@latest add skeleton
npx shadcn@latest add separator
npx shadcn@latest add label
npx shadcn@latest add form
npx shadcn@latest add textarea
npx shadcn@latest add checkbox
npx shadcn@latest add scroll-area
npx shadcn@latest add tabs
npx shadcn@latest add toast
```

Installer next-themes pour le dark mode:
```bash
npm install next-themes
```

Creer `src/components/shared/ThemeProvider.tsx`:
```tsx
"use client";

import { ThemeProvider as NextThemesProvider } from "next-themes";
import type { ThemeProviderProps } from "next-themes";

export function ThemeProvider({ children, ...props }: ThemeProviderProps) {
  return <NextThemesProvider {...props}>{children}</NextThemesProvider>;
}
```

Creer `src/components/shared/ThemeToggle.tsx`:
```tsx
"use client";

import { Moon, Sun } from "lucide-react";
import { useTheme } from "next-themes";
import { Button } from "@/components/ui/button";

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();

  return (
    <Button
      variant="ghost"
      size="icon"
      onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
      aria-label="Changer de theme"
    >
      <Sun className="h-5 w-5 rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
      <Moon className="absolute h-5 w-5 rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
    </Button>
  );
}
```

Installer lucide-react si pas encore installe:
```bash
npm install lucide-react
```

Mettre a jour `src/app/[locale]/layout.tsx` pour integrer ThemeProvider (si le fichier existe deja depuis plan 03, sinon le layout racine `src/app/[locale]/layout.tsx`). Ajouter le ThemeProvider autour du NextIntlClientProvider:

Si `src/app/[locale]/layout.tsx` existe:
```tsx
// Ajouter l'import
import { ThemeProvider } from "@/components/shared/ThemeProvider";

// Wrapper dans le body
<body className={inter.className}>
  <ThemeProvider
    attribute="class"
    defaultTheme="light"
    enableSystem={false}
    disableTransitionOnChange
  >
    <NextIntlClientProvider messages={messages}>
      {children}
    </NextIntlClientProvider>
  </ThemeProvider>
</body>
```

Si `src/app/[locale]/layout.tsx` n'existe pas encore (plan 03 pas execute), creer `src/app/layout.tsx` avec le ThemeProvider pour integration future.
  </action>
  <verify>
```bash
cd C:/Users/LENOVO/tawa-services

# Verifier que les composants shadcn sont installes
ls src/components/ui/ | head -20
# Doit lister: button.tsx, card.tsx, input.tsx, badge.tsx, avatar.tsx, etc.

# Verifier next-themes
ls node_modules/next-themes/package.json

# Verifier lucide-react
ls node_modules/lucide-react/package.json

# Typecheck complet
npm run typecheck

# Build
npm run build 2>&1 | grep -E "error|warning|compiled" | tail -10
```
  </verify>
  <done>
- `src/components/ui/` contient >= 15 composants shadcn/ui (button, card, input, badge, avatar, dialog, sheet, dropdown-menu, select, skeleton, separator, label, form, textarea, checkbox, scroll-area, tabs, toast)
- `next-themes` est installe
- `src/components/shared/ThemeProvider.tsx` existe avec le wrapper NextThemesProvider
- `src/components/shared/ThemeToggle.tsx` existe avec les icones Sun/Moon de lucide-react
- `lucide-react` est installe
- `ThemeProvider` est integre dans le layout racine avec `defaultTheme: "light"`
- `npm run typecheck` et `npm run build` passent sans erreur
  </done>
</task>

</tasks>

<verification>
Verification globale du plan 04:

```bash
cd C:/Users/LENOVO/tawa-services

# Build complet
npm run build && echo "BUILD OK"

# Verifier les tokens de couleur dans globals.css
grep "--primary: 217" src/app/globals.css && echo "PRIMARY COLOR OK"
grep "--secondary: 38" src/app/globals.css && echo "SECONDARY COLOR OK"
grep "--radius: 0.75rem" src/app/globals.css && echo "RADIUS OK"

# Compter les composants UI
echo "shadcn/ui components: $(ls src/components/ui/ | wc -l)"

# Demarrer et verifier visuellement
npm run dev &
sleep 5
curl -s http://localhost:3000/fr | grep -i "tawa"
kill %1
```
</verification>

<success_criteria>
Acceptance criteria:
1. `npm run build` produit un build valide avec shadcn/ui configure
2. `--primary: 217 91% 60%` (bleu #2563EB) est defini dans globals.css
3. `--secondary: 38 92% 50%` (orange #F59E0B) est defini comme accent/CTA
4. `--radius: 0.75rem` est configure pour le style arrondi et soft
5. >= 15 composants shadcn/ui sont installes dans `src/components/ui/`
6. `next-themes` est installe et le `ThemeProvider` est configure avec `defaultTheme: "light"`
7. `ThemeToggle` utilise les icones `Sun`/`Moon` de `lucide-react`
8. `src/lib/utils.ts` exporte la fonction `cn()` (clsx + tailwind-merge)
9. `tailwind.config.ts` scanne `src/features/**/*.{tsx,ts}` pour les classes Tailwind futures
10. `npm run typecheck` passe sans erreur
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-foundation-infrastructure/01-04-SUMMARY.md` avec:
- Liste complete des composants shadcn/ui installes
- Version de shadcn/ui et next-themes
- Tokens de design definis (couleurs, radius, shadows)
- Configuration du dark mode
- Note: `src/features/**` est dans le content path Tailwind pour les futures features
</output>
