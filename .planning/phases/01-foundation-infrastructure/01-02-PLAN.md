---
phase: 01-foundation-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/prisma.ts
  - package.json
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Le schema Prisma contient tous les modeles v1 (User, Provider, Service, Category, Booking, Payment, Review, Message, Notification, Gouvernorat, Delegation)"
    - "`npx prisma migrate dev` applique les migrations sans erreur sur PostgreSQL local"
    - "`npx prisma generate` genere le client Prisma sans erreur TypeScript"
    - "Tous les modeles utilisent CUID2 comme identifiant (cuid() dans Prisma)"
    - "Tous les modeles ont les champs `isDeleted Boolean @default(false)` et `deletedAt DateTime?` pour soft delete"
    - "La localisation utilise des tables normalisees Gouvernorat et Delegation reliees par cle etrangere"
    - "Les categories de services ont une hierarchie deux niveaux (Category parent → Category enfant)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Schema complet avec tous les modeles v1"
      contains: "model User"
      min_lines: 200
    - path: "src/lib/prisma.ts"
      provides: "Singleton Prisma client pour Next.js"
      exports: ["prisma"]
  key_links:
    - from: "src/lib/prisma.ts"
      to: "prisma/schema.prisma"
      via: "PrismaClient generation"
      pattern: "PrismaClient|prisma"
    - from: "prisma/schema.prisma"
      to: "DATABASE_URL"
      via: "datasource db"
      pattern: "datasource db"
---

<objective>
Configurer PostgreSQL + Prisma ORM avec le schema complet v1 — tous les modeles de la plateforme Tawa Services avec CUID2, soft delete, et tables de localisation normalisees.

User story: En tant que developpeur, je veux un schema Prisma complet avec tous les modeles v1 et les migrations appliquees, afin que tous les sprints de features (Auth, Booking, Payment, etc.) puissent referencer des modeles de donnees precis et types.

Purpose: La base de donnees est le fondement de toute la logique metier. Un schema complet et bien modelise des Phase 1 evite les migrations cassantes dans les phases suivantes.
Output: `prisma/schema.prisma` avec 15+ modeles, migrations PostgreSQL appliquees, client Prisma genere et singleton configure.
</objective>

<execution_context>
@C:/Users/LENOVO/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer Prisma et creer le schema complet v1</name>
  <files>
    prisma/schema.prisma
    package.json
  </files>
  <action>
Depuis `C:/Users/LENOVO/tawa-services`, installer Prisma:

```bash
npm install @prisma/client
npm install -D prisma
```

Creer `prisma/schema.prisma` avec le contenu complet suivant. Respecter scrupuleusement les decisions du CONTEXT.md:
- IDs: `@default(cuid())` (CUID2, URL-safe, sortable)
- Soft delete: `isDeleted Boolean @default(false)` + `deletedAt DateTime?` sur TOUS les modeles
- Localisation: tables normalisees `Gouvernorat` et `Delegation` avec FK
- Categories: hierarchie deux niveaux avec `parentId` self-relation

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ============================================================
// ENUMERATIONS
// ============================================================

enum Role {
  CLIENT
  PROVIDER
  ADMIN
}

enum KYCStatus {
  NOT_SUBMITTED
  PENDING
  APPROVED
  REJECTED
}

enum ServiceStatus {
  DRAFT
  PENDING_APPROVAL
  ACTIVE
  SUSPENDED
  DELETED
}

enum PricingType {
  FIXED
  SUR_DEVIS
}

enum BookingStatus {
  PENDING
  ACCEPTED
  IN_PROGRESS
  COMPLETED
  REJECTED
  CANCELLED
}

enum QuoteStatus {
  PENDING
  RESPONDED
  ACCEPTED
  DECLINED
  EXPIRED
}

enum PaymentStatus {
  PENDING
  HELD
  RELEASED
  REFUNDED
  FAILED
}

enum PaymentMethod {
  CARD
  D17
  FLOUCI
  CASH
}

enum NotifType {
  BOOKING_REQUEST
  BOOKING_ACCEPTED
  BOOKING_REJECTED
  BOOKING_COMPLETED
  BOOKING_CANCELLED
  QUOTE_RECEIVED
  QUOTE_RESPONDED
  PAYMENT_RECEIVED
  REVIEW_RECEIVED
  KYC_APPROVED
  KYC_REJECTED
  NEW_MESSAGE
  SYSTEM
}

// ============================================================
// LOCALISATION
// ============================================================

model Gouvernorat {
  id          String       @id @default(cuid())
  name        String       @unique
  nameAr      String?
  code        String?      @unique
  delegations Delegation[]
  createdAt   DateTime     @default(now())
  isDeleted   Boolean      @default(false)
  deletedAt   DateTime?

  @@map("gouvernorats")
}

model Delegation {
  id             String      @id @default(cuid())
  name           String
  nameAr         String?
  gouvernoratId  String
  gouvernorat    Gouvernorat @relation(fields: [gouvernoratId], references: [id])
  providers      ProviderDelegation[]
  createdAt      DateTime    @default(now())
  isDeleted      Boolean     @default(false)
  deletedAt      DateTime?

  @@unique([name, gouvernoratId])
  @@map("delegations")
}

// ============================================================
// USERS & AUTH
// ============================================================

model User {
  id            String    @id @default(cuid())
  email         String    @unique
  passwordHash  String?
  phone         String?   @unique
  role          Role      @default(CLIENT)
  emailVerified Boolean   @default(false)
  phoneVerified Boolean   @default(false)
  name          String?
  avatarUrl     String?
  isActive      Boolean   @default(true)
  isBanned      Boolean   @default(false)
  bannedAt      DateTime?
  bannedReason  String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  isDeleted     Boolean   @default(false)
  deletedAt     DateTime?

  // Relations
  provider              Provider?
  clientBookings        Booking[]             @relation("ClientBookings")
  sentMessages          Message[]             @relation("SentMessages")
  notifications         Notification[]
  notificationPrefs     NotificationPreference?
  accounts              Account[]
  sessions              Session[]
  emailVerifications    EmailVerification[]
  passwordResets        PasswordReset[]

  @@map("users")
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@map("accounts")
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("sessions")
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
  @@map("verification_tokens")
}

model EmailVerification {
  id        String   @id @default(cuid())
  userId    String
  token     String   @unique
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("email_verifications")
}

model PasswordReset {
  id        String   @id @default(cuid())
  userId    String
  token     String   @unique
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("password_resets")
}

// ============================================================
// PROVIDER PROFILE
// ============================================================

model Provider {
  id               String     @id @default(cuid())
  userId           String     @unique
  user             User       @relation(fields: [userId], references: [id], onDelete: Cascade)
  displayName      String
  bio              String?    @db.Text
  photoUrl         String?
  phone            String?
  kycStatus        KYCStatus  @default(NOT_SUBMITTED)
  kycSubmittedAt   DateTime?
  kycApprovedAt    DateTime?
  kycRejectedAt    DateTime?
  kycRejectedReason String?
  yearsExperience  Int?
  languages        String[]   @default([])
  rating           Float      @default(0)
  ratingCount      Int        @default(0)
  completedMissions Int       @default(0)
  responseTimeHours Float?
  isActive         Boolean    @default(true)
  isFeatured       Boolean    @default(false)
  createdAt        DateTime   @default(now())
  updatedAt        DateTime   @updatedAt
  isDeleted        Boolean    @default(false)
  deletedAt        DateTime?

  // Relations
  services         Service[]
  availabilities   Availability[]
  blockedDates     BlockedDate[]
  certifications   Certification[]
  kycDocuments     KYCDocument[]
  delegations      ProviderDelegation[]
  providerBookings Booking[]  @relation("ProviderBookings")
  trustBadges      TrustBadge[]

  @@map("providers")
}

model ProviderDelegation {
  id           String     @id @default(cuid())
  providerId   String
  delegationId String
  provider     Provider   @relation(fields: [providerId], references: [id], onDelete: Cascade)
  delegation   Delegation @relation(fields: [delegationId], references: [id])

  @@unique([providerId, delegationId])
  @@map("provider_delegations")
}

model TrustBadge {
  id         String   @id @default(cuid())
  providerId String
  provider   Provider @relation(fields: [providerId], references: [id], onDelete: Cascade)
  badgeType  String   // "IDENTITY_VERIFIED" | "QUICK_RESPONSE" | "TOP_PROVIDER"
  awardedAt  DateTime @default(now())
  isActive   Boolean  @default(true)

  @@unique([providerId, badgeType])
  @@map("trust_badges")
}

model Availability {
  id         String   @id @default(cuid())
  providerId String
  provider   Provider @relation(fields: [providerId], references: [id], onDelete: Cascade)
  dayOfWeek  Int      // 0=Sunday, 6=Saturday
  startTime  String   // "HH:MM" format
  endTime    String   // "HH:MM" format
  isActive   Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@unique([providerId, dayOfWeek])
  @@map("availabilities")
}

model BlockedDate {
  id         String   @id @default(cuid())
  providerId String
  provider   Provider @relation(fields: [providerId], references: [id], onDelete: Cascade)
  date       DateTime @db.Date
  reason     String?
  createdAt  DateTime @default(now())

  @@unique([providerId, date])
  @@map("blocked_dates")
}

model Certification {
  id         String   @id @default(cuid())
  providerId String
  provider   Provider @relation(fields: [providerId], references: [id], onDelete: Cascade)
  title      String
  fileUrl    String
  issuedAt   DateTime?
  createdAt  DateTime @default(now())
  isDeleted  Boolean  @default(false)
  deletedAt  DateTime?

  @@map("certifications")
}

// ============================================================
// KYC
// ============================================================

model KYCDocument {
  id          String   @id @default(cuid())
  providerId  String
  provider    Provider @relation(fields: [providerId], references: [id], onDelete: Cascade)
  docType     String   // "CIN_PASSPORT" | "SELFIE" | "PROOF_OF_ADDRESS"
  fileUrl     String
  uploadedAt  DateTime @default(now())
  adminNote   String?
  isDeleted   Boolean  @default(false)
  deletedAt   DateTime?

  @@map("kyc_documents")
}

// ============================================================
// CATEGORIES & SERVICES
// ============================================================

model Category {
  id          String     @id @default(cuid())
  name        String
  nameAr      String?
  slug        String     @unique
  icon        String?    // lucide-react icon name
  description String?
  parentId    String?
  parent      Category?  @relation("SubCategories", fields: [parentId], references: [id])
  children    Category[] @relation("SubCategories")
  isActive    Boolean    @default(true)
  sortOrder   Int        @default(0)
  services    Service[]
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  isDeleted   Boolean    @default(false)
  deletedAt   DateTime?

  @@map("categories")
}

model Service {
  id            String        @id @default(cuid())
  providerId    String
  provider      Provider      @relation(fields: [providerId], references: [id], onDelete: Cascade)
  categoryId    String
  category      Category      @relation(fields: [categoryId], references: [id])
  title         String        @db.VarChar(80)
  description   String        @db.Text
  pricingType   PricingType
  fixedPrice    Float?
  durationMinutes Int?
  inclusions    String[]      @default([])
  exclusions    String[]      @default([])
  conditions    String?       @db.Text
  photoUrls     String[]      @default([])
  status        ServiceStatus @default(DRAFT)
  isFeatured    Boolean       @default(false)
  viewCount     Int           @default(0)
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  isDeleted     Boolean       @default(false)
  deletedAt     DateTime?

  // Relations
  bookings      Booking[]
  quotes        Quote[]

  @@map("services")
}

// ============================================================
// BOOKINGS & QUOTES
// ============================================================

model Booking {
  id              String        @id @default(cuid())
  clientId        String
  client          User          @relation("ClientBookings", fields: [clientId], references: [id])
  providerId      String
  provider        Provider      @relation("ProviderBookings", fields: [providerId], references: [id])
  serviceId       String
  service         Service       @relation(fields: [serviceId], references: [id])
  status          BookingStatus @default(PENDING)
  scheduledAt     DateTime?
  completedAt     DateTime?
  cancelledAt     DateTime?
  cancelledBy     String?       // "CLIENT" | "PROVIDER"
  cancelReason    String?
  totalAmount     Float
  clientNote      String?       @db.Text
  providerNote    String?       @db.Text
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
  isDeleted       Boolean       @default(false)
  deletedAt       DateTime?

  // Relations
  payment         Payment?
  reviews         Review[]
  conversation    Conversation?
  quoteId         String?       @unique
  quote           Quote?        @relation(fields: [quoteId], references: [id])

  @@map("bookings")
}

model Quote {
  id           String      @id @default(cuid())
  clientId     String
  serviceId    String
  service      Service     @relation(fields: [serviceId], references: [id])
  status       QuoteStatus @default(PENDING)
  description  String      @db.Text
  proposedPrice Float?
  proposedDelay String?
  expiresAt    DateTime
  respondedAt  DateTime?
  acceptedAt   DateTime?
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt
  isDeleted    Boolean     @default(false)
  deletedAt    DateTime?

  // Relations
  booking      Booking?

  @@map("quotes")
}

// ============================================================
// PAYMENTS
// ============================================================

model Payment {
  id            String        @id @default(cuid())
  bookingId     String        @unique
  booking       Booking       @relation(fields: [bookingId], references: [id])
  method        PaymentMethod
  status        PaymentStatus @default(PENDING)
  amount        Float
  commission    Float         @default(0)
  providerEarning Float       @default(0)
  paidAt        DateTime?
  heldAt        DateTime?
  releasedAt    DateTime?
  refundedAt    DateTime?
  refundAmount  Float?
  invoiceUrl    String?
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  isDeleted     Boolean       @default(false)
  deletedAt     DateTime?

  // Relations
  withdrawalRequest WithdrawalRequest?

  @@map("payments")
}

model WithdrawalRequest {
  id          String   @id @default(cuid())
  providerId  String
  paymentId   String   @unique
  payment     Payment  @relation(fields: [paymentId], references: [id])
  amount      Float
  status      String   @default("PENDING") // "PENDING" | "APPROVED" | "REJECTED" | "PAID"
  requestedAt DateTime @default(now())
  processedAt DateTime?
  adminNote   String?
  isDeleted   Boolean  @default(false)
  deletedAt   DateTime?

  @@map("withdrawal_requests")
}

// ============================================================
// REVIEWS
// ============================================================

model Review {
  id              String   @id @default(cuid())
  bookingId       String
  booking         Booking  @relation(fields: [bookingId], references: [id])
  authorId        String   // USER who wrote the review
  targetId        String   // USER or PROVIDER being reviewed
  authorRole      String   // "CLIENT" | "PROVIDER"
  stars           Int
  qualityRating   Int?     // 1-5 criteria
  punctualityRating Int?
  communicationRating Int?
  cleanlinessRating Int?
  text            String?  @db.Text
  photoUrls       String[] @default([])
  published       Boolean  @default(false)
  publishedAt     DateTime?
  flagged         Boolean  @default(false)
  flaggedReason   String?
  moderatedAt     DateTime?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  isDeleted       Boolean  @default(false)
  deletedAt       DateTime?

  @@unique([bookingId, authorId])
  @@map("reviews")
}

// ============================================================
// MESSAGING
// ============================================================

model Conversation {
  id         String    @id @default(cuid())
  bookingId  String    @unique
  booking    Booking   @relation(fields: [bookingId], references: [id])
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt
  isDeleted  Boolean   @default(false)
  deletedAt  DateTime?

  // Relations
  messages   Message[]

  @@map("conversations")
}

model Message {
  id             String       @id @default(cuid())
  conversationId String
  conversation   Conversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  senderId       String
  sender         User         @relation("SentMessages", fields: [senderId], references: [id])
  content        String       @db.Text
  isRead         Boolean      @default(false)
  readAt         DateTime?
  flagged        Boolean      @default(false)
  flaggedReason  String?
  createdAt      DateTime     @default(now())
  isDeleted      Boolean      @default(false)
  deletedAt      DateTime?

  @@map("messages")
}

// ============================================================
// NOTIFICATIONS
// ============================================================

model Notification {
  id        String    @id @default(cuid())
  userId    String
  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  type      NotifType
  title     String
  body      String?
  read      Boolean   @default(false)
  readAt    DateTime?
  data      Json?     // Extra contextual data (bookingId, providerId, etc.)
  createdAt DateTime  @default(now())
  isDeleted Boolean   @default(false)
  deletedAt DateTime?

  @@map("notifications")
}

model NotificationPreference {
  id          String   @id @default(cuid())
  userId      String   @unique
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  emailEnabled Boolean @default(true)
  inAppEnabled Boolean @default(true)
  quietHoursStart String? // "HH:MM"
  quietHoursEnd   String? // "HH:MM"
  disabledTypes   String[] @default([])
  updatedAt   DateTime @updatedAt

  @@map("notification_preferences")
}
```

Apres avoir cree le fichier, verifier sa validite:
```bash
npx prisma validate
```
  </action>
  <verify>
```bash
cd C:/Users/LENOVO/tawa-services

# Valider le schema sans connexion DB
npx prisma validate
# Doit afficher "The schema at prisma/schema.prisma is valid"

# Compter les modeles (doit etre >= 20)
grep -c "^model " prisma/schema.prisma

# Verifier la presence des champs soft delete sur tous les modeles principaux
grep -A 5 "model User" prisma/schema.prisma | grep "isDeleted"
grep -A 5 "model Provider" prisma/schema.prisma | grep "isDeleted"
grep -A 5 "model Booking" prisma/schema.prisma | grep "isDeleted"
```
  </verify>
  <done>
- `npx prisma validate` retourne "The schema is valid" (exit 0)
- Le schema contient >= 20 modeles couvrant tous les domaines v1
- Tous les modeles principaux (User, Provider, Service, Booking, Payment, Review, Message, Notification) ont `isDeleted Boolean @default(false)` et `deletedAt DateTime?`
- La table Category a une auto-relation `parentId` pour la hierarchie deux niveaux
- Les tables Gouvernorat et Delegation existent avec leur FK
- La table ProviderDelegation est une junction table pour la zone d'intervention
- Tous les IDs utilisent `@default(cuid())`
  </done>
</task>

<task type="auto">
  <name>Task 2: Appliquer les migrations et configurer le singleton Prisma client</name>
  <files>
    prisma/migrations/
    src/lib/prisma.ts
  </files>
  <action>
Appliquer les migrations sur PostgreSQL local (le service PostgreSQL doit etre demarre):

```bash
cd C:/Users/LENOVO/tawa-services

# Creer et appliquer la migration initiale
npx prisma migrate dev --name init
```

Si PostgreSQL n'est pas disponible localement, utiliser `prisma db push` pour synchroniser sans migration:
```bash
npx prisma db push
```

Generer le client Prisma:
```bash
npx prisma generate
```

Creer `src/lib/prisma.ts` — singleton pattern pour Next.js (evite les connexions multiples en development hot-reload):
```ts
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log:
      process.env.NODE_ENV === "development"
        ? ["query", "error", "warn"]
        : ["error"],
  });

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;
```

Ajouter le script Prisma dans `package.json`:
```json
{
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset --force"
  }
}
```

Ajouter `prisma generate` dans le script `postinstall` pour les environments de deploiement:
```json
{
  "scripts": {
    "postinstall": "prisma generate"
  }
}
```
  </action>
  <verify>
```bash
cd C:/Users/LENOVO/tawa-services

# Verifier que le client est genere
ls node_modules/@prisma/client/index.d.ts
# Doit exister

# Verifier la compilation TypeScript du fichier prisma.ts
npx tsc --noEmit
# Doit passer sans erreur sur src/lib/prisma.ts

# Si DB disponible: test de connexion basique
node -e "const { prisma } = require('./src/lib/prisma.ts'); console.log('Prisma client OK')" 2>/dev/null || echo "DB not available - client generation verified"
```
  </verify>
  <done>
- `npx prisma generate` complete sans erreur
- `src/lib/prisma.ts` existe avec le singleton pattern
- `node_modules/@prisma/client/index.d.ts` existe (types generes)
- `npm run typecheck` passe (src/lib/prisma.ts est valide TypeScript)
- Les scripts `db:generate`, `db:push`, `db:migrate`, `db:studio`, `db:reset` sont dans package.json
- `postinstall` inclut `prisma generate` pour les deployments
  </done>
</task>

</tasks>

<verification>
Verification globale du plan 02:

```bash
cd C:/Users/LENOVO/tawa-services

# Valider le schema
npx prisma validate && echo "SCHEMA VALID"

# Verifier la generation du client
npx prisma generate && echo "CLIENT GENERATED"

# Typecheck complet
npm run typecheck && echo "TYPECHECK OK"

# Compter les modeles (doit etre >= 20)
echo "Models count: $(grep -c '^model ' prisma/schema.prisma)"

# Verifier soft delete present
echo "Soft delete fields:"
grep -n "isDeleted" prisma/schema.prisma | wc -l
```
</verification>

<success_criteria>
Acceptance criteria:
1. `npx prisma validate` retourne "The schema is valid"
2. `npx prisma generate` complete sans erreur TypeScript
3. Le schema contient tous les modeles: User, Account, Session, Provider, Category, Service, Booking, Quote, Payment, Review, Conversation, Message, Notification, Gouvernorat, Delegation, et les tables junction/auxiliaires
4. Tous les modeles principaux ont `isDeleted` et `deletedAt` (soft delete)
5. `Category` a la hierarchie parent-enfant (`parentId` self-relation)
6. `ProviderDelegation` est la junction table pour la zone d'intervention prestataire
7. `src/lib/prisma.ts` exporte le singleton `prisma` avec logging dev/prod adapte
8. `npm run typecheck` passe apres ajout de `src/lib/prisma.ts`
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md` avec:
- Liste complete des modeles crees dans le schema
- Enumerations definies
- Decisions de schema prises (ex: pourquoi Conversation est separee de Booking)
- Commandes DB disponibles dans package.json
- Note sur l'etat de la migration (appliquee ou db push)
</output>
