---
phase: 01-foundation-infrastructure
plan: 05
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-03"
  - "01-04"
files_modified:
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/(client)/layout.tsx
  - src/app/[locale]/(provider)/layout.tsx
  - src/app/[locale]/(admin)/layout.tsx
  - src/components/layout/Navbar.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/BottomNav.tsx
  - src/components/layout/AdminSidebar.tsx
  - src/components/layout/ProviderSidebar.tsx
autonomous: false
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "La Navbar desktop est visible sur >= 768px avec: Logo Tawa Services, dropdown categories (icones lucide-react), barre de recherche, boutons auth et ThemeToggle"
    - "Le Footer multi-colonnes est visible sur toutes les pages client et provider (cache sur admin)"
    - "La BottomNav mobile (5 onglets) est visible sur < 768px avec les icones lucide-react corrects"
    - "Les 3 layouts sont implementes avec les route groups: (client), (provider), (admin)"
    - "L'AdminSidebar est collapsible (etat ouvert/ferme) avec la navigation admin complete"
    - "Le layout admin n'affiche pas le Footer ni la BottomNav"
    - "Les tabs BottomNav s'adaptent au role: CLIENT voit Home/Search/Bookings/Messages/Profile, PROVIDER voit Dashboard/Services/Bookings/Messages/Profile"
  artifacts:
    - path: "src/components/layout/Navbar.tsx"
      provides: "Navbar desktop avec categories dropdown, recherche, auth, theme toggle"
      min_lines: 80
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer multi-colonnes avec 5 sections"
      min_lines: 60
    - path: "src/components/layout/BottomNav.tsx"
      provides: "Bottom navigation mobile 5 onglets avec adaptation role"
      min_lines: 60
    - path: "src/components/layout/AdminSidebar.tsx"
      provides: "Sidebar admin collapsible avec navigation complete"
      min_lines: 80
    - path: "src/app/[locale]/(client)/layout.tsx"
      provides: "Layout client: Navbar + Footer + BottomNav"
    - path: "src/app/[locale]/(provider)/layout.tsx"
      provides: "Layout provider: Navbar + Footer + BottomNav (adaptation role)"
    - path: "src/app/[locale]/(admin)/layout.tsx"
      provides: "Layout admin: AdminSidebar sans Footer ni BottomNav"
  key_links:
    - from: "src/components/layout/BottomNav.tsx"
      to: "src/app/[locale]/(client)/layout.tsx"
      via: "Import et rendu conditionnel mobile"
      pattern: "BottomNav"
    - from: "src/components/layout/AdminSidebar.tsx"
      to: "src/app/[locale]/(admin)/layout.tsx"
      via: "Import dans layout admin"
      pattern: "AdminSidebar"
    - from: "src/components/layout/Navbar.tsx"
      to: "src/i18n/routing.ts"
      via: "Link/usePathname from createNavigation helpers"
      pattern: "from.*@/i18n/routing"
    - from: "src/components/layout/Footer.tsx"
      to: "src/messages/fr.json"
      via: "useTranslations('footer') namespace"
      pattern: "useTranslations.*footer"
---

<objective>
Implementer les 3 layouts distincts de Tawa Services (client, provider, admin) avec leurs composants de navigation respectifs: Navbar desktop, BottomNav mobile 5-onglets, Footer multi-colonnes et AdminSidebar collapsible — tous utilisant les tokens de design etablis et les traductions next-intl.

User story: En tant qu'utilisateur de Tawa Services, je veux une navigation claire et adaptee a mon role (client, prestataire, admin), afin de pouvoir acceder rapidement aux fonctionnalites pertinentes depuis n'importe quel appareil (mobile ou desktop).

Purpose: Les layouts sont la structure visuelle dans laquelle toutes les features des sprints suivants s'inscriront. Un layout bien fait rend les pages des sprints 2-10 immediatement presentables sans retouche.
Output: 3 layouts distincts avec composants de navigation complets, mobile-first, i18n, design tokens appliques.
</objective>

<execution_context>
@C:/Users/LENOVO/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-03-SUMMARY.md
@.planning/phases/01-foundation-infrastructure/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer la Navbar desktop, le Footer et la BottomNav mobile</name>
  <files>
    src/components/layout/Navbar.tsx
    src/components/layout/Footer.tsx
    src/components/layout/BottomNav.tsx
  </files>
  <action>
Creer `src/components/layout/Navbar.tsx` — navigation desktop (visible >= 768px).
IMPORTANT: Utiliser `Link` de `@/i18n/routing` (helper createNavigation — prefixe locale automatique). Les hrefs sont sans prefixe `/fr/` (ex: `href="/services"` pas `href="/fr/services"`). Le placeholder de recherche utilise `t('key')` via namespace "navigation".

```tsx
"use client";

import { useTranslations } from "next-intl";
import { Search, Menu, Bell, ChevronDown } from "lucide-react";
import { Button } from "@/components/ui/button";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuTrigger,
  DropdownMenuItem,
} from "@/components/ui/dropdown-menu";
import { ThemeToggle } from "@/components/shared/ThemeToggle";
import { LocaleSwitcher } from "@/components/shared/LocaleSwitcher";
import { Input } from "@/components/ui/input";
import { Link } from "@/i18n/routing";

// Categories placeholder — sera remplace par donnees DB en Phase 5.
// Les labels proviennent de fr.json sous le namespace "categories".
const CATEGORIES = [
  { slug: "plomberie", icon: "🔧", labelKey: "plomberie" },
  { slug: "electricite", icon: "⚡", labelKey: "electricite" },
  { slug: "menage", icon: "🧹", labelKey: "menage" },
  { slug: "cours", icon: "📚", labelKey: "cours" },
  { slug: "jardinage", icon: "🌱", labelKey: "jardinage" },
  { slug: "peinture", icon: "🎨", labelKey: "peinture" },
];

export function Navbar() {
  const t = useTranslations("navigation");
  const tAuth = useTranslations("auth");
  const tCommon = useTranslations("common");
  // Labels categories depuis fr.json["categories"] — aucun label hardcode
  const tCat = useTranslations("categories");

  return (
    <header className="sticky top-0 z-50 hidden w-full border-b bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60 md:block">
      <div className="container mx-auto flex h-16 max-w-7xl items-center justify-between px-4">
        {/* Logo */}
        <Link href="/" className="flex items-center gap-2">
          <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-primary">
            <span className="text-sm font-bold text-primary-foreground">T</span>
          </div>
          <span className="text-lg font-bold text-primary">Tawa Services</span>
        </Link>

        {/* Categories Dropdown */}
        <DropdownMenu>
          <DropdownMenuTrigger asChild>
            <Button variant="ghost" className="flex items-center gap-1">
              <Menu className="h-4 w-4" />
              <span>{t("categories")}</span>
              <ChevronDown className="h-4 w-4" />
            </Button>
          </DropdownMenuTrigger>
          <DropdownMenuContent className="w-56">
            {CATEGORIES.map((cat) => (
              <DropdownMenuItem key={cat.slug} asChild>
                <Link href={`/services/${cat.slug}` as never} className="flex items-center gap-2">
                  <span>{cat.icon}</span>
                  <span>{tCat(cat.labelKey)}</span>
                </Link>
              </DropdownMenuItem>
            ))}
          </DropdownMenuContent>
        </DropdownMenu>

        {/* Search Bar */}
        <div className="hidden w-72 md:block lg:w-96">
          <div className="relative">
            <Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" />
            <Input
              placeholder={tCommon("search")}
              className="pl-9 rounded-full"
            />
          </div>
        </div>

        {/* Right Actions */}
        <div className="flex items-center gap-2">
          <LocaleSwitcher />
          <ThemeToggle />
          {/* Notifications placeholder — Phase 9 */}
          <Button variant="ghost" size="icon" aria-label="Notifications">
            <Bell className="h-5 w-5" />
          </Button>
          {/* Auth placeholder — Phase 2 */}
          <Button variant="ghost" asChild>
            <Link href="/auth/login">{tAuth("login")}</Link>
          </Button>
          <Button asChild className="rounded-full">
            <Link href="/auth/register">{tAuth("register")}</Link>
          </Button>
        </div>
      </div>
    </header>
  );
}
```

Creer `src/components/layout/Footer.tsx` — footer multi-colonnes.
IMPORTANT: Toutes les chaines visibles utilisent `t('key')` depuis le namespace "footer" (defini dans src/messages/fr.json en plan 01-03). Les liens utilisent `Link` de `@/i18n/routing` (helper createNavigation) pour le prefixe locale automatique — PAS `Link` de `next/link` ni href hardcode avec `/fr/`.

```tsx
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/routing";

export function Footer() {
  const t = useTranslations("footer");

  return (
    <footer className="hidden border-t bg-muted/30 md:block">
      <div className="container mx-auto max-w-7xl px-4 py-12">
        <div className="grid grid-cols-2 gap-8 md:grid-cols-5">
          {/* Brand */}
          <div className="col-span-2 md:col-span-1">
            <div className="flex items-center gap-2">
              <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-primary">
                <span className="text-sm font-bold text-primary-foreground">T</span>
              </div>
              <span className="font-bold text-primary">Tawa Services</span>
            </div>
            <p className="mt-3 text-sm text-muted-foreground">
              {t("tagline")}
            </p>
          </div>

          {/* Pour les clients */}
          <div>
            <h3 className="mb-4 text-sm font-semibold">{t("forClients")}</h3>
            <ul className="space-y-2 text-sm text-muted-foreground">
              <li><Link href="/services" className="hover:text-foreground transition-colors">{t("findProvider")}</Link></li>
              <li><Link href="/how-it-works" className="hover:text-foreground transition-colors">{t("howItWorks")}</Link></li>
              <li><Link href="/categories" className="hover:text-foreground transition-colors">{t("serviceCategories")}</Link></li>
            </ul>
          </div>

          {/* Pour les prestataires */}
          <div>
            <h3 className="mb-4 text-sm font-semibold">{t("forProviders")}</h3>
            <ul className="space-y-2 text-sm text-muted-foreground">
              <li><Link href="/become-provider" className="hover:text-foreground transition-colors">{t("becomeProvider")}</Link></li>
              <li><Link href="/provider/dashboard" className="hover:text-foreground transition-colors">{t("dashboard")}</Link></li>
              <li><Link href="/provider/kyc" className="hover:text-foreground transition-colors">{t("kycVerification")}</Link></li>
            </ul>
          </div>

          {/* Aide */}
          <div>
            <h3 className="mb-4 text-sm font-semibold">{t("help")}</h3>
            <ul className="space-y-2 text-sm text-muted-foreground">
              <li><Link href="/faq" className="hover:text-foreground transition-colors">{t("faq")}</Link></li>
              <li><Link href="/contact" className="hover:text-foreground transition-colors">{t("contact")}</Link></li>
              <li><Link href="/support" className="hover:text-foreground transition-colors">{t("support")}</Link></li>
            </ul>
          </div>

          {/* Legal */}
          <div>
            <h3 className="mb-4 text-sm font-semibold">{t("legal")}</h3>
            <ul className="space-y-2 text-sm text-muted-foreground">
              <li><Link href="/terms" className="hover:text-foreground transition-colors">{t("terms")}</Link></li>
              <li><Link href="/privacy" className="hover:text-foreground transition-colors">{t("privacy")}</Link></li>
            </ul>
          </div>
        </div>

        <div className="mt-10 border-t pt-6 text-center text-sm text-muted-foreground">
          <p>&copy; {new Date().getFullYear()} Tawa Services. {t("copyright")}</p>
        </div>
      </div>
    </footer>
  );
}
```

Creer `src/components/layout/BottomNav.tsx` — navigation mobile (visible < 768px).
IMPORTANT: Utiliser `Link` et `usePathname` de `@/i18n/routing` (helpers createNavigation). Les hrefs sont sans prefixe `/fr/`.

```tsx
"use client";

import { useTranslations } from "next-intl";
import { Home, Search, Calendar, MessageSquare, User, LayoutDashboard, Briefcase } from "lucide-react";
import { cn } from "@/lib/utils";
import { Link, usePathname } from "@/i18n/routing";

type NavItem = {
  href: string;
  icon: React.ElementType;
  labelKey: string;
};

// Navigation client — hrefs sans prefixe locale (gere par createNavigation)
const CLIENT_ITEMS: NavItem[] = [
  { href: "/", icon: Home, labelKey: "home" },
  { href: "/services", icon: Search, labelKey: "search" },
  { href: "/bookings", icon: Calendar, labelKey: "bookings" },
  { href: "/messages", icon: MessageSquare, labelKey: "messages" },
  { href: "/profile", icon: User, labelKey: "profile" },
];

// Navigation provider
const PROVIDER_ITEMS: NavItem[] = [
  { href: "/provider/dashboard", icon: LayoutDashboard, labelKey: "dashboard" },
  { href: "/provider/services", icon: Briefcase, labelKey: "services" },
  { href: "/provider/bookings", icon: Calendar, labelKey: "bookings" },
  { href: "/messages", icon: MessageSquare, labelKey: "messages" },
  { href: "/profile", icon: User, labelKey: "profile" },
];

interface BottomNavProps {
  role?: "CLIENT" | "PROVIDER";
}

export function BottomNav({ role = "CLIENT" }: BottomNavProps) {
  const pathname = usePathname();
  const t = useTranslations("navigation");
  const items = role === "PROVIDER" ? PROVIDER_ITEMS : CLIENT_ITEMS;

  return (
    <nav className="fixed bottom-0 left-0 right-0 z-50 border-t bg-background md:hidden">
      <div className="flex h-16 items-center justify-around px-2">
        {items.map((item) => {
          const Icon = item.icon;
          const isActive = pathname === item.href || pathname.startsWith(item.href + "/");

          return (
            <Link
              key={item.href}
              href={item.href as never}
              className={cn(
                "flex flex-1 flex-col items-center justify-center gap-1 py-2 text-xs transition-colors",
                isActive
                  ? "text-primary"
                  : "text-muted-foreground hover:text-foreground"
              )}
            >
              <Icon className={cn("h-5 w-5", isActive && "stroke-[2.5px]")} />
              <span className={cn(isActive && "font-medium")}>{t(item.labelKey)}</span>
            </Link>
          );
        })}
      </div>
    </nav>
  );
}
```
  </action>
  <verify>
```bash
cd C:/Users/LENOVO/tawa-services

# Typecheck
npm run typecheck

# Lint
npm run lint

# Verifier l'import de tous les composants
node -e "
const fs = require('fs');
['Navbar', 'Footer', 'BottomNav'].forEach(c => {
  const exists = fs.existsSync('src/components/layout/' + c + '.tsx');
  console.log(c + ': ' + (exists ? 'OK' : 'MISSING'));
});
"
```
  </verify>
  <done>
- `src/components/layout/Navbar.tsx` existe avec logo, dropdown categories, searchbar, auth buttons, ThemeToggle
- `src/components/layout/Footer.tsx` existe avec 5 colonnes (Brand, Clients, Prestataires, Aide, Legal) et utilise `useTranslations("footer")` pour toutes les chaines visibles
- `src/components/layout/BottomNav.tsx` existe avec 5 onglets adaptes au role (CLIENT/PROVIDER)
- `Navbar` est `hidden` par defaut et `md:block` (visible desktop seulement)
- `Footer` est `hidden` par defaut et `md:block` (visible desktop seulement)
- `BottomNav` est fixe en bas et `md:hidden` (visible mobile seulement)
- Tous les composants importent `Link`, `usePathname` depuis `@/i18n/routing` — AUCUN href hardcode avec `/fr/`
- Tous les composants utilisent `useTranslations()` pour les chaines — aucune chaine hardcodee en francais
- `npm run typecheck` passe sans erreur
  </done>
</task>

<task type="auto">
  <name>Task 2: Creer les 3 layouts distincts et l'AdminSidebar collapsible</name>
  <files>
    src/app/[locale]/(client)/layout.tsx
    src/app/[locale]/(provider)/layout.tsx
    src/app/[locale]/(admin)/layout.tsx
    src/components/layout/AdminSidebar.tsx
  </files>
  <action>
Creer les route groups et leurs layouts. S'assurer que la structure de dossiers existe:
```bash
mkdir -p src/app/[locale]/(client)
mkdir -p src/app/[locale]/(provider)
mkdir -p src/app/[locale]/(admin)
```

Creer `src/app/[locale]/(client)/layout.tsx`:
```tsx
import { Navbar } from "@/components/layout/Navbar";
import { Footer } from "@/components/layout/Footer";
import { BottomNav } from "@/components/layout/BottomNav";

export default function ClientLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      <Navbar />
      <main className="min-h-[calc(100vh-4rem)] pb-16 md:pb-0">
        {children}
      </main>
      <Footer />
      <BottomNav role="CLIENT" />
    </>
  );
}
```

Creer `src/app/[locale]/(provider)/layout.tsx`:
```tsx
import { Navbar } from "@/components/layout/Navbar";
import { Footer } from "@/components/layout/Footer";
import { BottomNav } from "@/components/layout/BottomNav";

export default function ProviderLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      <Navbar />
      <main className="min-h-[calc(100vh-4rem)] pb-16 md:pb-0">
        {children}
      </main>
      <Footer />
      <BottomNav role="PROVIDER" />
    </>
  );
}
```

Creer `src/components/layout/AdminSidebar.tsx` — sidebar admin collapsible.
IMPORTANT: Utiliser `Link` et `usePathname` de `@/i18n/routing`. Toutes les chaines visibles utilisent `useTranslations("layout")` — aucune chaine hardcodee.

```tsx
"use client";

import { useState } from "react";
import { useTranslations } from "next-intl";
import { Link, usePathname } from "@/i18n/routing";
import {
  LayoutDashboard,
  Users,
  ShieldCheck,
  Grid2X2,
  Flag,
  BarChart3,
  FileText,
  Bell,
  ChevronLeft,
  ChevronRight,
} from "lucide-react";
import { cn } from "@/lib/utils";
import { Button } from "@/components/ui/button";
import { Separator } from "@/components/ui/separator";

const NAV_ITEMS = [
  { href: "/admin", icon: LayoutDashboard, labelKey: "home" },
  { href: "/admin/users", icon: Users, labelKey: "users" },
  { href: "/admin/kyc", icon: ShieldCheck, labelKey: "kyc" },
  { href: "/admin/categories", icon: Grid2X2, labelKey: "categories" },
  { href: "/admin/reports", icon: Flag, labelKey: "reports" },
  { href: "/admin/analytics", icon: BarChart3, labelKey: "analytics" },
  { href: "/admin/content", icon: FileText, labelKey: "content" },
  { href: "/admin/notifications", icon: Bell, labelKey: "notifications" },
];

export function AdminSidebar() {
  const [collapsed, setCollapsed] = useState(false);
  const pathname = usePathname();
  const t = useTranslations("navigation");
  // "layout" namespace pour les labels generiques de la sidebar (collapse, expand, aria-labels)
  const tLayout = useTranslations("layout");

  return (
    <aside
      className={cn(
        "relative flex h-screen flex-col border-r bg-background transition-all duration-300",
        collapsed ? "w-16" : "w-64"
      )}
    >
      {/* Logo */}
      <div className={cn("flex h-16 items-center border-b px-4", collapsed && "justify-center")}>
        {!collapsed && (
          <Link href="/admin" className="flex items-center gap-2">
            <div className="flex h-7 w-7 items-center justify-center rounded-lg bg-primary">
              <span className="text-xs font-bold text-primary-foreground">T</span>
            </div>
            <span className="font-semibold text-primary">Admin</span>
          </Link>
        )}
        {collapsed && (
          <Link href="/admin">
            <div className="flex h-7 w-7 items-center justify-center rounded-lg bg-primary">
              <span className="text-xs font-bold text-primary-foreground">T</span>
            </div>
          </Link>
        )}
      </div>

      {/* Navigation */}
      <nav className="flex-1 space-y-1 p-3" aria-label={tLayout("sidebarNav")}>
        {NAV_ITEMS.map((item) => {
          const Icon = item.icon;
          const isActive = pathname === item.href || pathname.startsWith(item.href + "/");

          return (
            <Link
              key={item.href}
              href={item.href}
              className={cn(
                "flex items-center gap-3 rounded-lg px-3 py-2 text-sm transition-colors",
                isActive
                  ? "bg-primary/10 text-primary font-medium"
                  : "text-muted-foreground hover:bg-muted hover:text-foreground",
                collapsed && "justify-center px-2"
              )}
              title={collapsed ? t(item.labelKey) : undefined}
            >
              <Icon className="h-4 w-4 flex-shrink-0" />
              {!collapsed && <span>{t(item.labelKey)}</span>}
            </Link>
          );
        })}
      </nav>

      <Separator />

      {/* Collapse Toggle */}
      <div className="p-3">
        <Button
          variant="ghost"
          size="sm"
          onClick={() => setCollapsed(!collapsed)}
          className={cn("w-full", collapsed && "px-2")}
          aria-label={collapsed ? tLayout("sidebarExpand") : tLayout("sidebarCollapse")}
        >
          {collapsed ? (
            <ChevronRight className="h-4 w-4" />
          ) : (
            <>
              <ChevronLeft className="h-4 w-4 mr-2" />
              <span>{tLayout("sidebarCollapse")}</span>
            </>
          )}
        </Button>
      </div>
    </aside>
  );
}
```

Creer `src/app/[locale]/(admin)/layout.tsx` — layout admin sans Footer ni BottomNav.
IMPORTANT: Le header utilise `getTranslations("layout")` (Server Component) — aucune chaine hardcodee.

```tsx
import { getTranslations } from "next-intl/server";
import { AdminSidebar } from "@/components/layout/AdminSidebar";

export default async function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const t = await getTranslations("layout");

  return (
    <div className="flex h-screen overflow-hidden">
      <AdminSidebar />
      <div className="flex flex-1 flex-col overflow-hidden">
        {/* Admin header */}
        <header className="flex h-16 items-center justify-between border-b bg-background px-6">
          <h1 className="text-lg font-semibold">{t("adminPanelTitle")}</h1>
          {/* Admin user actions — Phase 2 */}
        </header>
        <main className="flex-1 overflow-y-auto bg-muted/20 p-6">
          {children}
        </main>
      </div>
    </div>
  );
}
```

Mettre a jour `src/app/[locale]/layout.tsx` pour s'assurer que les route groups fonctionnent: le layout racine localise ne doit PAS inclure Navbar/Footer (c'est la responsabilite des layouts enfants). Si ce fichier a ete cree en plan 03 avec le contenu du ThemeProvider, s'assurer qu'il ne contient pas de Navbar ou Footer.
  </action>
  <verify>
```bash
cd C:/Users/LENOVO/tawa-services

# Verifier la structure des route groups
ls src/app/[locale]/
# Doit afficher: (client)/ (provider)/ (admin)/ layout.tsx page.tsx

ls src/app/[locale]/\(client\)/
ls src/app/[locale]/\(provider\)/
ls src/app/[locale]/\(admin\)/

# Typecheck
npm run typecheck

# Lint
npm run lint
```
  </verify>
  <done>
- `src/app/[locale]/(client)/layout.tsx` inclut Navbar + Footer + BottomNav(CLIENT)
- `src/app/[locale]/(provider)/layout.tsx` inclut Navbar + Footer + BottomNav(PROVIDER)
- `src/app/[locale]/(admin)/layout.tsx` inclut uniquement AdminSidebar (pas Footer, pas BottomNav)
- `src/components/layout/AdminSidebar.tsx` est collapsible (useState), affiche les 8 items de navigation admin; utilise `useTranslations("layout")` pour `sidebarCollapse`, `sidebarExpand`, `sidebarNav` — aucune chaine hardcodee
- `src/app/[locale]/(admin)/layout.tsx` est un Server Component async qui utilise `getTranslations("layout")` pour `adminPanelTitle` — aucune chaine hardcodee
- L'AdminSidebar est en `w-64` ouvert et `w-16` collapsed avec les labels caches
- `npm run typecheck` et `npm run lint` passent sans erreur
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verification visuelle des 3 layouts et de la navigation</name>
  <action>Verification visuelle par l'utilisateur des layouts desktop et mobile, de l'AdminSidebar collapsible et du ThemeToggle.</action>
  <what-built>
    Les 3 layouts complets de Tawa Services:
    - Layout CLIENT: Navbar desktop (logo, categories dropdown, searchbar, auth, ThemeToggle) + Footer multi-colonnes + BottomNav 5-onglets mobile
    - Layout PROVIDER: Idem client mais BottomNav adapte (Dashboard, Services, Bookings, Messages, Profile)
    - Layout ADMIN: AdminSidebar collapsible 8 items + header admin — sans Footer ni BottomNav
  </what-built>
  <how-to-verify>
    1. Demarrer le serveur: `npm run dev` dans `C:/Users/LENOVO/tawa-services`
    2. Ouvrir http://localhost:3000/fr sur desktop (>= 768px)
       - Verifier: Navbar visible en haut (logo "T Tawa Services", menu categories, barre recherche, boutons Login/Register, theme toggle)
       - Verifier: Footer visible en bas (5 colonnes)
       - Verifier: PAS de BottomNav visible
    3. Redimensionner la fenetre a < 768px (ou DevTools mobile)
       - Verifier: Navbar cachee (hidden)
       - Verifier: BottomNav visible en bas (5 onglets avec icones)
       - Verifier: Footer cache (hidden sur mobile)
    4. Naviguer vers http://localhost:3000/fr/admin (si la page existe, sinon creer une page placeholder)
       - Verifier: AdminSidebar visible a gauche (logo, 8 items de navigation)
       - Verifier: Bouton "Reduire" en bas de la sidebar
       - Cliquer "Reduire" — la sidebar doit passer en mode compact (icones seulement, w-16)
       - Verifier: PAS de Footer ni BottomNav dans le layout admin
    5. Cliquer le ThemeToggle dans la Navbar — le theme dark/light doit basculer
  </how-to-verify>
  <resume-signal>
    Tapez "approved" si tous les points de verification passent.
    Ou decrivez les problemes visuels observes (ex: "BottomNav visible sur desktop", "AdminSidebar pas collapsible").
  </resume-signal>
</task>

</tasks>

<verification>
Verification finale apres approbation humaine:

```bash
cd C:/Users/LENOVO/tawa-services

# Build final
npm run build && echo "BUILD OK"

# Typecheck
npm run typecheck && echo "TYPECHECK OK"

# Lint
npm run lint && echo "LINT OK"
```
</verification>

<success_criteria>
Acceptance criteria:
1. Sur desktop (>= 768px): Navbar visible avec logo, categories dropdown, searchbar, buttons auth, ThemeToggle
2. Sur mobile (< 768px): BottomNav visible avec 5 onglets (Home/Search/Bookings/Messages/Profile pour client)
3. Le Footer multi-colonnes est visible sur desktop, cache sur mobile ET cache sur le layout admin
4. L'AdminSidebar est collapsible (ouvert: w-64 avec labels, ferme: w-16 avec icones uniquement)
5. Les 3 route groups (client), (provider), (admin) ont leurs layouts distincts
6. Le ThemeToggle change correctement entre light et dark mode
7. Tous les composants utilisent `useTranslations()` — aucune chaine hardcodee en francais dans le code source (y compris AdminSidebar "Reduire"/"Ouvrir", AdminLayout header titre, Navbar category labels)
8. `Link`, `usePathname` de `@/i18n/routing` sont utilises dans tous les composants de navigation — aucun href hardcode avec `/fr/`
9. `Footer.tsx` utilise `useTranslations("footer")` pour le tagline, les titres de colonnes et les labels de liens
10. `Navbar.tsx` utilise `useTranslations("categories")` pour les labels de categories — CATEGORY_LABELS inline supprime
11. `AdminSidebar.tsx` utilise `useTranslations("layout")` pour `sidebarCollapse`, `sidebarExpand`, `sidebarNav`
12. `AdminLayout` (`(admin)/layout.tsx`) utilise `getTranslations("layout")` pour `adminPanelTitle`
13. `npm run build` produit un build valide (exit 0)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-foundation-infrastructure/01-05-SUMMARY.md` avec:
- Structure des route groups implementee
- Composants de layout crees (Navbar, Footer, BottomNav, AdminSidebar)
- Breakpoints utilises (md: 768px)
- Navigation items de chaque layout
- Note: les routes comme /fr/admin/* sont des placeholders — les pages reelles sont en Phase 10
</output>
