---
phase: 01-foundation-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - next.config.ts
  - middleware.ts
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - src/messages/fr.json
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/components/shared/LocaleSwitcher.tsx
  - package.json
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "La page d'accueil est accessible via http://localhost:3000 et redirige automatiquement vers /fr"
    - "Toutes les chaines UI de la page d'accueil utilisent `t('key')` — aucune chaine hardcodee en francais visible dans le code source"
    - "Le middleware next-intl intercepte les requetes et ajoute le prefixe locale manquant"
    - "Le fichier `src/messages/fr.json` contient les cles de base (navigation, common, home) en francais"
    - "Le composant LocaleSwitcher est cree et fonctionnel (pret pour ajout de locales AR/EN)"
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "Configuration des locales supportees et locale par defaut"
      contains: "fr"
    - path: "src/i18n/request.ts"
      provides: "Configuration de la requete i18n pour next-intl"
    - path: "src/messages/fr.json"
      provides: "Dictionnaire francais avec toutes les cles de base"
      min_lines: 50
    - path: "middleware.ts"
      provides: "Middleware next-intl pour le routage par locale"
    - path: "src/app/[locale]/layout.tsx"
      provides: "Layout racine avec NextIntlClientProvider"
    - path: "src/components/shared/LocaleSwitcher.tsx"
      provides: "Composant selecteur de langue"
  key_links:
    - from: "middleware.ts"
      to: "src/i18n/routing.ts"
      via: "routing config import"
      pattern: "routing"
    - from: "src/components/**"
      to: "src/i18n/routing.ts"
      via: "Link/useRouter/redirect from createNavigation"
      pattern: "from.*@/i18n/routing"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/messages/fr.json"
      via: "NextIntlClientProvider messages"
      pattern: "NextIntlClientProvider"
    - from: "src/app/[locale]/page.tsx"
      to: "src/messages/fr.json"
      via: "useTranslations hook"
      pattern: "useTranslations"
---

<objective>
Configurer next-intl avec le middleware de routage par locale, les dictionnaires francais, et le pattern `t('key')` global — posant l'infrastructure i18n complete qui sera utilisee par toutes les features des sprints suivants.

User story: En tant que developpeur, je veux une infrastructure i18n complete avec next-intl depuis le premier jour, afin que chaque chaine UI soit automatiquement internationalisable sans refactoring futur (pret pour l'ajout de l'arabe et l'anglais).

Purpose: Si l'i18n n'est pas configure des le debut, chaque string hardcodee devra etre extraite plus tard — un refactoring penible sur 50+ composants. Le faire maintenant a cout quasi nul.
Output: Routage par locale (/fr/*), middleware next-intl, dictionnaire fr.json avec cles de base, NextIntlClientProvider dans le layout racine, composant LocaleSwitcher pret.
</objective>

<execution_context>
@C:/Users/LENOVO/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer next-intl et configurer le routage par locale</name>
  <files>
    package.json
    next.config.ts
    middleware.ts
    src/i18n/routing.ts
    src/i18n/request.ts
    src/messages/fr.json
  </files>
  <action>
Installer next-intl:
```bash
cd C:/Users/LENOVO/tawa-services
npm install next-intl
```

Creer `src/i18n/routing.ts` — configuration des locales ET helpers de navigation:
```ts
import { defineRouting } from "next-intl/routing";
import { createNavigation } from "next-intl/navigation";

export const routing = defineRouting({
  locales: ["fr"],
  defaultLocale: "fr",
  // AR et EN seront ajoutes en Phase 11 (v2 deferred)
  // locales: ["fr", "ar", "en"],
});

// Navigation helpers type-safe qui gerent le prefixe locale automatiquement.
// Utiliser ces exports PARTOUT dans l'application a la place de next/navigation:
//   import { Link, useRouter, redirect } from "@/i18n/routing";
export const { Link, useRouter, redirect, usePathname } = createNavigation(routing);
```

Creer `src/i18n/request.ts` — configuration de la requete:
```ts
import { getRequestConfig } from "next-intl/server";
import { routing } from "./routing";

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;

  // Valider la locale ou utiliser la locale par defaut
  if (!locale || !routing.locales.includes(locale as "fr")) {
    locale = routing.defaultLocale;
  }

  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  };
});
```

Mettre a jour `next.config.ts` pour integrer next-intl:
```ts
import type { NextConfig } from "next";
import createNextIntlPlugin from "next-intl/plugin";

const withNextIntl = createNextIntlPlugin("./src/i18n/request.ts");

const nextConfig: NextConfig = {
  experimental: {
    typedRoutes: true,
  },
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "**" },
    ],
  },
};

export default withNextIntl(nextConfig);
```

Creer `middleware.ts` a la racine du projet (pas dans src/):
```ts
import createMiddleware from "next-intl/middleware";
import { routing } from "./src/i18n/routing";

export default createMiddleware(routing);

export const config = {
  // Matcher qui exclut les fichiers statiques et API routes
  matcher: [
    "/((?!api|_next/static|_next/image|favicon.ico|robots.txt|sitemap.xml).*)",
  ],
};
```

Creer `src/messages/fr.json` avec les cles de base couvrant navigation, home, common, et les entites metier:
```json
{
  "common": {
    "loading": "Chargement...",
    "error": "Une erreur est survenue",
    "retry": "Reessayer",
    "save": "Enregistrer",
    "cancel": "Annuler",
    "confirm": "Confirmer",
    "delete": "Supprimer",
    "edit": "Modifier",
    "view": "Voir",
    "back": "Retour",
    "next": "Suivant",
    "previous": "Precedent",
    "search": "Rechercher",
    "filter": "Filtrer",
    "sort": "Trier",
    "all": "Tous",
    "yes": "Oui",
    "no": "Non",
    "or": "ou",
    "and": "et",
    "from": "De",
    "to": "A",
    "by": "Par",
    "on": "Le",
    "at": "A",
    "required": "Requis",
    "optional": "Optionnel"
  },
  "navigation": {
    "home": "Accueil",
    "search": "Rechercher",
    "bookings": "Reservations",
    "messages": "Messages",
    "profile": "Profil",
    "dashboard": "Tableau de bord",
    "services": "Services",
    "earnings": "Revenus",
    "settings": "Parametres",
    "admin": "Administration",
    "users": "Utilisateurs",
    "categories": "Categories",
    "reports": "Signalements",
    "analytics": "Analytique"
  },
  "home": {
    "title": "Tawa Services",
    "subtitle": "La plateforme de services locaux en Tunisie",
    "heroTitle": "Trouvez le bon prestataire pres de chez vous",
    "heroSubtitle": "Plombiers, electriciens, menage, cours particuliers et bien plus — verifies et disponibles dans votre ville",
    "searchPlaceholder": "Quel service cherchez-vous ?",
    "searchButton": "Rechercher",
    "featuredCategories": "Categories populaires",
    "howItWorks": "Comment ca marche",
    "step1Title": "Recherchez",
    "step1Description": "Parcourez les prestataires par categorie ou recherchez directement",
    "step2Title": "Reservez",
    "step2Description": "Choisissez un creneau et confirmez votre reservation en 3 etapes",
    "step3Title": "Payez & Evaluez",
    "step3Description": "Payez en securite et laissez votre avis apres le service",
    "becomeProvider": "Devenir prestataire"
  },
  "auth": {
    "login": "Se connecter",
    "logout": "Se deconnecter",
    "register": "S'inscrire",
    "email": "Email",
    "password": "Mot de passe",
    "confirmPassword": "Confirmer le mot de passe",
    "forgotPassword": "Mot de passe oublie ?",
    "resetPassword": "Reinitialiser le mot de passe",
    "phone": "Numero de telephone",
    "name": "Nom complet",
    "role": "Je suis",
    "roleClient": "Client — je cherche des prestataires",
    "roleProvider": "Prestataire — je propose des services",
    "alreadyHaveAccount": "Deja un compte ?",
    "noAccount": "Pas encore de compte ?",
    "verifyEmail": "Verifier votre email",
    "emailVerificationSent": "Un email de verification a ete envoye a {email}",
    "emailVerified": "Email verifie avec succes !"
  },
  "provider": {
    "profile": "Profil prestataire",
    "services": "Mes services",
    "availability": "Mes disponibilites",
    "kycStatus": "Verification d'identite",
    "kycPending": "En cours de verification",
    "kycApproved": "Identite verifiee",
    "kycRejected": "Verification rejetee",
    "kycNotSubmitted": "Non soumise",
    "trustBadges": "Badges de confiance",
    "rating": "Note",
    "reviews": "Avis",
    "completedMissions": "Missions terminees",
    "responseTime": "Temps de reponse"
  },
  "service": {
    "book": "Reserver",
    "requestQuote": "Demander un devis",
    "fixedPrice": "Prix fixe",
    "onQuote": "Sur devis",
    "duration": "Duree",
    "category": "Categorie",
    "inclusions": "Ce qui est inclus",
    "exclusions": "Ce qui n'est pas inclus",
    "conditions": "Conditions particulieres"
  },
  "booking": {
    "status": {
      "PENDING": "En attente",
      "ACCEPTED": "Acceptee",
      "IN_PROGRESS": "En cours",
      "COMPLETED": "Terminee",
      "REJECTED": "Rejetee",
      "CANCELLED": "Annulee"
    },
    "myBookings": "Mes reservations",
    "newBooking": "Nouvelle reservation",
    "cancelBooking": "Annuler la reservation",
    "confirmBooking": "Confirmer la reservation"
  },
  "payment": {
    "method": {
      "CARD": "Carte bancaire",
      "D17": "D17",
      "FLOUCI": "Flouci",
      "CASH": "Especes"
    },
    "status": {
      "PENDING": "En attente",
      "HELD": "Retenu (escrow)",
      "RELEASED": "Libere",
      "REFUNDED": "Rembourse",
      "FAILED": "Echoue"
    },
    "checkout": "Paiement",
    "totalAmount": "Montant total",
    "commission": "Commission plateforme (12%)",
    "earnings": "Vos revenus"
  },
  "review": {
    "writeReview": "Laisser un avis",
    "stars": "Etoiles",
    "quality": "Qualite",
    "punctuality": "Ponctualite",
    "communication": "Communication",
    "cleanliness": "Proprete",
    "yourReview": "Votre avis",
    "noReviews": "Aucun avis pour le moment"
  },
  "errors": {
    "required": "Ce champ est requis",
    "emailInvalid": "Email invalide",
    "phoneInvalid": "Numero de telephone invalide (format: +216 XX XXX XXX ou 8 chiffres)",
    "passwordTooShort": "Le mot de passe doit contenir au moins 8 caracteres",
    "passwordMismatch": "Les mots de passe ne correspondent pas",
    "emailAlreadyExists": "Cet email est deja utilise",
    "phoneAlreadyExists": "Ce numero de telephone est deja utilise",
    "unauthorized": "Acces non autorise",
    "forbidden": "Acces interdit",
    "notFound": "Page introuvable",
    "serverError": "Erreur serveur, veuillez reessayer"
  },
  "locale": {
    "fr": "Francais",
    "ar": "Arabe",
    "en": "Anglais"
  },
  "categories": {
    "plomberie": "Plomberie",
    "electricite": "Electricite",
    "menage": "Menage",
    "cours": "Cours",
    "jardinage": "Jardinage",
    "peinture": "Peinture",
    "informatique": "Informatique",
    "demenagement": "Demenagement",
    "maison": "Maison & Renovation"
  },
  "layout": {
    "adminPanelTitle": "Panneau d'administration",
    "sidebarCollapse": "Reduire",
    "sidebarExpand": "Ouvrir",
    "sidebarNav": "Navigation administration"
  },
  "footer": {
    "tagline": "La plateforme de services locaux de confiance en Tunisie.",
    "forClients": "Pour les clients",
    "findProvider": "Trouver un prestataire",
    "howItWorks": "Comment ca marche",
    "serviceCategories": "Categories de services",
    "forProviders": "Pour les prestataires",
    "becomeProvider": "Devenir prestataire",
    "dashboard": "Tableau de bord",
    "kycVerification": "Verification KYC",
    "help": "Aide",
    "faq": "FAQ",
    "contact": "Nous contacter",
    "support": "Support",
    "legal": "Legal",
    "terms": "Conditions d'utilisation",
    "privacy": "Politique de confidentialite",
    "copyright": "Tous droits reserves."
  }
}
```

Reorganiser la structure des routes pour le routage par locale. Deplacer `src/app/layout.tsx` et `src/app/page.tsx` vers `src/app/[locale]/`:
```bash
mkdir -p src/app/[locale]
# Le contenu sera cree dans la tache suivante
```
  </action>
  <verify>
```bash
cd C:/Users/LENOVO/tawa-services

# Verifier l'installation de next-intl
ls node_modules/next-intl/package.json

# Verifier les fichiers crees
ls src/i18n/routing.ts
ls src/i18n/request.ts
ls src/messages/fr.json
ls middleware.ts

# Valider JSON
node -e "JSON.parse(require('fs').readFileSync('src/messages/fr.json', 'utf8')); console.log('JSON valid')"

# Typecheck
npm run typecheck
```
  </verify>
  <done>
- `next-intl` est installe dans node_modules
- `src/i18n/routing.ts` configure les locales `["fr"]` avec `defaultLocale: "fr"` ET exporte `Link`, `useRouter`, `redirect`, `usePathname` via `createNavigation(routing)`
- `src/i18n/request.ts` charge les messages depuis `src/messages/${locale}.json`
- `src/messages/fr.json` contient >= 50 cles couvrant common, navigation, home, auth, provider, service, booking, payment, review, errors, locale, footer
- `middleware.ts` intercepte toutes les routes non-statiques
- `npm run typecheck` passe sans erreur
  </done>
</task>

<task type="auto">
  <name>Task 2: Mettre a jour le layout App Router avec NextIntlClientProvider et page placeholder i18n</name>
  <files>
    src/app/[locale]/layout.tsx
    src/app/[locale]/page.tsx
    src/app/layout.tsx
  </files>
  <action>
Creer le layout racine localise `src/app/[locale]/layout.tsx`:
```tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import { NextIntlClientProvider } from "next-intl";
import { getMessages, getTranslations } from "next-intl/server";
import { notFound } from "next/navigation";
import { routing } from "@/i18n/routing";
import "../globals.css";

const inter = Inter({ subsets: ["latin"] });

export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>;
}): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "home" });

  return {
    title: t("title"),
    description: t("subtitle"),
  };
}

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;

  // Valider la locale
  if (!routing.locales.includes(locale as "fr")) {
    notFound();
  }

  // Charger les messages pour le client
  const messages = await getMessages();

  return (
    <html lang={locale}>
      <body className={inter.className}>
        <NextIntlClientProvider messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

Mettre a jour `src/app/layout.tsx` (layout racine minimal — le layout localise prend le dessus):
```tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
```

Creer `src/app/[locale]/page.tsx` utilisant le pattern `t('key')`:
```tsx
import { useTranslations } from "next-intl";

export default function HomePage() {
  const t = useTranslations("home");

  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-white">
      <h1 className="text-4xl font-bold text-blue-600">{t("title")}</h1>
      <p className="mt-4 text-lg text-gray-600">{t("subtitle")}</p>
      <p className="mt-2 text-sm text-gray-400">
        {t("heroTitle")}
      </p>
    </main>
  );
}
```

Creer le composant `src/components/shared/LocaleSwitcher.tsx` (pret pour ajout de locales):
```tsx
"use client";

import { useLocale, useTranslations } from "next-intl";
// IMPORTANT: Utiliser usePathname/useRouter de @/i18n/routing (createNavigation helpers),
// PAS de next/navigation — les helpers gèrent le prefixe locale automatiquement.
import { usePathname, useRouter, routing } from "@/i18n/routing";

export function LocaleSwitcher() {
  const locale = useLocale();
  const t = useTranslations("locale");
  const pathname = usePathname();
  const router = useRouter();

  function handleChange(newLocale: string) {
    // useRouter de @/i18n/routing gère le changement de locale nativement —
    // pas besoin de manipuler les segments du chemin manuellement.
    router.replace(pathname, { locale: newLocale });
  }

  // Ne montrer que si plusieurs locales sont disponibles
  if (routing.locales.length <= 1) {
    return null;
  }

  return (
    <select
      value={locale}
      onChange={(e) => handleChange(e.target.value)}
      className="rounded-md border border-gray-300 px-2 py-1 text-sm"
      aria-label="Changer de langue"
    >
      {routing.locales.map((loc) => (
        <option key={loc} value={loc}>
          {t(loc)}
        </option>
      ))}
    </select>
  );
}
```

S'assurer que `src/components/shared/` existe:
```bash
mkdir -p src/components/shared
mkdir -p src/components/ui
```
  </action>
  <verify>
```bash
cd C:/Users/LENOVO/tawa-services

# Demarrer le serveur et tester la redirection
npm run dev &
sleep 5

# Test: http://localhost:3000 doit rediriger vers /fr
curl -I http://localhost:3000 | grep -E "Location|301|302"
# Doit afficher Location: /fr

# Test: /fr doit retourner 200 avec le contenu HTML
curl -s http://localhost:3000/fr | grep "Tawa Services"
# Doit afficher le contenu de la page

kill %1

# Typecheck final
npm run typecheck
```
  </verify>
  <done>
- `http://localhost:3000` redirige automatiquement vers `http://localhost:3000/fr`
- `http://localhost:3000/fr` retourne 200 avec "Tawa Services" en contenu
- `src/app/[locale]/page.tsx` utilise `useTranslations("home")` — aucune chaine hardcodee
- `src/app/[locale]/layout.tsx` inclut `NextIntlClientProvider` avec les messages charges
- `src/components/shared/LocaleSwitcher.tsx` est cree et se cache si une seule locale; importe `usePathname`, `useRouter`, `routing` depuis `@/i18n/routing` (PAS `next/navigation`) et utilise `router.replace(pathname, { locale: newLocale })` pour le changement de locale
- `npm run typecheck` passe sans erreur
  </done>
</task>

</tasks>

<verification>
Verification globale du plan 03:

```bash
cd C:/Users/LENOVO/tawa-services

# Build complet avec next-intl
npm run build && echo "BUILD OK"

# Typecheck
npm run typecheck && echo "TYPECHECK OK"

# Lint
npm run lint && echo "LINT OK"

# Verifier l'absence de strings hardcodees en francais dans les composants TSX
# (uniquement des appels a t('key'))
grep -r "Accueil\|Rechercher\|Reservations\|Connexion" src/app/ --include="*.tsx" | grep -v "fr.json" | grep -v ".md"
# Doit retourner vide (aucun string hardcode)
```
</verification>

<success_criteria>
Acceptance criteria:
1. `npm run build` produit un build valide avec next-intl configure
2. `http://localhost:3000` redirige vers `/fr` (locale par defaut)
3. `http://localhost:3000/fr` affiche la page avec les traductions correctes depuis `fr.json`
4. `src/messages/fr.json` contient >= 50 cles de traduction pour tous les domaines metier
5. `src/app/[locale]/page.tsx` utilise uniquement `t('key')` — zero chaine hardcodee
6. `src/components/shared/LocaleSwitcher.tsx` est cree et pret pour l'ajout de locales AR/EN
7. `npm run typecheck` passe sans erreur TypeScript
8. `middleware.ts` intercepte les routes et ajoute le prefixe locale manquant
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-foundation-infrastructure/01-03-SUMMARY.md` avec:
- Version de next-intl installee
- Structure du dictionnaire fr.json (namespaces et cles principales)
- Pattern utilise pour les traductions (useTranslations / getTranslations)
- Instructions pour ajouter une nouvelle locale (AR ou EN) plus tard
- Note: LocaleSwitcher se cache automatiquement si une seule locale est configuree
</output>
