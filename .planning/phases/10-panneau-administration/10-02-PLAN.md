---
phase: 10-panneau-administration
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/app/[locale]/(admin)/admin/page.tsx
  - src/features/admin/components/DashboardStatsCards.tsx
  - src/features/admin/components/DashboardCharts.tsx
  - src/app/[locale]/(admin)/admin/users/page.tsx
  - src/features/admin/components/UsersDataTable.tsx
  - src/features/admin/components/UserActionsDropdown.tsx
  - src/app/[locale]/(admin)/admin/users/[id]/page.tsx
autonomous: true
requirements:
  - ADMN-01

must_haves:
  truths:
    - "Admin dashboard shows real stats cards with total users, providers, bookings, revenue from DB"
    - "Admin dashboard shows KPI cards with trend arrows comparing current month vs last month"
    - "Admin can see paginated user table with avatar, name, email, role badge, status, join date"
    - "Admin can search users by name/email and filter by role and status"
    - "Admin can ban/unban, activate/deactivate, delete users from actions dropdown with confirmation dialogs"
    - "Admin can view user detail page with profile info, bookings count, reviews count"
  artifacts:
    - path: "src/app/[locale]/(admin)/admin/page.tsx"
      provides: "Admin dashboard homepage with real data"
      min_lines: 30
    - path: "src/features/admin/components/DashboardStatsCards.tsx"
      provides: "Stats cards grid with trend arrows"
      min_lines: 40
    - path: "src/features/admin/components/UsersDataTable.tsx"
      provides: "User management data table with search, filters, pagination"
      min_lines: 80
    - path: "src/app/[locale]/(admin)/admin/users/[id]/page.tsx"
      provides: "User detail page"
      min_lines: 40
  key_links:
    - from: "src/app/[locale]/(admin)/admin/page.tsx"
      to: "src/features/admin/actions/admin-queries.ts"
      via: "getAdminStatsAction"
      pattern: "getAdminStatsAction"
    - from: "src/features/admin/components/UsersDataTable.tsx"
      to: "src/features/admin/actions/admin-actions.ts"
      via: "banUserAction, unbanUserAction"
      pattern: "(ban|unban)UserAction"
---

<objective>
Build the admin dashboard homepage with real-time stats and complete user management interface including data table, search/filters, actions, and user detail page.

Purpose: Admins need at-a-glance stats and full user management capabilities. This is the most used admin panel section.
Output: Working dashboard with stats + complete user CRUD interface.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-panneau-administration/10-01-SUMMARY.md
@src/app/[locale]/(admin)/admin/page.tsx
@src/components/layout/AdminSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin dashboard homepage with real stats and KPI cards</name>
  <files>
    src/app/[locale]/(admin)/admin/page.tsx
    src/features/admin/components/DashboardStatsCards.tsx
    src/features/admin/components/DashboardCharts.tsx
  </files>
  <action>
1. Create src/features/admin/components/DashboardStatsCards.tsx ("use client"):
- Accept stats prop: { totalUsers, totalProviders, totalBookings, totalRevenue, pendingKyc, activeServices, openReports, previousMonthUsers, previousMonthBookings, previousMonthRevenue }.
- Render 4 main stats cards in a grid (2 cols mobile, 4 cols desktop):
  - Total Users: icon Users, count, trend arrow (green up / red down) comparing vs previous month percentage
  - Total Providers: icon ShieldCheck, count, no trend
  - Total Bookings: icon Calendar, count, trend arrow vs previous month
  - Revenue: icon DollarSign (or Banknote), format as TND with Intl.NumberFormat("fr-TN"), trend arrow vs previous month
- Below main cards, render 3 secondary cards (KYC pending with amber dot like existing, active services, open reports with red dot if critical).
- Each card uses Card component from shadcn: Card > CardContent with p-4, icon top-right, count large, label small muted.
- Trend arrow: if current > previous show ArrowUpRight in green-600, if less show ArrowDownRight in red-600, if equal show Minus in muted.
- Use useTranslations("admin.dashboard") for all labels.

2. Create src/features/admin/components/DashboardCharts.tsx ("use client"):
- This is a PLACEHOLDER component that renders 4 chart containers in a 2x2 grid with Card wrappers and placeholder text saying "Graphiques disponibles dans Analytique".
- Each card has title (revenueChart, bookingsChart, categoriesChart, userGrowthChart from i18n).
- Link each card to /admin/analytics.
- Recharts will be installed and wired in Plan 10-06 (analytics dashboard). Do NOT install recharts here.

3. Rewrite src/app/[locale]/(admin)/admin/page.tsx:
- Server component that calls getAdminStatsAction().
- Pass stats to DashboardStatsCards as props.
- Render DashboardCharts below.
- Add recent activity section: last 5 reports (getAdminReportsAction with page=1, pageSize=5) as a compact list with priority badge, type, and time ago.
- Add quick links grid: "Gestion utilisateurs", "Services", "Signalements", "Analytique" as Link cards.
- Use getTranslations("admin.dashboard") for all labels.
- Metadata: title "Tableau de bord | Admin".
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
    <manual>Visit /admin and verify stats cards show real DB counts, trend arrows display, chart placeholders visible</manual>
  </verify>
  <done>Admin dashboard shows real stats from DB (users, providers, bookings, revenue), KPI cards with trend arrows vs last month, chart placeholder cards linking to analytics, and recent reports list. Mobile responsive (2-col grid on small screens).</done>
</task>

<task type="auto">
  <name>Task 2: User management data table with actions and detail page</name>
  <files>
    src/app/[locale]/(admin)/admin/users/page.tsx
    src/features/admin/components/UsersDataTable.tsx
    src/features/admin/components/UserActionsDropdown.tsx
    src/app/[locale]/(admin)/admin/users/[id]/page.tsx
  </files>
  <action>
1. Create src/features/admin/components/UserActionsDropdown.tsx ("use client"):
- Accept user prop with id, name, role, isActive, isBanned.
- DropdownMenu with DropdownMenuTrigger (MoreHorizontal icon button), DropdownMenuContent with:
  - "Voir le profil" -> Link to /admin/users/[id]
  - Separator
  - If not banned: "Bannir" -> opens AlertDialog with reason textarea, calls banUserAction
  - If banned: "Debannir" -> calls unbanUserAction
  - If active: "Desactiver" -> calls deactivateUserAction
  - If not active: "Activer" -> calls activateUserAction
  - Separator
  - "Supprimer" (destructive) -> opens AlertDialog confirmation, calls deleteUserAction
- Cannot ban/deactivate/delete ADMIN users — hide those options if user.role === "ADMIN".
- After each action, call router.refresh() to reload server data.
- Show toast on success/error using useToast.
- Use useTranslations("admin.users") for all labels.

2. Create src/features/admin/components/UsersDataTable.tsx ("use client"):
- Accept users[], total, currentPage, pageSize props.
- Search input at top (debounced 300ms) that updates URL searchParams with ?search=.
- Two Select filters side by side: role (CLIENT/PROVIDER/ADMIN/All) and status (active/banned/inactive/All).
- Filters also update URL searchParams (?role=, ?status=).
- shadcn Table with columns:
  - Avatar (Avatar component, fallback = initials from name) + Name (bold) + Email (muted below)
  - Role: Badge with variant (default for CLIENT, secondary for PROVIDER, destructive outline for ADMIN)
  - Status: Badge green "Actif" / red "Banni" / gray "Inactif"
  - Inscription: formatted date with Intl.DateTimeFormat("fr-FR")
  - Actions: UserActionsDropdown
- Pagination at bottom: Previous/Next buttons, "Page X sur Y" text, "Affichage de X a Y sur Z".
- Pagination navigates via URL searchParams (?page=).
- Mobile responsive: hide Email and Inscription columns on < md.

3. Rewrite src/app/[locale]/(admin)/admin/users/page.tsx:
- Server component that reads searchParams (search, role, status, page).
- Calls getAdminUsersAction(filters) with parsed searchParams.
- Passes results + pagination to UsersDataTable.
- Header with Users icon + title + total count badge.
- Metadata: title "Utilisateurs | Admin".

4. Create src/app/[locale]/(admin)/admin/users/[id]/page.tsx:
- Server component that calls getUserDetailAction(params.id).
- If not found, redirect to /admin/users.
- Display user profile card: avatar (large), name, email, phone, role badge, status badge, member since date.
- If user is Provider: show KYC status badge, display name, rating, completed missions.
- Stats grid: bookings count, reviews count, reports count.
- Action buttons: Ban/Unban, Activate/Deactivate (same logic as dropdown but as full-width buttons).
- Back link to /admin/users.
- Metadata: title "{userName} | Admin".
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
    <manual>Visit /admin/users, verify table renders, search works, filter by role/status works, ban/unban actions work with confirmation dialog, pagination works, user detail page loads</manual>
  </verify>
  <done>User management page shows paginated data table with avatar/name/email/role badge/status/date columns. Search by name/email works with 300ms debounce. Role and status filters work. Ban/unban/activate/deactivate/delete actions available with confirmation dialogs. User detail page shows full profile with stats. Mobile responsive with hidden columns.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. /admin shows real stats from DB with trend arrows
3. /admin/users shows paginated user table with working search, filters, and actions
4. /admin/users/[id] shows user detail with profile and stats
5. Ban/unban actions update user status in DB and reflect immediately in table
</verification>

<success_criteria>
- Dashboard homepage replaces placeholder with real aggregate stats
- KPI trend arrows correctly compare current vs previous month
- User data table supports search, role filter, status filter, pagination
- All user actions (ban/unban/activate/deactivate/delete) work with confirmation
- User detail page renders profile, stats, and action buttons
- Mobile responsive layout
</success_criteria>

<output>
After completion, create `.planning/phases/10-panneau-administration/10-02-SUMMARY.md`
</output>
