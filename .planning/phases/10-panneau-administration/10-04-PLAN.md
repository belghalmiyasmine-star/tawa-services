---
phase: 10-panneau-administration
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/app/[locale]/(admin)/admin/reports/page.tsx
  - src/features/admin/components/ReportsDataTable.tsx
  - src/features/admin/components/ReportDetailSheet.tsx
  - src/features/admin/components/ReportActionsDropdown.tsx
  - src/features/admin/components/SlaBadge.tsx
autonomous: true
requirements:
  - ADMN-03

must_haves:
  truths:
    - "Reports display in a prioritized table (CRITICAL first, then IMPORTANT, then MINOR)"
    - "Each report shows remaining SLA time as a countdown badge (green/amber/red)"
    - "Admin can change report status through investigate/resolve/dismiss workflow"
    - "Admin can add notes to reports"
    - "Report detail opens in a Sheet overlay showing full information"
    - "SLA expired reports are visually highlighted in red"
  artifacts:
    - path: "src/features/admin/components/ReportsDataTable.tsx"
      provides: "Prioritized reports data table with SLA badges"
      min_lines: 80
    - path: "src/features/admin/components/ReportDetailSheet.tsx"
      provides: "Report detail view in Sheet overlay"
      min_lines: 60
    - path: "src/features/admin/components/SlaBadge.tsx"
      provides: "SLA countdown badge component"
      min_lines: 25
  key_links:
    - from: "src/features/admin/components/ReportsDataTable.tsx"
      to: "src/features/admin/actions/admin-actions.ts"
      via: "updateReportAction"
      pattern: "updateReportAction"
    - from: "src/features/admin/components/SlaBadge.tsx"
      to: "report.slaDeadline"
      via: "time comparison"
      pattern: "slaDeadline"
---

<objective>
Build the reports/signalements management system with prioritized table, SLA countdown timers, detail Sheet overlay, and investigate/resolve/dismiss workflow.

Purpose: Admins must handle user reports within SLA deadlines (critical <2h, important <24h, minor <48h). This plan creates the report handling interface with visual SLA tracking.
Output: Complete report management interface with priority ordering, SLA badges, detail sheet, and status workflow.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-panneau-administration/10-01-SUMMARY.md
@src/features/admin/actions/admin-actions.ts
@src/features/admin/actions/admin-queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SLA badge component and report detail Sheet</name>
  <files>
    src/features/admin/components/SlaBadge.tsx
    src/features/admin/components/ReportDetailSheet.tsx
  </files>
  <action>
1. Create src/features/admin/components/SlaBadge.tsx ("use client"):
- Accept slaDeadline: string (ISO date), status: string props.
- If status is RESOLVED or DISMISSED, show Badge "Cloture" in gray.
- Calculate time remaining: slaDeadline - now.
- If expired (remaining <= 0): Badge "SLA expire" in destructive red, with Clock icon.
- If < 30 min remaining: Badge "Xm restantes" in destructive red, pulsing animation.
- If < 2h remaining: Badge "Xh Xm" in amber/warning.
- If >= 2h remaining: Badge "Xh" or "Xj Xh" in green.
- Use useEffect with setInterval(1000 * 60) to update every minute for live countdown.
- Format: hours and minutes for < 24h, days and hours for >= 24h.
- Use useTranslations("admin.reports") for "SLA expire" etc.

2. Create src/features/admin/components/ReportDetailSheet.tsx ("use client"):
- Accept report prop (full report data with reporter, reported, admin notes) and onClose callback.
- Uses shadcn Sheet (side="right", open controlled by parent).
- SheetHeader: Report type badge + priority badge + SLA badge.
- SheetContent sections:
  a) Reporter info: avatar, name, email, "Signale le [date]"
  b) Reported user info (if exists): avatar, name, email, role
  c) Report details: type, reason (full text), description (if exists)
  d) Reference link: if referenceId exists, link to the referenced service/review/message
  e) Status timeline: horizontal steps showing OPEN -> INVESTIGATING -> RESOLVED/DISMISSED with current step highlighted
  f) Admin notes section: existing adminNote displayed in a gray box, textarea to add/update note
  g) Action buttons at bottom:
     - If OPEN: "Investiguer" button (sets status to INVESTIGATING)
     - If INVESTIGATING: "Resoudre" and "Rejeter" buttons
     - If RESOLVED/DISMISSED: no action buttons, show "Cloture le [date]"
- Each action button calls updateReportAction with the new status and admin note.
- Show toast on success, call onClose + router.refresh().
- Use useTranslations("admin.reports") for all labels.
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
    <manual>Verify SlaBadge shows correct countdown colors, ReportDetailSheet opens with full report info and action buttons</manual>
  </verify>
  <done>SlaBadge component renders live countdown with color-coded urgency (red/amber/green). ReportDetailSheet shows full report details in side panel with reporter/reported info, status timeline, admin notes, and investigate/resolve/dismiss action buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Reports data table page with filters and priority ordering</name>
  <files>
    src/features/admin/components/ReportActionsDropdown.tsx
    src/features/admin/components/ReportsDataTable.tsx
    src/app/[locale]/(admin)/admin/reports/page.tsx
  </files>
  <action>
1. Create src/features/admin/components/ReportActionsDropdown.tsx ("use client"):
- Accept report prop with id, status.
- DropdownMenu:
  - "Voir les details" -> triggers onViewDetail callback (opens ReportDetailSheet in parent)
  - Separator
  - If OPEN: "Investiguer" -> calls updateReportAction(reportId, INVESTIGATING), router.refresh()
  - If INVESTIGATING: "Resoudre" -> calls updateReportAction(reportId, RESOLVED), "Rejeter" -> updateReportAction(reportId, DISMISSED)
  - Disabled items for RESOLVED/DISMISSED (already closed)
- Toast on success/error.
- Use useTranslations("admin.reports").

2. Create src/features/admin/components/ReportsDataTable.tsx ("use client"):
- Accept reports[], total, currentPage, pageSize props.
- Manages selectedReport state to control ReportDetailSheet open/close.
- Search input (debounced 300ms) updates URL ?search=.
- Three Select filters: priority (CRITICAL/IMPORTANT/MINOR/All), status (OPEN/INVESTIGATING/RESOLVED/DISMISSED/All), type (USER/SERVICE/REVIEW/MESSAGE/All).
- shadcn Table columns:
  - Priority: colored dot indicator (red=CRITICAL, amber=IMPORTANT, gray=MINOR) + text label
  - Reporter: avatar + name
  - Type: Badge (USER/SERVICE/REVIEW/MESSAGE) with i18n labels
  - Reason: truncated to 60 chars
  - SLA: SlaBadge component
  - Status: Badge colored (OPEN=blue, INVESTIGATING=amber, RESOLVED=green, DISMISSED=gray)
  - Actions: ReportActionsDropdown
- Table rows with CRITICAL priority have a subtle red-50 background tint.
- Rows with expired SLA have red left border (border-l-2 border-destructive).
- Ordering: CRITICAL open first, then IMPORTANT open, then MINOR open, then resolved/dismissed.
- Pagination: same pattern as other tables.
- Clicking a row opens ReportDetailSheet.
- Mobile: hide Reporter and Type columns on < md. Show priority dot + reason + SLA.

3. Rewrite src/app/[locale]/(admin)/admin/reports/page.tsx:
- Server component reading searchParams (search, priority, status, type, page).
- Calls getAdminReportsAction(filters).
- Wraps content in a client ReportsPageClient that manages Sheet state.
- Pass reports + pagination + detail data to ReportsDataTable.
- Header with Flag icon + title + open count badge (red if any CRITICAL).
- Metadata: title "Signalements | Admin".
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
    <manual>Visit /admin/reports, verify reports ordered by priority, SLA badges countdown live, clicking row opens detail Sheet, investigate/resolve/dismiss workflow works</manual>
  </verify>
  <done>Reports page shows prioritized table with CRITICAL first. Each row has priority dot, SLA countdown badge with live timer, status badge. Filters by priority/status/type work. Clicking row opens detail Sheet with full report info, status timeline, admin notes, and action buttons. Expired SLA rows highlighted with red border. Mobile responsive.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. /admin/reports shows prioritized report table
3. SLA badges update live and show correct colors
4. Report detail Sheet opens on row click
5. Investigate -> Resolve/Dismiss workflow updates DB
6. Admin notes persist
7. Filters by priority, status, type work
</verification>

<success_criteria>
- Reports ordered by priority (CRITICAL first) then by creation date
- SLA countdown badges with color coding (red=urgent, amber=warning, green=ok)
- Live SLA timer updates every minute
- Expired SLA visually highlighted
- Report detail in Sheet with full info and status timeline
- Investigate/resolve/dismiss workflow with admin notes
- Search and filters work
- Mobile responsive
</success_criteria>

<output>
After completion, create `.planning/phases/10-panneau-administration/10-04-SUMMARY.md`
</output>
