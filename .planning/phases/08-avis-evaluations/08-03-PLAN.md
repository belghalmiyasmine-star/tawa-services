---
phase: 08-avis-evaluations
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/[locale]/(provider)/provider/bookings/[bookingId]/review/page.tsx
  - src/app/[locale]/(provider)/provider/bookings/[bookingId]/page.tsx
autonomous: true
requirements:
  - REVW-02
  - REVW-03

must_haves:
  truths:
    - "Provider can rate a client with 1-5 stars and 4 criteria after completed booking"
    - "Provider sees 'Evaluer le client' button on completed booking detail"
    - "Provider review form reuses the same ReviewForm component with authorRole=PROVIDER"
  artifacts:
    - path: "src/app/[locale]/(provider)/provider/bookings/[bookingId]/review/page.tsx"
      provides: "Provider review submission page"
      min_lines: 40
  key_links:
    - from: "src/app/[locale]/(provider)/provider/bookings/[bookingId]/review/page.tsx"
      to: "src/features/review/components/ReviewForm.tsx"
      via: "ReviewForm component with authorRole=PROVIDER"
      pattern: "ReviewForm"
    - from: "src/app/[locale]/(provider)/provider/bookings/[bookingId]/review/page.tsx"
      to: "src/features/review/actions/review-queries.ts"
      via: "getReviewWindowAction eligibility check"
      pattern: "getReviewWindowAction"
---

<objective>
Build the provider-side review page that allows providers to rate clients after completed bookings, reusing the shared ReviewForm component with provider-specific role context.

Purpose: Enables bidirectional rating system (REVW-02) where providers can evaluate clients.
Output: Provider review page and booking detail integration.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-avis-evaluations/08-01-SUMMARY.md
@src/app/[locale]/(provider)/provider/bookings/[bookingId]/page.tsx
@src/features/review/components/ReviewForm.tsx
@src/features/review/actions/review-queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provider review page and booking detail integration</name>
  <files>
    src/app/[locale]/(provider)/provider/bookings/[bookingId]/review/page.tsx
    src/app/[locale]/(provider)/provider/bookings/[bookingId]/page.tsx
  </files>
  <action>
**1. Create `src/app/[locale]/(provider)/provider/bookings/[bookingId]/review/page.tsx`:**
- Server component page
- Auth guard: getServerSession, redirect if not PROVIDER role
- Fetch booking detail, verify the provider owns this booking (booking.provider.userId === session.user.id)
- Verify booking is COMPLETED
- Call getReviewWindowAction(bookingId) to check eligibility
- If canReview=false and hasReviewed=true: show "Vous avez deja laisse un avis" with link back to booking
- If canReview=false and window expired: show "La periode d'evaluation est terminee" message
- If canReview=true: render ReviewForm with `bookingId` and `authorRole="PROVIDER"`
- Page header: "Evaluer le client" (using t('review.rateClient')) with provider's view of client name
- Show client name/info: booking.client name, service title context
- Back link to `/provider/bookings/${bookingId}`
- Show remaining days in review window: "{days} jour(s) restant(s)"

**2. Add "Evaluer le client" button to provider booking detail page:**
- Read `src/app/[locale]/(provider)/provider/bookings/[bookingId]/page.tsx` to understand its structure
- Add a review call-to-action section visible when booking.status === "COMPLETED":
  - Link to `/provider/bookings/${bookingId}/review` styled as primary button
  - Label: "Evaluer le client" (Star icon from lucide-react + text)
  - Only show if review window is open (completedAt + 10 days > now, simple date check)
  - If already reviewed, show "Avis soumis" badge instead of button
  - Position: in the Actions section of the booking detail, alongside any other action buttons
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to provider booking detail for a COMPLETED booking and verify "Evaluer le client" button appears</manual>
  </verify>
  <done>Provider can access review page from completed booking detail, form submits with authorRole=PROVIDER, eligibility checks work (window + already reviewed)</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Provider booking detail for COMPLETED booking shows "Evaluer le client" button
- Provider review page renders ReviewForm with PROVIDER role
- Review submission creates review with authorRole="PROVIDER" and correct targetId (client)
</verification>

<success_criteria>
- Provider booking detail shows review CTA for completed bookings within 10-day window
- Provider review page validates auth and booking ownership
- ReviewForm reused with authorRole="PROVIDER"
- After provider submits, both reviews get published if client already reviewed
</success_criteria>

<output>
After completion, create `.planning/phases/08-avis-evaluations/08-03-SUMMARY.md`
</output>
