---
phase: 08-avis-evaluations
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04"]
files_modified:
  - src/features/review/components/ReviewCard.tsx
  - src/features/review/components/ReviewsList.tsx
  - src/features/review/components/RatingBreakdown.tsx
  - src/features/review/components/CriteriaRadarChart.tsx
autonomous: true
requirements:
  - REVW-06
  - REVW-08

must_haves:
  truths:
    - "Published reviews display with avatar, stars, date, criteria breakdown, comment, and photos"
    - "ReviewsList shows average rating, distribution bars, sort options, and pagination"
    - "Rating breakdown shows bar chart for 1-5 star distribution"
    - "Criteria radar/bar chart shows average per criterion on provider profile"
  artifacts:
    - path: "src/features/review/components/ReviewCard.tsx"
      provides: "Single review display card"
      min_lines: 50
    - path: "src/features/review/components/ReviewsList.tsx"
      provides: "Reviews list with sorting and pagination"
      min_lines: 80
    - path: "src/features/review/components/RatingBreakdown.tsx"
      provides: "Rating distribution bar chart"
      min_lines: 40
    - path: "src/features/review/components/CriteriaRadarChart.tsx"
      provides: "Criteria average display chart"
      min_lines: 40
  key_links:
    - from: "src/features/review/components/ReviewsList.tsx"
      to: "src/features/review/actions/review-queries.ts"
      via: "getProviderReviewsAction for data fetching"
      pattern: "getProviderReviewsAction"
---

<objective>
Build the review display components: ReviewCard for individual reviews, ReviewsList with sorting/pagination, RatingBreakdown for star distribution, and CriteriaRadarChart for criteria averages.

Purpose: These components are needed for provider profile integration and public review visibility (REVW-06, REVW-08).
Output: Complete set of review display components ready for profile integration.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-avis-evaluations/08-01-SUMMARY.md
@.planning/phases/08-avis-evaluations/08-02-SUMMARY.md
@src/features/review/actions/review-queries.ts
@src/features/review/components/StarRating.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReviewCard and RatingBreakdown components</name>
  <files>
    src/features/review/components/ReviewCard.tsx
    src/features/review/components/RatingBreakdown.tsx
  </files>
  <action>
**1. Create `src/features/review/components/ReviewCard.tsx`:**
- Server-compatible component (no "use client" needed unless interactive)
- Props:
  ```typescript
  interface ReviewCardProps {
    review: {
      id: string;
      stars: number;
      qualityRating: number | null;
      punctualityRating: number | null;
      communicationRating: number | null;
      cleanlinessRating: number | null;
      text: string | null;
      photoUrls: string[];
      publishedAt: Date | null;
      authorName: string; // First name only
      flagged: boolean;
    };
  }
  ```
- Layout:
  - Top row: Author avatar (first letter circle, bg-primary/10), author name, date (relative format "il y a 3 jours" using Intl.RelativeTimeFormat or manual calc)
  - Star display: StarRating readonly mode showing overall stars
  - Criteria mini-grid (2x2): quality, punctuality, communication, cleanliness — small bar or stars for each
  - Text: review.text rendered, truncated at 300 chars with "Lire la suite" expand toggle (if text > 300)
  - Photos: horizontal scroll of thumbnails (h-24 w-24 rounded-lg) with click to expand (Dialog/lightbox)
  - If flagged: show "Cet avis est en cours de moderation" banner instead of content
  - "Reservation verifiee" badge (green checkmark) — all reviews are from verified bookings

**2. Create `src/features/review/components/RatingBreakdown.tsx`:**
- Props: `distribution: { [stars: number]: number }`, `total: number`, `average: number`
- Layout:
  - Average rating: large number (e.g., "4.3") with StarRating readonly + total count text
  - Distribution bars: 5 rows (5 stars to 1 star), each with star label, progress bar (bg-amber-400 fill proportional to percentage), count
  - Progress bars use Tailwind w-[X%] with style={{ width: `${percent}%` }} for dynamic widths
  - Responsive: full width, compact on mobile
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify ReviewCard displays all review data and RatingBreakdown shows distribution bars</manual>
  </verify>
  <done>ReviewCard displays author, stars, criteria, text (with expand), photos, and flagged state. RatingBreakdown shows average + 5-star distribution bars.</done>
</task>

<task type="auto">
  <name>Task 2: ReviewsList with sorting/pagination and CriteriaRadarChart</name>
  <files>
    src/features/review/components/ReviewsList.tsx
    src/features/review/components/CriteriaRadarChart.tsx
  </files>
  <action>
**1. Create `src/features/review/components/ReviewsList.tsx`:**
- "use client" component (needs state for sorting/pagination)
- Props: `providerId: string`, `initialData?: { reviews: Review[]; total: number; averages: CriteriaAverages }`
- If no initialData, fetches on mount via getProviderReviewsAction
- Layout:
  - Header section: RatingBreakdown component (left), CriteriaRadarChart (right) — side by side on desktop, stacked on mobile
  - Sort dropdown: "Plus recents", "Meilleures notes", "Moins bonnes notes" — using Select from shadcn
  - Review list: map over reviews rendering ReviewCard for each
  - Pagination: "Voir plus" button that loads next page (append to list, infinite-scroll style)
  - Empty state: "Aucun avis pour le moment" centered message with star icon
- Fetching: useEffect + useState for reviews array, useTransition for loading states
- Sort change: reset reviews array, fetch page 1 with new sort
- Pagination: fetch next page, append to existing reviews

**2. Create `src/features/review/components/CriteriaRadarChart.tsx`:**
- Props: `averages: { quality: number; punctuality: number; communication: number; cleanliness: number }`
- Since recharts is NOT in package.json and adding it for one chart is not justified:
  - Build a simple CSS-based horizontal bar chart instead (no recharts dependency)
  - 4 rows: criterion label, colored bar (width proportional to value/5 * 100%), value text
  - Bar colors: quality=blue, punctuality=green, communication=purple, cleanliness=amber
  - Each bar: rounded-full, h-2, transition-all for animation
  - Show value as "X.X/5" next to bar
- Alternative: if recharts is already available, use a radar chart. Check package.json first.
- Fallback: pure CSS horizontal bars are lightweight and PFE-appropriate
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify ReviewsList renders with sorting and pagination, CriteriaRadarChart shows 4 criteria bars</manual>
  </verify>
  <done>ReviewsList fetches and displays reviews with sort (recent/best/worst) and "Voir plus" pagination. CriteriaRadarChart shows 4 criteria as horizontal bars with values. RatingBreakdown integrated in header.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- ReviewCard renders all review fields including photos and criteria mini-display
- ReviewsList handles empty state, sorting, and pagination
- RatingBreakdown shows accurate distribution bars
- CriteriaRadarChart shows 4 criteria averages
</verification>

<success_criteria>
- Published reviews show author name, stars, criteria, text, photos, date
- Flagged reviews show moderation message instead of content
- Reviews sortable by recent, best, worst
- Pagination loads more reviews on demand
- Rating breakdown shows 5-star distribution with counts
- Criteria chart shows per-criterion averages
</success_criteria>

<output>
After completion, create `.planning/phases/08-avis-evaluations/08-05-SUMMARY.md`
</output>
