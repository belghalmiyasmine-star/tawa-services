---
phase: 08-avis-evaluations
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/features/review/components/StarRating.tsx
  - src/features/review/components/CriteriaRatingGroup.tsx
  - src/features/review/components/ReviewPhotoUploader.tsx
  - src/features/review/components/ReviewForm.tsx
  - src/app/[locale]/(client)/bookings/[bookingId]/review/page.tsx
autonomous: true
requirements:
  - REVW-01
  - REVW-03
  - REVW-04

must_haves:
  truths:
    - "Client can rate provider on 4 criteria (quality, punctuality, communication, cleanliness) with 1-5 stars each"
    - "Client can write a text review with character counter (20-500)"
    - "Client can upload up to 3 photos with the review"
    - "Review form submits via submitReviewAction and shows success/moderation feedback"
  artifacts:
    - path: "src/features/review/components/StarRating.tsx"
      provides: "Reusable interactive star rating component"
      min_lines: 30
    - path: "src/features/review/components/ReviewForm.tsx"
      provides: "Complete review form with criteria, text, photos"
      min_lines: 80
    - path: "src/app/[locale]/(client)/bookings/[bookingId]/review/page.tsx"
      provides: "Client review submission page"
      min_lines: 40
  key_links:
    - from: "src/features/review/components/ReviewForm.tsx"
      to: "src/features/review/actions/review-actions.ts"
      via: "submitReviewAction call on form submit"
      pattern: "submitReviewAction"
    - from: "src/app/[locale]/(client)/bookings/[bookingId]/review/page.tsx"
      to: "src/features/review/actions/review-queries.ts"
      via: "getReviewWindowAction to check eligibility"
      pattern: "getReviewWindowAction"
---

<objective>
Build the client-side review form with interactive star ratings per criterion, text input with character counter, photo upload, and integrate with the review submission page accessible from completed booking detail.

Purpose: Enables clients to evaluate providers after service completion (REVW-01, REVW-03, REVW-04).
Output: Fully functional client review form and submission page.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-avis-evaluations/08-01-SUMMARY.md
@src/app/[locale]/(client)/bookings/[bookingId]/page.tsx
@src/features/review/schemas/review.ts
@src/features/review/actions/review-actions.ts
@src/features/review/actions/review-queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Star rating, criteria group, and photo uploader components</name>
  <files>
    src/features/review/components/StarRating.tsx
    src/features/review/components/CriteriaRatingGroup.tsx
    src/features/review/components/ReviewPhotoUploader.tsx
  </files>
  <action>
**1. Create `src/features/review/components/StarRating.tsx`:**
- "use client" component
- Props: `value: number`, `onChange: (v: number) => void`, `size?: 'sm' | 'md' | 'lg'`, `readonly?: boolean`, `label?: string`
- Render 5 Star icons from lucide-react
- Hover state: highlight stars up to hover position (useState for hoverValue)
- Click: call onChange with clicked star number (1-5)
- Visual: filled stars (fill-amber-400 text-amber-400) for rated, outline stars (text-gray-300) for unrated
- Hover preview: fill-amber-200 for preview state
- Readonly mode: no hover/click handlers, just display
- Size variants: sm=h-4 w-4, md=h-5 w-5, lg=h-6 w-6
- Accessible: role="radiogroup", aria-label from label prop, each star has aria-label "X etoiles"

**2. Create `src/features/review/components/CriteriaRatingGroup.tsx`:**
- "use client" component
- Props: `criteria: { key: string; label: string; value: number }[]`, `onChange: (key: string, value: number) => void`
- Renders 4 StarRating rows: quality, punctuality, communication, cleanliness
- Each row: label (from i18n via useTranslations('review')) + StarRating size="sm"
- Layout: grid with label on left, stars on right, mobile-friendly (stack on small screens)

**3. Create `src/features/review/components/ReviewPhotoUploader.tsx`:**
- "use client" component
- Props: `photos: string[]`, `onAdd: (url: string) => void`, `onRemove: (index: number) => void`, `maxPhotos?: number` (default 3)
- Upload button triggers hidden file input (accept="image/jpeg,image/png,image/webp")
- On file select: POST to `/api/review/photos` with FormData, get back URL, call onAdd
- Display uploaded photos as thumbnails (h-20 w-20 rounded-lg object-cover) with X remove button
- Disable upload button when photos.length >= maxPhotos
- Loading state during upload (Loader2 spinner from lucide-react)
- Error toast (useToast from shadcn) if upload fails
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify StarRating renders 5 interactive stars, CriteriaRatingGroup shows 4 criteria</manual>
  </verify>
  <done>StarRating supports hover preview and click selection, CriteriaRatingGroup renders all 4 criteria, ReviewPhotoUploader handles upload/remove with max 3 limit</done>
</task>

<task type="auto">
  <name>Task 2: ReviewForm component and client review page</name>
  <files>
    src/features/review/components/ReviewForm.tsx
    src/app/[locale]/(client)/bookings/[bookingId]/review/page.tsx
    src/app/[locale]/(client)/bookings/[bookingId]/page.tsx
  </files>
  <action>
**1. Create `src/features/review/components/ReviewForm.tsx`:**
- "use client" component
- Props: `bookingId: string`, `authorRole: "CLIENT" | "PROVIDER"`, `onSuccess?: () => void`
- Uses react-hook-form with zodResolver(reviewSubmitSchema)
- Form layout:
  1. Overall rating: StarRating size="lg" with label "Note globale"
  2. Criteria ratings: CriteriaRatingGroup for 4 criteria
  3. Text area: Textarea with character counter showing "{count}/500 caracteres", min 20 validation message
  4. Photos: ReviewPhotoUploader (only if authorRole === "CLIENT" — photos optional for providers too, but available for both per CDC)
  5. Submit button: "Soumettre mon avis" with loading state (useTransition)
- On submit: call submitReviewAction(formData)
  - If success and review.flagged: show info toast "Votre avis contient des informations de contact et sera examine par un moderateur"
  - If success and not flagged: show success toast "Votre avis a ete soumis avec succes" + info toast about simultaneous publication
  - If error: show error toast with message
  - Call onSuccess callback on success
- Character counter: real-time, changes color: gray <20, green 20-450, amber 450-500

**2. Create `src/app/[locale]/(client)/bookings/[bookingId]/review/page.tsx`:**
- Server component page
- Auth guard: getServerSession, redirect if not CLIENT
- Fetch booking via getBookingDetailAction, verify COMPLETED status
- Call getReviewWindowAction to check eligibility
- If canReview=false and hasReviewed=true: show "Vous avez deja laisse un avis" message with link back
- If canReview=false and window expired: show "La periode d'evaluation est terminee" message
- If canReview=true: render ReviewForm with bookingId and authorRole="CLIENT"
- Show review window info: "X jour(s) restant(s) pour laisser un avis"
- Page header: "Evaluer le prestataire" with back link to booking detail

**3. Add "Laisser un avis" button to client booking detail page:**
- Edit `src/app/[locale]/(client)/bookings/[bookingId]/page.tsx`
- After the Actions section (cancel button area), add a new section visible when `booking.status === "COMPLETED"`:
  - Link to `/bookings/${booking.id}/review` styled as primary button
  - Label: "Laisser un avis" (Star icon + text)
  - Only show if review window is still open (check completedAt + 10 days > now)
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to a completed booking detail page and verify "Laisser un avis" button appears, clicking leads to review form</manual>
  </verify>
  <done>ReviewForm renders with all criteria, text counter, photo upload. Client review page checks eligibility, shows form or status message. Booking detail shows "Laisser un avis" for completed bookings.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Client booking detail for COMPLETED booking shows "Laisser un avis" button
- Review page shows form with 5 overall stars + 4 criteria + text area + photo upload
- Submitting review calls submitReviewAction
</verification>

<success_criteria>
- Interactive star rating with hover preview for all 5 criteria (overall + 4 specific)
- Character counter updates in real-time (20-500 range)
- Photo upload accepts up to 3 images
- Form validates all required fields before submission
- "Laisser un avis" button visible on completed booking detail
</success_criteria>

<output>
After completion, create `.planning/phases/08-avis-evaluations/08-02-SUMMARY.md`
</output>
