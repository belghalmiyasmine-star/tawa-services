---
phase: 08-avis-evaluations
plan: 06
type: execute
wave: 3
depends_on: ["08-05"]
files_modified:
  - src/app/[locale]/(client)/providers/[providerId]/page.tsx
  - src/features/review/actions/review-queries.ts
  - src/app/[locale]/(admin)/admin/reviews/page.tsx
autonomous: true
requirements:
  - REVW-07
  - REVW-08

must_haves:
  truths:
    - "Provider profile Avis tab shows real reviews with sorting, pagination, and rating breakdown"
    - "Provider rating and ratingCount are updated after review publication"
    - "Admin can see flagged reviews queue for moderation"
  artifacts:
    - path: "src/app/[locale]/(client)/providers/[providerId]/page.tsx"
      provides: "Provider profile with reviews tab integration"
      contains: "ReviewsList"
    - path: "src/app/[locale]/(admin)/admin/reviews/page.tsx"
      provides: "Admin review moderation page"
      min_lines: 50
  key_links:
    - from: "src/app/[locale]/(client)/providers/[providerId]/page.tsx"
      to: "src/features/review/components/ReviewsList.tsx"
      via: "ReviewsList component in Avis tab"
      pattern: "ReviewsList"
    - from: "src/app/[locale]/(admin)/admin/reviews/page.tsx"
      to: "src/features/review/actions/review-queries.ts"
      via: "getFlaggedReviewsAction query"
      pattern: "getFlaggedReviewsAction"
---

<objective>
Integrate the review display components into the provider profile page (replacing the Phase 8 placeholder), build the admin moderation queue for flagged reviews, and ensure rating aggregation updates search sorting.

Purpose: Completes the public-facing review display (REVW-08) and admin moderation workflow (REVW-07).
Output: Reviews visible on provider profiles, admin moderation page functional.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-avis-evaluations/08-05-SUMMARY.md
@src/app/[locale]/(client)/providers/[providerId]/page.tsx
@src/features/review/components/ReviewsList.tsx
@src/features/search/lib/search-query.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provider profile Avis tab integration with real reviews</name>
  <files>
    src/app/[locale]/(client)/providers/[providerId]/page.tsx
    src/features/review/actions/review-queries.ts
  </files>
  <action>
**1. Update provider profile page Avis tab:**
- Replace the Phase 8 placeholder in `src/app/[locale]/(client)/providers/[providerId]/page.tsx`:
  - Remove the placeholder div with star emoji and "Les avis seront disponibles prochainement"
  - Import ReviewsList from `@/features/review/components/ReviewsList`
  - In the Avis TabsContent, render `<ReviewsList providerId={provider.id} />`
  - The ReviewsList component handles its own data fetching, sorting, and pagination
  - Pass initialData if possible by fetching first page server-side for SSR

**2. Add initial review data fetching for SSR (optional optimization):**
- In the page server component, call `getProviderReviewsAction(provider.id, { page: 1, limit: 5, sort: 'recent' })`
- Pass result as `initialData` prop to ReviewsList
- This avoids a loading flash on initial tab switch

**3. Ensure rating aggregation is reflected in search:**
- The search already uses `provider.rating` for sorting (confirmed in search-query.ts: `orderBy = [{ provider: { rating: "desc" } }]`)
- The `updateProviderRating` function in review-queries.ts updates `provider.rating` and `provider.ratingCount`
- Verify that after updateProviderRating runs, the provider's rating field is correctly updated
- No code changes needed in search — it already sorts by provider.rating

**4. Add `getProviderRatingDistribution` to review-queries.ts:**
- Query to compute the 1-5 star distribution for a provider:
  - Group published, non-flagged reviews by `stars` value
  - Return `{ 1: count, 2: count, 3: count, 4: count, 5: count }`
- Used by ReviewsList/RatingBreakdown for the distribution bars
- Add to existing getProviderReviewsAction response or as separate helper
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to a provider profile page, click Avis tab, verify reviews display with breakdown</manual>
  </verify>
  <done>Provider profile Avis tab shows real reviews from database, rating breakdown, criteria chart, sorting, and pagination. Rating aggregation feeds into search sorting automatically.</done>
</task>

<task type="auto">
  <name>Task 2: Admin review moderation page</name>
  <files>
    src/app/[locale]/(admin)/admin/reviews/page.tsx
    src/features/review/actions/review-queries.ts
    src/features/review/actions/review-actions.ts
  </files>
  <action>
**1. Add admin query and actions to review module:**
- Add `getFlaggedReviewsAction(): Promise<ActionResult<FlaggedReview[]>>` to review-queries.ts:
  - Query reviews where flagged=true, isDeleted=false
  - Include booking info (service title), author name, target name
  - Order by createdAt desc (newest flagged first)
  - Return with flaggedReason

- Add `moderateReviewAction(reviewId: string, action: 'approve' | 'reject'): Promise<ActionResult<void>>` to review-actions.ts:
  - Validate admin session
  - If approve: set flagged=false, moderatedAt=now(), keep published status
  - If reject: set isDeleted=true, deletedAt=now(), moderatedAt=now()
  - After reject: recalculate provider rating if the review was published

**2. Create `src/app/[locale]/(admin)/admin/reviews/page.tsx`:**
- Server component page
- Auth guard: admin role only
- Fetch flagged reviews via getFlaggedReviewsAction
- Layout:
  - Page title: "Moderation des avis"
  - Count badge showing number of flagged reviews
  - Table/card list of flagged reviews:
    - Author name, author role (CLIENT/PROVIDER), target name
    - Stars, text content (full), photos (thumbnails)
    - Flagged reason (contact info detected, spam score, etc.)
    - Booking reference (service title, date)
    - CreatedAt date
  - Action buttons per review: "Approuver" (green) and "Supprimer" (red)
  - Use the AdminReviewActions client wrapper for the approve/reject buttons (useState for loading, calls moderateReviewAction, router.refresh on success)
  - Empty state: "Aucun avis signale" message
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to /admin/reviews and verify flagged reviews appear with moderation actions</manual>
  </verify>
  <done>Admin can view flagged reviews with full context (author, target, reason, content) and approve or reject them. Rejection soft-deletes the review and recalculates provider rating.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Provider profile Avis tab shows ReviewsList with real data (or empty state)
- Admin /reviews page shows flagged reviews with approve/reject actions
- Rating updates propagate to search sorting
</verification>

<success_criteria>
- Provider profile Avis tab replaces placeholder with real review list
- Rating breakdown and criteria chart visible on provider profile
- Admin can moderate flagged reviews (approve removes flag, reject soft-deletes)
- Provider.rating recalculated after moderation actions
- Search sorting by rating works with updated provider ratings
</success_criteria>

<output>
After completion, create `.planning/phases/08-avis-evaluations/08-06-SUMMARY.md`
</output>
