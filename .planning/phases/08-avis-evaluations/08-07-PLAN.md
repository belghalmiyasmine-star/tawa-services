---
phase: 08-avis-evaluations
plan: 07
type: execute
wave: 4
depends_on: ["08-06"]
files_modified:
  - src/app/[locale]/(admin)/admin/layout.tsx
  - src/components/layout/AdminSidebar.tsx
  - src/app/[locale]/(client)/bookings/page.tsx
  - src/app/[locale]/(provider)/provider/bookings/page.tsx
  - src/messages/fr.json
autonomous: true
requirements:
  - REVW-01
  - REVW-08

must_haves:
  truths:
    - "Admin sidebar includes link to review moderation page"
    - "Client and provider booking lists show review status indicators for completed bookings"
    - "End-to-end flow works: completed booking -> submit review -> simultaneous publication -> profile display"
  artifacts:
    - path: "src/components/layout/AdminSidebar.tsx"
      provides: "Admin sidebar with reviews moderation link"
      contains: "reviews"
  key_links:
    - from: "src/components/layout/AdminSidebar.tsx"
      to: "src/app/[locale]/(admin)/admin/reviews/page.tsx"
      via: "navigation link to /admin/reviews"
      pattern: "admin/reviews"
    - from: "src/app/[locale]/(client)/bookings/page.tsx"
      to: "src/features/review/actions/review-queries.ts"
      via: "review status check for completed bookings"
      pattern: "getReviewWindowAction|canReview"
---

<objective>
Wire navigation links for the review system: admin sidebar link to moderation page, review status indicators on booking lists, and verify the end-to-end review flow works correctly.

Purpose: Final integration ensuring all review features are accessible and connected (REVW-01, REVW-08).
Output: Fully wired review system with all navigation paths functional.
</objective>

<execution_context>
@C:/Users/pc dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-avis-evaluations/08-06-SUMMARY.md
@src/components/layout/AdminSidebar.tsx
@src/app/[locale]/(client)/bookings/page.tsx
@src/app/[locale]/(provider)/provider/bookings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin sidebar link and booking list review indicators</name>
  <files>
    src/components/layout/AdminSidebar.tsx
    src/app/[locale]/(client)/bookings/page.tsx
    src/app/[locale]/(provider)/provider/bookings/page.tsx
    src/messages/fr.json
  </files>
  <action>
**1. Add review moderation link to AdminSidebar:**
- Read `src/components/layout/AdminSidebar.tsx` to understand existing nav structure
- Add a new navigation item: "Moderation avis" with Star icon (lucide-react), href `/admin/reviews`
- Position: after KYC management, before other admin links
- Add i18n key: `navigation.adminReviews` = "Moderation avis" in fr.json under the `navigation` or `admin` namespace (follow existing pattern)
- Optionally: show pending flagged count badge (fetch count via API or server action — same pattern as KYC pending badge if one exists)

**2. Add review status indicators to client booking list:**
- Read `src/app/[locale]/(client)/bookings/page.tsx` to understand structure
- For COMPLETED bookings in the list, add a small indicator:
  - If review not submitted and window open: show "Laisser un avis" chip/link (amber, Star icon)
  - If review submitted but not published: show "Avis soumis, en attente" chip (gray)
  - If review published: show "Avis publie" chip (green, checkmark)
  - If window expired and not reviewed: no indicator (window closed)
- Implementation: The booking list component may need to include review status data
  - Option A: Add review status to the booking query (check if Review exists for this booking+userId)
  - Option B: Batch check via a new query that returns review status per bookingId list
  - Prefer Option A if the existing query can be extended with `include: { reviews: { where: { authorId }, select: { published: true } } }`

**3. Add review status indicators to provider booking list:**
- Same pattern as client but for provider side
- Read `src/app/[locale]/(provider)/provider/bookings/page.tsx`
- For COMPLETED bookings: show review indicators (same 3 states)
- Use authorRole=PROVIDER for the query filter

**4. Add any missing i18n keys to fr.json:**
- `navigation.adminReviews`: "Moderation avis"
- `review.statusPending`: "Avis soumis, en attente"
- `review.statusPublished`: "Avis publie"
- `review.leaveReview`: "Laisser un avis"
- `review.windowClosed`: "Periode d'evaluation terminee"
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -30 && node -e "JSON.parse(require('fs').readFileSync('src/messages/fr.json','utf8')); console.log('fr.json valid')"</automated>
    <manual>Check admin sidebar has "Moderation avis" link. Check client and provider booking lists show review status for COMPLETED bookings.</manual>
  </verify>
  <done>Admin sidebar links to /admin/reviews. Booking lists show review status (pending, submitted, published) for completed bookings. All i18n keys present.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end flow verification and final cleanup</name>
  <files>
    src/features/review/actions/review-actions.ts
    src/features/review/actions/review-queries.ts
  </files>
  <action>
**Verify and fix the complete review flow:**

1. **Booking completion -> review CTA:**
   - Confirm that when a booking reaches COMPLETED status, both client and provider booking detail pages show the review button
   - Verify the 10-day window calculation uses booking.completedAt correctly

2. **Review submission flow:**
   - Confirm submitReviewAction correctly:
     - Validates all 4 criteria + overall stars + text
     - Runs moderation check
     - Creates review with correct authorId/targetId
     - Triggers publication check
   - Confirm photo upload integrates with the review create

3. **Simultaneous publication:**
   - Confirm that when second review is submitted:
     - Both reviews set to published=true
     - Provider.rating and ratingCount updated
     - UI shows "Les avis ont ete publies" feedback

4. **Provider profile display:**
   - Confirm ReviewsList loads on Avis tab
   - Confirm RatingBreakdown shows correct distribution
   - Confirm CriteriaRadarChart shows correct averages

5. **Search integration:**
   - Confirm search sorting by "Meilleure note" uses the updated provider.rating
   - No code change needed — just verify the existing orderBy pattern works

6. **Admin moderation:**
   - Confirm flagged reviews appear in /admin/reviews
   - Confirm approve/reject actions work correctly

**Fix any TypeScript compilation issues found during the flow verification.**
**Ensure all imports resolve correctly across the review module.**
  </action>
  <verify>
    <automated>cd "C:/Users/pc dell/tawa-services" && npx tsc --noEmit --pretty 2>&1 | head -40 && npm run build 2>&1 | tail -20</automated>
    <manual>Run through the complete flow: mark booking completed, submit client review, submit provider review, verify both published on profile, check search sorting</manual>
  </verify>
  <done>Complete review flow verified end-to-end: submission, moderation, simultaneous publication, profile display, search integration, admin moderation. No TypeScript errors. Build passes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` passes
- fr.json is valid JSON
- Admin sidebar has review moderation link
- Booking lists show review status indicators
- Full flow: completed booking -> review form -> submit -> publication -> profile display -> search sort
</verification>

<success_criteria>
- Admin can navigate to review moderation from sidebar
- Completed bookings show clear review CTAs
- After both reviews submitted, they appear on provider profile
- Provider rating updates affect search ranking
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-avis-evaluations/08-07-SUMMARY.md`
</output>
