---
phase: 03-verification-kyc
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-04"]
files_modified:
  - src/components/shared/KycBanner.tsx
  - src/app/[locale]/(provider)/provider/dashboard/page.tsx
  - src/app/[locale]/(admin)/admin/page.tsx
autonomous: false
requirements:
  - KYC-05

must_haves:
  truths:
    - "KYC guard is documented in provider layout for Phase 4 service creation pages (guard infrastructure in place, enforced at page-level in Phase 4)"
    - "Provider dashboard shows banner prompting KYC verification"
    - "Provider CAN access dashboard and messaging before KYC approval"
    - "Admin dashboard shows count of pending KYC verifications"
  artifacts:
    - path: "src/components/shared/KycBanner.tsx"
      provides: "KYC prompt banner for provider dashboard"
      min_lines: 20
    - path: "src/app/[locale]/(provider)/provider/dashboard/page.tsx"
      provides: "Updated provider dashboard with KYC banner and badge display"
      min_lines: 30
    - path: "src/app/[locale]/(admin)/admin/page.tsx"
      provides: "Updated admin dashboard with KYC pending count widget"
      min_lines: 25
  key_links:
    - from: "src/components/shared/KycBanner.tsx"
      to: "/provider/kyc"
      via: "Link navigation to KYC page"
      pattern: "href.*provider/kyc"
    - from: "src/app/[locale]/(provider)/provider/dashboard/page.tsx"
      to: "prisma.provider"
      via: "server component kycStatus check"
      pattern: "provider\\.kycStatus"
    - from: "src/app/[locale]/(provider)/provider/dashboard/page.tsx"
      to: "src/components/shared/TrustBadges.tsx"
      via: "import and render TrustBadges component"
      pattern: "import.*TrustBadges"
---

<objective>
Implement KYC guard preventing non-approved providers from listing services, add KYC prompt banner to provider dashboard, and update admin dashboard with pending KYC count widget. Wire TrustBadges display on provider dashboard.

Purpose: Ensure non-verified providers are blocked from service creation with clear guidance, and admins have visibility into pending KYC workload.
Output: KYC banner on provider dashboard, service creation guard, admin KYC widget.
</objective>

<execution_context>
@C:/Users/LENOVO/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verification-kyc/CONTEXT.md
@.planning/phases/03-verification-kyc/03-02-SUMMARY.md
@.planning/phases/03-verification-kyc/03-04-SUMMARY.md
@src/app/[locale]/(provider)/provider/dashboard/page.tsx
@src/app/[locale]/(admin)/admin/page.tsx
@src/components/shared/TrustBadges.tsx
@src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KYC banner and update provider/admin dashboards</name>
  <files>
    src/components/shared/KycBanner.tsx
    src/app/[locale]/(provider)/provider/dashboard/page.tsx
    src/app/[locale]/(admin)/admin/page.tsx
  </files>
  <action>
1. Create `src/components/shared/KycBanner.tsx` — server component (no "use client"):
   - Props: `kycStatus: "NOT_SUBMITTED" | "PENDING" | "APPROVED" | "REJECTED"`, `locale: string`
   - Render logic:
     a. If APPROVED: return null (no banner needed)
     b. If NOT_SUBMITTED or REJECTED:
        - Yellow/amber background banner (bg-amber-50 dark:bg-amber-950 border border-amber-200)
        - `ShieldAlert` icon (lucide), text from `useTranslations("kyc")`: "Completez votre verification d'identite pour commencer a proposer vos services"
        - `Link` button to `/provider/kyc`: "Verifier mon identite"
        - If REJECTED: show additional text "Votre dossier a ete rejete. Soumettez a nouveau."
     c. If PENDING:
        - Blue/info background banner (bg-blue-50 dark:bg-blue-950 border border-blue-200)
        - `Hourglass` icon, text: "Votre dossier est en cours de verification"
        - `Link` to `/provider/kyc` to check status: "Voir le statut"
   - Use `getTranslations("kyc")` (server-side) for all text
   - Use `Link` from `@/i18n/routing` for locale-aware links

2. Update `src/app/[locale]/(provider)/provider/dashboard/page.tsx`:
   - Convert to server component (remove `useTranslations`, use `getTranslations`)
   - Import `getServerSession` and `authOptions` — get current user session
   - Query provider record: `prisma.provider.findUnique({ where: { userId: session.user.id }, include: { trustBadges: { where: { isActive: true } } } })`
   - Render `<KycBanner kycStatus={provider.kycStatus} locale={locale} />` at top of page
   - Below banner, render `<TrustBadges badges={provider.trustBadges} kycStatus={provider.kycStatus} size="md" />` showing current badge status
   - Keep existing placeholder content for dashboard (will be fully built in Phase 4)
   - Use `getTranslations("navigation")` and `getTranslations("placeholder")` for existing strings

3. Update `src/app/[locale]/(admin)/admin/page.tsx`:
   - Convert to server component (it already is — uses `useTranslations` which should be `getTranslations`)
   - Add KYC pending count query: `prisma.provider.count({ where: { kycStatus: "PENDING" } })`
   - Update the "kyc" KPI card to show the actual pending count instead of hardcoded "0"
   - Make the KYC card clickable — wrap in `Link` to `/admin/kyc`
   - Add visual indicator: if count > 0, show amber dot next to the count
   - Use `getTranslations` instead of `useTranslations` for server component compatibility

4. **KYC guard for service creation** — Note: Service creation pages don't exist yet (Phase 4). The guard will be implemented as:
   - Add a comment in the provider layout `src/app/[locale]/(provider)/layout.tsx` documenting: "// KYC guard: Phase 4 service creation pages must check provider.kycStatus === APPROVED before allowing access"
   - The actual guard will be a server-side check in Phase 4's service creation page, NOT middleware-level (per CONTEXT.md: provider CAN access dashboard and messaging before approval, only service listing is blocked)
   - Do NOT modify middleware.ts for KYC guard — the guard is page-level, not route-level
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Provider dashboard shows KYC banner for non-approved providers
    - Provider dashboard shows TrustBadges component
    - Admin dashboard shows real KYC pending count
    - Admin KYC card links to `/admin/kyc`
    - No middleware changes (guard is page-level for Phase 4)
  </verify>
  <done>Provider dashboard shows contextual KYC banner (complete/check status/resubmit based on kycStatus) and trust badges. Admin dashboard displays real-time pending KYC count with link to review page. Service creation guard documented for Phase 4 implementation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete KYC workflow end-to-end</name>
  <files>no files — human verification only</files>
  <action>
Human verifies the complete KYC workflow built across Plans 01-05:
- Provider 4-step document upload wizard
- Admin review interface with approve/reject
- Trust badges awarded on approval
- KYC banner on provider dashboard
- Pending KYC count on admin dashboard

Steps to verify:
1. Navigate to `http://localhost:3000/fr/auth/login` — log in as a PROVIDER user
2. Visit `http://localhost:3000/fr/provider/dashboard` — verify KYC banner appears: "Completez votre verification d'identite..."
3. Click "Verifier mon identite" link — should navigate to `/provider/kyc`
4. Walk through the 4-step wizard:
   a. Step 1: Upload a JPG/PNG image as CIN recto — verify preview shows
   b. Step 2: Upload CIN verso
   c. Step 3: Upload selfie — verify instruction text visible
   d. Step 4: Upload proof of address
5. Verify review screen shows all 4 documents in grid
6. Click "Soumettre mes documents" — verify confirmation dialog appears
7. Confirm submission — verify success toast and status changes to "En attente de verification"
8. Log in as ADMIN user
9. Visit `http://localhost:3000/fr/admin` — verify KYC pending count shows >= 1
10. Click the KYC card or navigate to `/admin/kyc` — verify submission list shows the provider
11. Click on the submission — verify all 4 documents display, zoomable on click
12. Click "Approuver" — verify success and redirect back to list
13. Log back in as the PROVIDER — verify dashboard shows "Identite Verifiee" green badge and banner is gone
14. Verify trust badge with blue BadgeCheck icon appears

Resume signal: Type "approved" if the KYC workflow works end-to-end, or describe any issues found.
  </action>
  <verify>Human confirms all 14 verification steps pass successfully</verify>
  <done>Complete KYC workflow verified end-to-end: upload -> submit -> admin review -> approve -> badge display -> banner removal</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Provider dashboard correctly renders KYC banner based on status
- Admin dashboard shows real pending KYC count
- Full workflow: upload -> submit -> admin review -> approve/reject -> status update
- Trust badges display correctly after approval
- Non-verified providers see appropriate banner messaging
- Rejected providers can resubmit
</verification>

<success_criteria>
- Provider with non-approved KYC sees banner on dashboard with link to KYC page
- KYC banner disappears after approval
- Admin sees pending KYC count on main dashboard
- Complete KYC workflow verified end-to-end by human tester
- Service creation guard documented for Phase 4
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-kyc/03-05-SUMMARY.md`
</output>
