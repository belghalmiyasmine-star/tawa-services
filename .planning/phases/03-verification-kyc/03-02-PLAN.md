---
phase: 03-verification-kyc
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/[locale]/(provider)/provider/kyc/page.tsx
  - src/features/kyc/components/KycWizard.tsx
  - src/features/kyc/components/KycDocumentUpload.tsx
  - src/features/kyc/components/KycStatusPage.tsx
autonomous: true
requirements:
  - KYC-01
  - KYC-02
  - KYC-03

must_haves:
  truths:
    - "Provider sees a 4-step wizard to upload CIN recto, CIN verso, selfie, and proof of address"
    - "Provider can drag-and-drop or click to upload each document with image preview"
    - "Provider sees a review screen with all 4 documents before final submission"
    - "After submission, provider sees KYC status (pending/approved/rejected)"
    - "Rejected provider can resubmit documents"
  artifacts:
    - path: "src/app/[locale]/(provider)/provider/kyc/page.tsx"
      provides: "KYC page routing and data loading"
      min_lines: 20
    - path: "src/features/kyc/components/KycWizard.tsx"
      provides: "4-step wizard with progress indicator"
      min_lines: 80
    - path: "src/features/kyc/components/KycDocumentUpload.tsx"
      provides: "Drag-and-drop upload zone with preview"
      min_lines: 50
    - path: "src/features/kyc/components/KycStatusPage.tsx"
      provides: "Status display with pending/approved/rejected states"
      min_lines: 40
  key_links:
    - from: "src/features/kyc/components/KycWizard.tsx"
      to: "/api/kyc/upload"
      via: "fetch multipart upload"
      pattern: "fetch.*api/kyc/upload"
    - from: "src/features/kyc/components/KycWizard.tsx"
      to: "submitKycAction"
      via: "server action call on final submit"
      pattern: "submitKycAction"
    - from: "src/features/kyc/components/KycStatusPage.tsx"
      to: "provider.kycStatus"
      via: "server component data loading"
      pattern: "kycStatus"
---

<objective>
Build the provider-facing KYC wizard (4-step upload) and status page at `/provider/kyc`. Provider uploads CIN recto, CIN verso, selfie holding CIN, and proof of address through a step-by-step wizard matching Phase 2 registration style. After submission, provider sees their KYC status.

Purpose: Providers need a clear, guided flow to submit their identity documents for verification.
Output: Working KYC wizard with upload, preview, review, and status tracking.
</objective>

<execution_context>
@C:/Users/LENOVO/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verification-kyc/CONTEXT.md
@.planning/phases/03-verification-kyc/03-01-SUMMARY.md
@prisma/schema.prisma
@src/app/[locale]/(provider)/layout.tsx
@src/features/auth/actions/register.ts
@src/lib/validations/kyc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KYC document upload component and wizard</name>
  <files>
    src/features/kyc/components/KycDocumentUpload.tsx
    src/features/kyc/components/KycWizard.tsx
  </files>
  <action>
1. Create `src/features/kyc/components/KycDocumentUpload.tsx` — "use client" component:
   - Props: `docType: string`, `label: string`, `description: string`, `onFileUploaded: (fileUrl: string) => void`, `existingUrl?: string`, `disabled?: boolean`
   - Drag-and-drop zone using native HTML5 drag events (onDragOver, onDragLeave, onDrop) — NO external library
   - Click-to-upload via hidden `<input type="file" accept="image/jpeg,image/png" />` triggered by zone click
   - Client-side validation BEFORE upload:
     a. Check MIME type against `ALLOWED_MIME_TYPES` from `@/lib/validations/kyc` — show toast error if invalid
     b. Check file.size <= `MAX_FILE_SIZE` — show toast error "Le fichier ne doit pas depasser 5 Mo"
   - Upload via `fetch("/api/kyc/upload", { method: "POST", body: formData })` where formData has `file` and `docType`
   - Show upload progress state: idle -> uploading (spinner) -> uploaded (preview) -> error
   - After successful upload, show image preview using `next/image` with the returned fileUrl
   - "Reprendre la photo" button (retake) resets to upload state
   - Use shadcn `Card` for the drop zone container with dashed border
   - Use Tailwind for all styling — rounded-lg, border-dashed, hover:border-primary transition
   - Use camera icon (lucide-react `Camera`) for selfie step, file icon (`FileUp`) for other steps
   - All text via `useTranslations("kyc")` — no hardcoded French strings

2. Create `src/features/kyc/components/KycWizard.tsx` — "use client" component:
   - Props: `providerId: string`, `onComplete: () => void`
   - 4 steps matching CONTEXT.md: CIN Recto, CIN Verso, Selfie avec CIN, Justificatif de domicile
   - Step state management via `useState<number>(1)` (steps 1-5, where 5 is review screen)
   - Track uploaded files as `Record<string, string>` mapping docType to fileUrl
   - Progress indicator at top: horizontal stepper showing step numbers with active/complete/pending states
   - Each step renders `<KycDocumentUpload>` with appropriate docType, label, description
   - Step 3 (selfie) shows instruction text from CONTEXT.md: "Prenez une photo de vous tenant votre CIN a cote de votre visage"
   - Navigation: "Precedent" and "Suivant" buttons — "Suivant" disabled until current step's file is uploaded
   - Step 5 (Review screen): display all 4 uploaded images in 2x2 grid with labels, "Soumettre mes documents" button
   - On submit: show confirmation dialog (shadcn `Dialog`) with text "Vos documents seront examines sous 48h"
   - On confirm: call `submitKycAction` with `{ documents: [{docType, fileUrl}, ...] }`
   - Handle loading state during submission (disable button, show spinner)
   - On success: call `onComplete()`, show success toast via sonner
   - On error: show error toast with message from action result
   - Use shadcn `Button`, `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter`
   - Mobile-first layout: full-width on mobile, max-w-2xl centered on desktop
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - KycDocumentUpload renders a drag-and-drop zone, calls /api/kyc/upload, shows preview
    - KycWizard renders 4 steps + review, calls submitKycAction on final submit
  </verify>
  <done>Provider can navigate through a 4-step wizard, upload each document with drag-and-drop, preview uploads, and submit all documents. Upload validates file type and size client-side.</done>
</task>

<task type="auto">
  <name>Task 2: Create KYC status page and wire route</name>
  <files>
    src/features/kyc/components/KycStatusPage.tsx
    src/app/[locale]/(provider)/provider/kyc/page.tsx
  </files>
  <action>
1. Create `src/features/kyc/components/KycStatusPage.tsx` — client component:
   - Props: `status: "NOT_SUBMITTED" | "PENDING" | "APPROVED" | "REJECTED"`, `submittedAt?: Date | null`, `approvedAt?: Date | null`, `rejectedAt?: Date | null`, `rejectedReason?: string | null`, `onResubmit: () => void`
   - Render based on status:
     a. NOT_SUBMITTED: should not normally show (redirect to wizard)
     b. PENDING: Yellow/amber Badge with hourglass icon (lucide `Hourglass`), text "En attente de verification", submission date, message "Vos documents seront examines sous 48h"
     c. APPROVED: Green Badge with checkmark icon (lucide `BadgeCheck`), text "Identite verifiee", approval date, congratulatory message
     d. REJECTED: Red Badge with X icon (lucide `XCircle`), text "Rejete", rejection date, display rejection reason, "Soumettre a nouveau" button calling `onResubmit`
   - Timeline visualization: vertical timeline with dots showing submission date and review date (if available)
   - Use shadcn `Badge`, `Card`, `Button`
   - All text via `useTranslations("kyc")`
   - Mobile-first: full-width on mobile, max-w-lg centered on desktop

2. Create `src/app/[locale]/(provider)/provider/kyc/page.tsx` — server component page:
   - Get session via `getServerSession(authOptions)` — should already be authenticated by layout
   - Get provider record with `prisma.provider.findUnique({ where: { userId: session.user.id }, include: { kycDocuments: true } })`
   - If no provider record, redirect to home (shouldn't happen for PROVIDER role, but safety check)
   - Render logic:
     a. If `kycStatus === "NOT_SUBMITTED"` or `kycStatus === "REJECTED"`: render `<KycWizard>` component
     b. If `kycStatus === "PENDING"` or `kycStatus === "APPROVED"`: render `<KycStatusPage>` with status data
   - For REJECTED status, initially show `<KycStatusPage>` with resubmit button — on resubmit, switch to wizard (use client wrapper component with state toggle, or use URL search params `?resubmit=true`)
   - Pass `providerId` to KycWizard, pass status props to KycStatusPage
   - Use `getTranslations("kyc")` for page metadata
   - Export `generateMetadata` for SEO: title = "Verification d'identite | Tawa Services"
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Navigating to `/provider/kyc` shows the wizard for NOT_SUBMITTED status
    - After submission, page shows PENDING status with correct information
    - REJECTED status shows reason and resubmit button
  </verify>
  <done>Provider at `/provider/kyc` sees the upload wizard if documents not yet submitted or if resubmitting after rejection. After submission, provider sees status page with pending/approved/rejected state, timeline, and resubmit option for rejected submissions.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- `/provider/kyc` route renders correctly for each KYC status
- Wizard progress indicator shows current step clearly
- Drag-and-drop upload works with client-side validation
- Image preview displays after successful upload
- Review screen shows all 4 documents before submission
- Confirmation dialog appears before final submission
- Status page renders correct badge/color/icon for each status
- Resubmit flow works for rejected providers
- All text rendered via `useTranslations("kyc")` — no hardcoded French strings
</verification>

<success_criteria>
- A provider with NOT_SUBMITTED status sees the 4-step wizard at /provider/kyc
- Provider can upload each document with preview and navigate between steps
- Provider reviews all 4 documents and submits with confirmation
- After submission, provider sees "En attente de verification" status
- Rejected provider sees rejection reason and can resubmit
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-kyc/03-02-SUMMARY.md`
</output>
